
//: Declare String Begin

/*: "cmd" :*/
fileprivate let user_infoName:[UInt8] = [0x60,0x6e,0x67]

/*: "cancelCall" :*/
fileprivate let showAddDefineContent:String = "calargece"

/*: "data" :*/
fileprivate let appSocialAtMakeName:[UInt8] = [0x35,0x30,0x25,0x30]

private func listDistance(make num: UInt8) -> UInt8 {
    return num ^ 81
}

/*: "logId" :*/
fileprivate let dataModelTitle:String = "shared return message all whitelogId"

/*: "#322870" :*/
fileprivate let notiAlongMsg:[Character] = ["#","3","2","2","8","7","0"]

/*: "Tap to start video chatting" :*/
fileprivate let constScreenUrl:String = "display mm let varTap t"
fileprivate let const_joinMessage:[Character] = ["a","r","t"," ","v","i","d","e","o"," ","c","h","a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let app_infoTitle:String = "icon_cvar self hang"
fileprivate let data_messagePath:String = "losstart"

/*: "icon_views_number" :*/
fileprivate let showTimeKey:String = "istatusn"
fileprivate let noti_kitData:String = "make let height water selfws_n"
fileprivate let constToName:String = "uplayer"

/*: "%@ chatting" :*/
fileprivate let kBeautyTouchId:[Character] = ["%","@"," ","c","h","a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let appEqualMsg:String = "icon_lousuper model to"
fileprivate let k_resultModelTitle:String = "gesturee"

/*: "icon_rank_coin" :*/
fileprivate let notiFileName:String = "live self screen tableicon_"
fileprivate let show_tableInsideValue:[Character] = ["c","o","i","n"]

/*: "vip_btn_top" :*/
fileprivate let app_giftConstraintPath:String = "vip_feedback day table"
fileprivate let data_currentKey:String = "btn_topof view conversation model"

/*: "btn_nearby_settings_nor" :*/
fileprivate let data_cornerUrl:String = "btn_like data equal corner card"
fileprivate let constTitleMessage:String = "by_setmake show inside let var"
fileprivate let main_hiddenToolId:String = "data trues_nor"

/*: "%@ Coins/min" :*/
fileprivate let const_pathTitle:[Character] = ["%","@"," ","C","o","i","n","s","/","m","i"]
fileprivate let data_toMessage:String = "table"

/*: "get json error" :*/
fileprivate let data_centerMessage:String = "get jload pop true array else"
fileprivate let k_dataStyleFormat:String = "emptyrror"

/*: "matchId" :*/
fileprivate let k_ofId:String = "matchIdevent for"

/*: "errno" :*/
fileprivate let main_equalUrl:[UInt8] = [0xa5,0xb2,0xb2,0xae,0xaf]

private func extraLast(up num: UInt8) -> UInt8 {
    return num ^ 192
}

/*: "VIPFee" :*/
fileprivate let kTitleMsg:String = "VIPFeeany object let"

/*: "Matching now" :*/
fileprivate let dataPointStr:String = "Matchstring phase position"

/*: "userTouch" :*/
fileprivate let app_hostCellGroupStr:String = "uscolor"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let kValueTitle:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V","i"]
fileprivate let k_honeyName:String = "show if count return elsedeoInfo"

/*: "fromType" :*/
fileprivate let const_equalPartyData:[UInt8] = [0xfb,0xef,0xf2,0xf0,0xc9,0xe4,0xed,0xf8]

private func pushRecord(begin num: UInt8) -> UInt8 {
    return num ^ 157
}

/*: "You've been barred from receiving calls" :*/
fileprivate let showTitleKey:[UInt8] = [0x73,0x6c,0x6c,0x61,0x63,0x20,0x67,0x6e,0x69,0x76,0x69,0x65,0x63,0x65,0x72,0x20,0x6d,0x6f,0x72,0x66,0x20,0x64,0x65,0x72,0x72,0x61,0x62,0x20,0x6e,0x65,0x65,0x62,0x20,0x65,0x76,0x27,0x75,0x6f,0x59]

/*: "source" :*/
fileprivate let noti_shadowFormat:[UInt8] = [0x8b,0x87,0x8d,0x8a,0x7b,0x7d]

fileprivate func modelTransform(running num: UInt8) -> UInt8 {
    let value = Int(num) - 24
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let userWarningValue:[UInt8] = [0x58,0x5d,0x54,0x49]

fileprivate func renderLocation(title num: UInt8) -> UInt8 {
    let value = Int(num) - 228
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let noti_addUrl:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let show_makeIncludeUrl:[Character] = ["f","r","o","m","F","r","e","e","C"]
fileprivate let show_arrayLayerId:[Character] = ["a","l","l"]

/*: "requestCall" :*/
fileprivate let show_colorName:[Character] = ["r","e","q","u","e"]
fileprivate let main_styleStr:[Character] = ["s","t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let data_downUrl:[Character] = ["o","n","V","i","d","e","o","M","a","t"]
fileprivate let mainModeKey:[Character] = ["c","h"]

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let noti_tapFrameData:[UInt8] = [0x49,0x53,0x67,0x66,0x7d,0x7f,0x73,0x66,0x7b,0x71,0x32,0x64,0x7b,0x76,0x77,0x7d,0x4f,0x32,0x40,0x77,0x71,0x77,0x7b,0x64,0x77,0x32,0x64,0x7b,0x76,0x77,0x7d,0x32,0x71,0x73,0x7e,0x7e,0x32,0x7b,0x7c,0x71,0x7d,0x7f,0x7b,0x7c,0x75]

private func nameIndexNone(table num: UInt8) -> UInt8 {
    return num ^ 18
}

/*: "refuseCall" :*/
fileprivate let constOverFormat:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let app_modelFormat:[Character] = ["o","n","C","a","n","c","e","l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let const_fieldPath:String = "self liveretryA"
fileprivate let main_viewMsg:String = "datater"

/*: "onRequestCall" :*/
fileprivate let user_monthPath:[Character] = ["o","n","R","e","q","u","e","s","t"]
fileprivate let userWithMatchValue:String = "equal view nameCall"

/*: "failType" :*/
fileprivate let notiCollectionTitle:[Character] = ["f","a","i"]
fileprivate let user_viewMomentTargetData:[Character] = ["l","T","y","p","e"]

/*: "callWaitingTime" :*/
fileprivate let dataMakeBeautyStr:String = "callWacontent except return number"
fileprivate let const_requestStr:String = "color with intimate import truegTime"

/*: "toUid" :*/
fileprivate let main_succeedName:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let dataConnectionUrl:String = "key"
fileprivate let dataAddFormat:[Character] = ["r","e","e"," ","1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let dataMinValue:String = "date return plain extra%@/min"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ColorCallerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class ColorCallerDelegate: SizeViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.shadowRefer()
            }
        }
        //: self.resetCamera()
        self.systemaLymphaticum()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        plainspoken()
        //: setupSubViewsConstraint()
        coverMessage()
        //: bindInteraction()
        appQuantityeraction()
        //: videoManager.startPreview(isMatch: true)
        videoManager.outEqual(isMatch: true)
        //: setPlayer(isRandon: false)
        faceManager(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            partyEffect()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: user_infoName.map{$0^3}, encoding: .utf8)!: (showAddDefineContent.replacingOccurrences(of: "large", with: "n") + "lCall"), String(bytes: appSocialAtMakeName.map{listDistance(make: $0)}, encoding: .utf8)!: [(String(dataModelTitle.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        forename()
        //: if TalkingSocketManager.shared.workSpace == false {
        if WrittenRecordSocketDelegate.shared.workSpace == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            WrittenRecordSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            WrittenRecordSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            WrittenRecordSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            WrittenRecordSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: SubSizeThen = {
        //: let m = TalkingVideoChatManager()
        let m = SubSizeThen()
        //: let info = TalkingVideoChatModel()
        let info = ExamineRequestChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .toEvent()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(partyEffect), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: kQueryValue + 1, height: notiKeyTitle + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(notiAlongMsg)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(constScreenUrl.suffix(5)) + "o st" + String(const_joinMessage)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.thoughtImageBundleSizeText(name: (String(app_infoTitle.prefix(6)) + data_messagePath.replacingOccurrences(of: "start", with: "e") + "_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fastenerObserver), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: StartImageButton = {
        //: let btn = TalkingButton()
        let btn = StartImageButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.thoughtImageBundleSizeText(name: (showTimeKey.replacingOccurrences(of: "status", with: "co") + "_vie" + String(noti_kitData.suffix(4)) + constToName.replacingOccurrences(of: "play", with: "mb"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.thoughtImageBundleSizeText(name: (showTimeKey.replacingOccurrences(of: "status", with: "co") + "_vie" + String(noti_kitData.suffix(4)) + constToName.replacingOccurrences(of: "play", with: "mb"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.clickLarge(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(kBeautyTouchId)).viewfinder(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.thoughtImageBundleSizeText(name: (String(appEqualMsg.prefix(8)) + k_resultModelTitle.replacingOccurrences(of: "gesture", with: "ng")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.thoughtImageBundleSizeText(name: (String(notiFileName.suffix(5)) + "rank_" + String(show_tableInsideValue)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.clickLarge(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.thoughtImageBundleSizeText(name: (String(app_giftConstraintPath.prefix(4)) + String(data_currentKey.prefix(7))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: StartImageButton = {
        //: let btn = TalkingButton()
        let btn = StartImageButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(livelihood), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.thoughtImageBundleSizeText(name: (String(data_cornerUrl.prefix(4)) + "near" + String(constTitleMessage.prefix(6)) + "ting" + String(main_hiddenToolId.suffix(5)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.thoughtImageBundleSizeText(name: (String(notiFileName.suffix(5)) + "rank_" + String(show_tableInsideValue)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .utiliserColor()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(const_pathTitle) + data_toMessage.replacingOccurrences(of: "table", with: "n")).viewfinder(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.clickLarge(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: LayerSomebodyView = //: return LayerSomebodyView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension ColorCallerDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func faceManager(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = SkullSessionReactiveCompatible.default.region(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(data_centerMessage.prefix(5)) + "son " + k_dataStyleFormat.replacingOccurrences(of: "empty", with: "e")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func errno(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(k_ofId.prefix(7))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: user_infoName.map{$0^3}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: main_equalUrl.map{extraLast(up: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        QuaternaryVideoManager.atApp(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.toehold()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(k_ofId.prefix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func stateEqual() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        QuaternaryVideoManager.colouredOn { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(kTitleMsg.prefix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.shadowRefer()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension ColorCallerDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func livelihood() {
        //: RangePushManager.share.func__pushToSubscribeAlert()
        RangePushManager.share.quantityernalRepresentation()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func fastenerObserver() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        presentUser()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        const_screenFormat.occurrenceIn(eventID: mainFormatMessage)

        //: if PunctuateThen.share.appConfigMode.displayMatchV4PopUp, PunctuateThen.share.loginUserMode.freeCallTimes > 0,
        if PunctuateThen.share.appConfigMode.displayMatchV4PopUp, PunctuateThen.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.shadowTheme()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        occurrentWith()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func partyEffect() {
        //: guard TalkingSocketManager.shared.workSpace == false else {
        guard WrittenRecordSocketDelegate.shared.workSpace == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.drawObserver(showMsg: mainKeyTransformMsg)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        textWith()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(dataPointStr.prefix(5)) + "ing now").localized
        //: setPlayer(isRandon: true)
        faceManager(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if PunctuateThen.share.loginUserMode.randomVideoType == 2 {
        if PunctuateThen.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            const_screenFormat.occurrenceIn(eventID: constChangeFormat)
            //: req_getVideoInfo()
            earn()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            const_screenFormat.occurrenceIn(eventID: notiStatusTitle)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            WrittenRecordSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            WrittenRecordSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            WrittenRecordSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            WrittenRecordSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            voiceEnter()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func voiceEnter(cmd: String = (app_hostCellGroupStr.replacingOccurrences(of: "color", with: "er") + "Touch"), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        forename()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        self.errno(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func toehold() {
        // 停止定时器
        //: destroyWaitingTimer()
        forename()
        //: showCloseBtn()
        presentUser()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(constScreenUrl.suffix(5)) + "o st" + String(const_joinMessage)).localized
        //: setPlayer(isRandon: false)
        faceManager(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.workSpace == false {
        if WrittenRecordSocketDelegate.shared.workSpace == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            WrittenRecordSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            WrittenRecordSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            WrittenRecordSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            WrittenRecordSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        shadowRefer()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func forename() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func earn() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(kValueTitle) + String(k_honeyName.suffix(7)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: const_equalPartyData.map{pushRecord(begin: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.toehold()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: PunctuateThen.share.loginUserMode.randomVideoType = 1
                    PunctuateThen.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.partyEffect()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.drawObserver(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = CustomHandyJSON.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = MemoryReactiveCompatible()
                //: player.setMute(bEnable: true)
                player.valueDoing(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.asDrunk(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                TvThen.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                CustomPermissionTool.showDrunk { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.timeThrough()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func timeThrough() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        const_screenFormat.occurrenceIn(eventID: appOkName)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard TvThen.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.drawObserver(showMsg: String(bytes: showTitleKey.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            TvThen.shared.finishBecome(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.toehold()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = AddObjectProtocol()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.occurrentWith()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.toehold()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func textWith() {
        //: if PunctuateThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if PunctuateThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(PunctuateThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(PunctuateThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func presentUser() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func systemaLymphaticum() {
        //: guard TalkingSocketManager.shared.workSpace == false else {
        guard WrittenRecordSocketDelegate.shared.workSpace == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.outEqual()
        }
    }
}

// MARK: - VoiceObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension ColorCallerDelegate: VoiceObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func onObject(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = TitleMeasurable.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(k_ofId.prefix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: noti_shadowFormat.map{modelTransform(running: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: userWarningValue.map{renderLocation(title: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: noti_addUrl.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = ColorHashableRepresentation.randow
        //: if PunctuateThen.share.loginUserMode.freeCallTimes > 0 && PunctuateThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if PunctuateThen.share.loginUserMode.freeCallTimes > 0 && PunctuateThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(show_makeIncludeUrl) + String(show_arrayLayerId)))
            //: TalkingSocketManager.shared.isFreeCall = true
            WrittenRecordSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: user_infoName.map{$0^3}, encoding: .utf8)!: (String(show_colorName) + String(main_styleStr)), String(bytes: appSocialAtMakeName.map{listDistance(make: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        WrittenRecordSocketDelegate.shared.belowAdd(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: user_infoName.map{$0^3}, encoding: .utf8)!: (showAddDefineContent.replacingOccurrences(of: "large", with: "n") + "lCall"), String(bytes: appSocialAtMakeName.map{listDistance(make: $0)}, encoding: .utf8)!: [(String(dataModelTitle.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.voiceEnter(cmd: (String(data_downUrl) + String(mainModeKey)))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func match(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(dataModelTitle.suffix(5)))].intValue
    }
}

// MARK: - PurveyorCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension ColorCallerDelegate: PurveyorCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func scrubCheck(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func behindOmit(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func showAggregationData(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func guide(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            FourthBaseDelegate.totaleraction(body: String(bytes: noti_tapFrameData.map{nameIndexNone(table: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = TitleMeasurable.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            timeUploadToTriumph(toUid: String(uid))
        }
        //: stop_randomVideo()
        toehold()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.indexCurrent()
        //: PunctuateThen.share.start1v1TalkCall(info: data, situation: .Calling)
        PunctuateThen.share.contestant(info: data, situation: .Calling)
    }
}

// MARK: - DataRequestObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ColorCallerDelegate: DataRequestObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func primaryColour(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        voiceEnter(cmd: (String(constOverFormat)))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func groupPic(data _: [String: Any]) {}
}

// MARK: - SendToErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension ColorCallerDelegate: SendToErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func nameUseData(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(app_modelFormat)) { // 取消通话异常
            //: stop_randomVideo()
            toehold()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(data_downUrl) + String(mainModeKey)), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            toehold()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(k_ofId.prefix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.drawObserver(showMsg: msg)
            //: uploadFailTime(type: 2)
            strikeOut(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(data_downUrl) + String(mainModeKey)), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(const_fieldPath.suffix(6)) + main_viewMsg.replacingOccurrences(of: "data", with: "f"))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(k_ofId.prefix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.voiceEnter(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(user_monthPath) + String(userWithMatchValue.suffix(4))), errorNo == ColorMirrorPath.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            toehold()
            //: guard PunctuateThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PunctuateThen.share.appStatus == HiddenHashableRepresentation.normal.rawValue else { return }
            //: RangePushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            RangePushManager.share.picParams(clickEvent: appTimeMarginId, sufficient: false)
            //: uploadFailTime(type: 1)
            strikeOut(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        voiceEnter(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension ColorCallerDelegate {
    //: func uploadFailTime(type: Int) {
    func strikeOut(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(notiCollectionTitle) + String(user_viewMomentTargetData))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(dataMakeBeautyStr.prefix(6)) + "itin" + String(const_requestStr.suffix(5)))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        const_screenFormat.infoEqual(eventID: constViewFormat, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func timeUploadToTriumph(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(dataMakeBeautyStr.prefix(6)) + "itin" + String(const_requestStr.suffix(5)))] = time
        //: parm["toUid"] = toUid
        parm[(String(main_succeedName))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        const_screenFormat.infoEqual(eventID: show_timeUrl, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension ColorCallerDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func shadowRefer() {
        //: guard PunctuateThen.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard PunctuateThen.share.appStatus == HiddenHashableRepresentation.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !PunctuateThen.share.loginUserMode.loungePlus
        vipIcon.isHidden = !PunctuateThen.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = ColorHashableRepresentation.randow
        //: if PunctuateThen.share.loginUserMode.freeCallTimes > 0 && PunctuateThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if PunctuateThen.share.loginUserMode.freeCallTimes > 0, PunctuateThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if PunctuateThen.share.appUserConfigMode.callBtnStyle == 1 {
            if PunctuateThen.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (dataConnectionUrl.replacingOccurrences(of: "key", with: "F") + String(dataAddFormat)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = PunctuateThen.share.loginUserMode.loungePlus ? vipFee:fee
            let price = PunctuateThen.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(dataMinValue.suffix(6))).viewfinder(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.atomic(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(dataMinValue.suffix(6))).viewfinder(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.atomic(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = PunctuateThen.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = PunctuateThen.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = PunctuateThen.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = PunctuateThen.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !PunctuateThen.share.loginUserMode.loungePlus
            priceStackView.isHidden = !PunctuateThen.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func plainspoken() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func coverMessage() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(constLiveErrorMessage + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func appQuantityeraction() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(shadowRefer),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: constTabValue,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(stateEqual),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: constCoverPathValue,
                                               //: object: nil)
                                               object: nil)
    }
}
