
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataMakeMessage:[UInt8] = [0xa2,0xa5,0xa2,0xbf,0xe3,0xa8,0xa4,0xaf,0xae,0xb9,0xf1,0xe2,0xeb,0xa3,0xaa,0xb8,0xeb,0xa5,0xa4,0xbf,0xeb,0xa9,0xae,0xae,0xa5,0xeb,0xa2,0xa6,0xbb,0xa7,0xae,0xa6,0xae,0xa5,0xbf,0xae,0xaf]

private func medicinalDrug(revenue num: UInt8) -> UInt8 {
    return num ^ 203
}

/*: "Text greeting" :*/
fileprivate let user_nameData:[Character] = ["T","e","x","t"," ","g","r"]
fileprivate let data_makeText:[Character] = ["e","e","t","i","n","g"]

/*: "Add" :*/
fileprivate let show_iconText:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let constDirectionMessage:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let userActionKey:String = "addStatusif string class"

/*: "list" :*/
fileprivate let constMakeFormat:String = "ltextt"

/*: "Are you sure to delete this message？" :*/
fileprivate let k_conversationMessage:[UInt8] = [0x9c,0xaf,0xb8,0xfd,0xa4,0xb2,0xa8,0xfd,0xae,0xa8,0xaf,0xb8,0xfd,0xa9,0xb2,0xfd,0xb9,0xb8,0xb1,0xb8,0xa9,0xb8,0xfd,0xa9,0xb5,0xb4,0xae,0xfd,0xb0,0xb8,0xae,0xae,0xbc,0xba,0xb8,0x32,0x61,0x42]

private func midColor(lab num: UInt8) -> UInt8 {
    return num ^ 221
}

/*: "Cancel" :*/
fileprivate let kTableFormat:[Character] = ["C","a","n","c","e"]
fileprivate let kTitleThoughtId:[Character] = ["l"]

/*: "Delete" :*/
fileprivate let showJumpData:String = "Deleteto float"

/*: "id" :*/
fileprivate let kHeadName:[Character] = ["i","d"]

/*: "The upper limit has been reached" :*/
fileprivate let show_managerStr:[UInt8] = [0x64,0x65,0x68,0x63,0x61,0x65,0x72,0x20,0x6e,0x65,0x65,0x62,0x20,0x73,0x61,0x68,0x20,0x74,0x69,0x6d,0x69,0x6c,0x20,0x72,0x65,0x70,0x70,0x75,0x20,0x65,0x68,0x54]

/*: "text" :*/
fileprivate let data_draftNameKey:[Character] = ["t","e","x","t"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let k_normalMsg:[UInt8] = [0xab,0x8d,0x9b,0x9b,0x9d,0x8b,0x8b,0x9e,0x8d,0x94,0x94,0x81,0xd8,0x99,0x9c,0x9c,0x9d,0x9c,0xd4,0xd8,0x88,0x94,0x9d,0x99,0x8b,0x9d,0xd8,0x8f,0x99,0x91,0x8c,0xd8,0x9e,0x97,0x8a,0xd8,0x99,0x88,0x88,0x8a,0x97,0x8e,0x99,0x94]

private func groupSession(up num: UInt8) -> UInt8 {
    return num ^ 248
}

/*: "Any-Hex/Java" :*/
fileprivate let user_stateMsg:String = "return handleAny-Hex/J"
fileprivate let kModeLimitStr:String = "countvcount"

/*: "#FF506D" :*/
fileprivate let show_intervalTopInValue:String = "self down method manager image#FF506"
fileprivate let notiPicKey:[Character] = ["D"]

/*: "btn_message_delete_nor" :*/
fileprivate let constPanelData:String = "btn_mlist content hide string"
fileprivate let kMessageName:String = "_deleimage count player"

/*: "Click the button below to add\ncontent" :*/
fileprivate let show_listKey:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63,0xa,0x64,0x64,0x61,0x20,0x6f,0x74,0x20,0x77,0x6f,0x6c,0x65,0x62,0x20,0x6e,0x6f,0x74,0x74,0x75,0x62,0x20,0x65,0x68,0x74,0x20,0x6b,0x63,0x69,0x6c,0x43]

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let main_fromMessage:[UInt8] = [0x65,0x74,0x65,0x6c,0x65,0x64,0x20,0x6f,0x74,0x20,0x65,0x67,0x61,0x73,0x73,0x65,0x6d,0x20,0x65,0x68,0x74,0x20,0x6f,0x74,0x20,0x65,0x64,0x69,0x6c,0x73,0x3b,0x73,0x6e,0x72,0x75,0x74,0x20,0x79,0x62,0x20,0x6d,0x65,0x68,0x74,0x20,0x64,0x6e,0x65,0x73,0x20,0x6c,0x6c,0x69,0x77,0x20,0x6d,0x65,0x20,0x74,0x73,0x79,0x73,0x20,0x65,0x68,0x74,0x20,0x2c,0x65,0x67,0x61,0x73,0x73,0x65,0x6d,0x20,0x74,0x78,0x65,0x74,0x20,0x65,0x6c,0x70,0x69,0x74,0x6c,0x75,0x6d,0x20,0x67,0x6e,0x69,0x64,0x64,0x61,0x20,0x6e,0x65,0x68,0x57]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FieldReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class FieldReactiveCompatible: SizeViewController {
    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: ElementView?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataMakeMessage.map{medicinalDrug(revenue: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(user_nameData) + String(data_makeText)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.sizeColor()
        //: designView()
        indexView()
        //: getAudioListData()
        card()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [MessageGreetModel] = {
        //: var array = [TalkingGreetModel]()
        var array = [MessageGreetModel]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: kQueryValue, height: notiKeyTitle - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(show_iconText)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.clickLarge(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.sinceNext(colors: UIColor.analogDigitalConverterColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(doTime), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension FieldReactiveCompatible {
    //: func getAudioListData() {
    func card() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(String(constDirectionMessage))] = 2
        //: ProgressHUD.show()
        DirectThen.levelHandle()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        TampReactiveCompatible.progress(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            DirectThen.cutWith()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.societalTitle()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(userActionKey.prefix(9)))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(constMakeFormat.replacingOccurrences(of: "text", with: "is"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [MessageGreetModel] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<MessageGreetModel>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func dataConverterStar(index: Int) {
        //: let config = ShowAlertConfig()
        let config = CurtilageAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.clickLarge(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        SizeReactiveCompatible.buttonEnter(message: String(bytes: k_conversationMessage.map{midColor(lab: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(kTableFormat) + String(kTitleThoughtId)).localized, rightBtnTitle: (String(showJumpData.prefix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            SizeReactiveCompatible.obstruct()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            SizeReactiveCompatible.obstruct()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: MessageGreetModel = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            DirectThen.levelHandle()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            TampReactiveCompatible.search(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                DirectThen.cutWith()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.card()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension FieldReactiveCompatible {
    //: @objc func addButtonClick() {
    @objc func doTime() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.drawObserver(showMsg: String(bytes: show_managerStr.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = ElementView(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.positionUp()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.sumAdd(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(String(constDirectionMessage))] = 2
            //: dict["text"] = tempStr
            dict[(String(data_draftNameKey))] = tempStr
            //: ProgressHUD.show()
            DirectThen.levelHandle()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            TampReactiveCompatible.addGreetParamsColorCurrentHide(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                DirectThen.cutWith()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.occupant(showMsg: String(bytes: k_normalMsg.map{groupSession(up: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.card()
                //: self.recordView?.dismiss()
                self.recordView?.clear()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func sumAdd(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(user_stateMsg.suffix(9)) + kModeLimitStr.replacingOccurrences(of: "count", with: "a")) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension FieldReactiveCompatible: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: kQueryValue - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.clickLarge(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetTextCell.className()
        let identifier = DrapeReactiveCompatible.className()
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: DrapeReactiveCompatible = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! DrapeReactiveCompatible

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.camera(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(showJumpData.prefix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.dataConverterStar(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(show_intervalTopInValue.suffix(6)) + String(notiPicKey)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.dataConverterStar(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.thoughtImageBundleSizeText(name: (String(constPanelData.prefix(5)) + "essage" + String(kMessageName.prefix(5)) + "te_nor")).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = PanWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension FieldReactiveCompatible {
    //: private func setupEmptyDataView() {
    private func societalTitle() {
        //: var style = EmptyStyle()
        var style = BareViewEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: show_listKey.reversed(), encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .sizeColor()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func indexView() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: TalkingGreetTextCell.className())
        MainTable.register(DrapeReactiveCompatible.self, forCellReuseIdentifier: DrapeReactiveCompatible.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: kQueryValue, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.hockeyPlayer()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.recoverOf(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: main_fromMessage.reversed(), encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
