
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_arrayImageOutValue:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Cancel" :*/
fileprivate let appClearEqualData:String = "title model firstCancel"

/*: "Submit" :*/
fileprivate let showNearbyUrl:String = "Submitsplit self title count"

/*: "Please tell us your problems                   " :*/
fileprivate let appObjectId:[UInt8] = [0x8a,0xb6,0xbf,0xbb,0xa9,0xbf,0xfa,0xae,0xbf,0xb6,0xb6,0xfa,0xaf,0xa9,0xfa,0xa3,0xb5,0xaf,0xa8,0xfa,0xaa,0xa8,0xb5,0xb8,0xb6,0xbf,0xb7,0xa9,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa,0xfa]

private func pointIndex(lab num: UInt8) -> UInt8 {
    return num ^ 218
}

/*: "You need to choose at least one." :*/
fileprivate let appViewId:[UInt8] = [0xb,0x21,0x27,0xd2,0x20,0x17,0x17,0x16,0xd2,0x26,0x21,0xd2,0x15,0x1a,0x21,0x21,0x25,0x17,0xd2,0x13,0x26,0xd2,0x1e,0x17,0x13,0x25,0x26,0xd2,0x21,0x20,0x17,0xe0]

fileprivate func scaleAlive(search num: UInt8) -> UInt8 {
    let value = Int(num) + 78
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Please tell us your problems." :*/
fileprivate let app_rowPath:[Character] = ["P","l","e","a","s","e"," ","t","e","l"]
fileprivate let appTextId:String = "add var atl us"
fileprivate let main_indexKey:String = "pstyleobl"

/*: "app/userReport" :*/
fileprivate let userVideoValue:[Character] = ["a","p","p","/"]
fileprivate let notiSearchFormat:[Character] = ["u","s","e","r","R","e","p","o","r","t"]

/*: "id" :*/
fileprivate let dataScreenMsg:[UInt8] = [0x64,0x69]

/*: "reason" :*/
fileprivate let noti_photoData:String = "REASO"
fileprivate let constToFormat:String = "make"

/*: "desc" :*/
fileprivate let constWithPath:[UInt8] = [0xc0,0xc1,0xd7,0xc7]

private func toContent(view num: UInt8) -> UInt8 {
    return num ^ 164
}

/*: "mf/moment/report" :*/
fileprivate let dataInfoFormat:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e","p","o","r","t"]

/*: "momentId" :*/
fileprivate let mainTextPath:[UInt8] = [0x91,0x93,0x91,0x99,0x92,0x88,0xb5,0x98]

private func locationEqual(up num: UInt8) -> UInt8 {
    return num ^ 252
}

/*: "mf/momentReply/report" :*/
fileprivate let user_momentPath:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","r","e","p","o","r"]
fileprivate let app_centerValue:String = "lab"

/*: "app/videoReport" :*/
fileprivate let k_deviceValue:String = "return strict var sharedapp/v"
fileprivate let user_viewValue:String = "in case mode to sessionideoR"

/*: "logId" :*/
fileprivate let kDirectionNameStr:[UInt8] = [0x5f,0x5c,0x54,0x7a,0x57]

private func blockTo(view num: UInt8) -> UInt8 {
    return num ^ 51
}

/*: "Submitted successfully" :*/
fileprivate let app_skipStr:[Character] = ["S","u","b","m","i"]
fileprivate let data_imageTableFormat:String = "dismiss to let interaction observetted "
fileprivate let notiLabMessage:String = "SUCCES"

/*: "Nudity" :*/
fileprivate let userAddValue:String = "value fill detail sinceNudity"

/*: "Violence" :*/
fileprivate let show_requestMakeValue:[Character] = ["V","i","o","l","e","n","c","e"]

/*: "Harassment" :*/
fileprivate let userInfoMyName:String = "Harassmimage count effect start self"
fileprivate let userDayStr:[Character] = ["e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let main_dataPathValue:[Character] = ["S","u","i","c","i","d","e"]
fileprivate let mainAddTitle:[Character] = [" "]
fileprivate let notiActualUrl:[Character] = ["o","r"," ","S","e","l","f","-","i","n","j","u","r","y"]

/*: "False Information" :*/
fileprivate let dataPhotoText:[Character] = ["F","a","l","s","e"," "]
fileprivate let constModelStyleContent:[Character] = ["I","n","f","o","r","m","a","t","i","o","n"]

/*: "Spam" :*/
fileprivate let showNameSendMessage:String = "Spamclear view"

/*: "Hate Speech" :*/
fileprivate let main_colorTitle:String = "in cell selfHate "
fileprivate let main_toInsideUrl:String = "Speechcolor digital succeed active"

/*: "Terrorism" :*/
fileprivate let main_labelNameFormat:String = "Terrmodel if at lab self"
fileprivate let k_angleNextText:String = "orminm"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let kItemFlushName:[UInt8] = [0xf2,0xc0,0xcd,0xc4,0x81,0xce,0xc7,0x81,0xe8,0xcd,0xcd,0xc4,0xc6,0xc0,0xcd,0x81,0xce,0xd3,0x81,0xf3,0xc4,0xc6,0xd4,0xcd,0xc0,0xd5,0xc4,0xc5,0x81,0xe6,0xce,0xce,0xc5,0xd2]

/*: "Sending disgusting messages" :*/
fileprivate let showTextUrl:String = "in touch level now viewSendi"
fileprivate let notiMomentId:String = "imanagergumanagert"
fileprivate let dataRowByName:String = "mtables"

/*: "Using photos of nudity or drugs" :*/
fileprivate let noti_progressId:[UInt8] = [0xd6,0xf0,0xea,0xed,0xe4,0xa3,0xf3,0xeb,0xec,0xf7,0xec,0xf0,0xa3,0xec,0xe5,0xa3,0xed,0xf6,0xe7,0xea,0xf7,0xfa,0xa3,0xec,0xf1,0xa3,0xe7,0xf1,0xf6,0xe4,0xf0]

/*: "Harassing or sending ads" :*/
fileprivate let dataComponentViewMessageValue:[Character] = ["H","a","r","a","s","s","i","n","g"," ","o","r"," ","s","e","n","d","i","n","g"," "]
fileprivate let noti_modelKey:String = "succeedds"

/*: "Defraud or using fake info" :*/
fileprivate let dataColorId:[Character] = ["D","e","f","r","a","u","d"," ","o","r"," ","u","s","i","n","g"," ","f","a","k","e"," "]
fileprivate let appQueryedData:String = "INFO"

/*: "Others" :*/
fileprivate let constCellOfStr:String = "Othersview player select range mode"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TitleView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum LayerAlertType: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class TitleView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: LayerAlertType?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [BlockReportModel] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = BlockReportModel()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: LayerAlertType?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        exceptLive()
        //: setupSubViewsConstraint()
        messageRestrictionClickSub()
        //: setupReasons()
        auspicate()
        //: bindInteraction()
        totaleractionData()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_arrayImageOutValue.reversed(), encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(appClearEqualData.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.readRemove(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.clickLarge(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.readRemove().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(clickCell), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(showNearbyUrl.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.clickLarge(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.sinceNext(colors: UIColor.analogDigitalConverterColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(latestAction), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: kQueryValue - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.loadRgba(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = CountoListThen(placeholder: String(bytes: appObjectId.map{pointIndex(lab: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.hockeyPlayer(), frame: CGRect(x: 10, y: 10, width: kQueryValue - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .clickLarge(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.toEvent()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .clickLarge(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.loadRgba(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(IntervaloThen.self, forCellReuseIdentifier: IntervaloThen.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension TitleView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func names() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func toColorView(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = PublisherReactiveCompatible.getWindow()
            let window = PublisherReactiveCompatible.exceptAction()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func pushSize(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func clickCell() {
        //: self.dismissReportView(succeed: false)
        self.pushSize(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func latestAction() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.drawObserver(showMsg: String(bytes: appViewId.map{scaleAlive(search: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.drawObserver(showMsg: (String(app_rowPath) + String(appTextId.suffix(4)) + " your " + main_indexKey.replacingOccurrences(of: "style", with: "r") + "ems.").localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(userVideoValue) + String(notiSearchFormat))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (noti_photoData.lowercased() + constToFormat.replacingOccurrences(of: "make", with: "n")): selectedModel.type, String(bytes: constWithPath.map{toContent(view: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(dataInfoFormat))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: mainTextPath.map{locationEqual(up: $0)}, encoding: .utf8)!: tempId, (noti_photoData.lowercased() + constToFormat.replacingOccurrences(of: "make", with: "n")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(user_momentPath) + app_centerValue.replacingOccurrences(of: "lab", with: "t"))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (noti_photoData.lowercased() + constToFormat.replacingOccurrences(of: "make", with: "n")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(k_deviceValue.suffix(5)) + String(user_viewValue.suffix(5)) + "eport")
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: kDirectionNameStr.map{blockTo(view: $0)}, encoding: .utf8)!: tempId, (noti_photoData.lowercased() + constToFormat.replacingOccurrences(of: "make", with: "n")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        BackgroundThen.sum(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.occupant(showMsg: (String(app_skipStr) + String(data_imageTableFormat.suffix(5)) + notiLabMessage.lowercased() + "sfully").localized)
                //: self.dismissReportView(succeed: true)
                self.pushSize(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension TitleView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: IntervaloThen.className()) as! IntervaloThen
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.giftPop(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension TitleView {
    /// 添加视图
    //: private func setupSubviews() {
    private func exceptLive() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func messageRestrictionClickSub() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func auspicate() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(userAddValue.suffix(6))).localized,
                           //: "Violence".localized,
                           (String(show_requestMakeValue)).localized,
                           //: "Harassment".localized,
                           (String(userInfoMyName.prefix(7)) + String(userDayStr)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(main_dataPathValue) + String(mainAddTitle) + String(notiActualUrl)).localized,
                           //: "False Information".localized,
                           (String(dataPhotoText) + String(constModelStyleContent)).localized,
                           //: "Spam".localized,
                           (String(showNameSendMessage.prefix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(main_colorTitle.suffix(5)) + String(main_toInsideUrl.prefix(6))).localized,
                           //: "Terrorism".localized,
                           (String(main_labelNameFormat.prefix(4)) + k_angleNextText.replacingOccurrences(of: "min", with: "is")).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: kItemFlushName.map{$0^161}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(showTextUrl.suffix(5)) + "ng d" + notiMomentId.replacingOccurrences(of: "manager", with: "s") + "ing " + dataRowByName.replacingOccurrences(of: "table", with: "es") + "ages").localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: noti_progressId.map{$0^131}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(dataComponentViewMessageValue) + noti_modelKey.replacingOccurrences(of: "succeed", with: "a")).localized,
                           //: "Defraud or using fake info".localized,
                           (String(dataColorId) + appQueryedData.lowercased()).localized,
                           //: "Others".localized]
                           (String(constCellOfStr.prefix(6))).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = PunctuateThen.share.appConfigMode.videoReport["reason"] {
            if let reason = PunctuateThen.share.appConfigMode.videoReport[(noti_photoData.lowercased() + constToFormat.replacingOccurrences(of: "make", with: "n"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = BlockReportModel()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func totaleractionData() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
