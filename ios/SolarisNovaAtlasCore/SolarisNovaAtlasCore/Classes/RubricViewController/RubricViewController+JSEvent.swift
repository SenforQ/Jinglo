
//: Declare String Begin

/*: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION" :*/
fileprivate let mainLengthLargeValue:[UInt8] = [0x90,0x82,0x85,0x91,0x8e,0x82,0x90,0x98,0x93,0x8f,0x8e,0x95,0x83,0x98,0x95,0x82,0x81,0x95,0x82,0x94,0x8f,0x84,0x88,0x8e,0x89,0x98,0x89,0x88,0x93,0x8e,0x81,0x8e,0x84,0x86,0x93,0x8e,0x88,0x89]

private func equalLocal(color num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "goodsId" :*/
fileprivate let mainTextLabelValue:String = "icon"
fileprivate let k_viewUrl:String = "share make gift size letodsId"

/*: "source" :*/
fileprivate let kHideName:String = "SOURC"
fileprivate let appImageMessage:[Character] = ["e"]

/*: "type" :*/
fileprivate let showAddName:[Character] = ["t","y","p","e"]

/*: "title" :*/
fileprivate let user_customMsg:[Character] = ["t","i","t","l","e"]

/*: "url" :*/
fileprivate let noti_textAcceptData:String = "imagerl"

/*: "money" :*/
fileprivate let show_signKey:String = "mlineey"

/*: "Other" :*/
fileprivate let constDataActionValue:[Character] = ["O","t","h","e","r"]

/*: "scene" :*/
fileprivate let app_domainUrl:String = "scfulle"

/*: "show" :*/
fileprivate let userCellStr:String = "shsharedw"

/*: "target" :*/
fileprivate let appMagnitudeervalStr:[Character] = ["t","a","r","g","e","t"]

/*: "eventName" :*/
fileprivate let appFillPath:[Character] = ["e","v","e","n","t","N","a","m","e"]

/*: "jsonString" :*/
fileprivate let data_modelMessage:[Character] = ["j","s","o","n","S","t","r","i","n","g"]

/*: "coin" :*/
fileprivate let notiBlockPath:[Character] = ["c","o","i","n"]

/*: "uid" :*/
fileprivate let k_policyTitle:[Character] = ["u","i","d"]

/*: "未实现的js事件： :*/
fileprivate let main_tagText:String = "comment listen data background make未实现的js事"
fileprivate let user_colorTitleViewPath:[Character] = ["件","："]

/*: "PurchaseClick" :*/
fileprivate let userButtonVideoFalseUrl:String = "let let singlePurc"
fileprivate let dataKitValue:[Character] = ["h","a","s","e","C","l","i","c","k"]

/*: "Retry After or Go to " :*/
fileprivate let main_modeMessage:[Character] = ["R","e","t","r","y"," "]
fileprivate let user_intimateKey:String = "Aftcomment view"
fileprivate let app_styleData:String = "table content userer or "

/*: "Feedback" :*/
fileprivate let k_noMsg:String = "Fedevice view color"
fileprivate let main_nameMessage:[Character] = ["e","d","b","a","c","k"]

/*: " to contact us" :*/
fileprivate let data_equalId:[Character] = [" ","t","o"," ","c","o","n","t","a","c"]
fileprivate let appFatalUrl:[Character] = ["t"," ","u","s"]

/*: "Apple" :*/
fileprivate let userToId:[Character] = ["A","p","p","l","e"]

/*: " apple支付充值失败： :*/
fileprivate let app_inputData:[Character] = [""," ","a","p","p","l","e","支","付"]
fileprivate let kShoveFormat:String = "\u{5145}值失\u{8d25}："

/*: "payResultCallback();" :*/
fileprivate let user_sectionName:[Character] = ["p","a","y","R","e","s","u","l","t","C","a","l","l","b","a","c","k","(",")"]
fileprivate let constActionId:String = "shared"

/*: "USD" :*/
fileprivate let data_giftContent:String = "UScrop"

/*: "amount" :*/
fileprivate let data_countStr:[UInt8] = [0xe6,0xea,0xe8,0xf2,0xe9,0xf3]

private func mainTo(remove num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "getSystemNotificationtStatus(true)" :*/
fileprivate let constViewName:[UInt8] = [0x88,0x86,0x95,0x74,0x9a,0x94,0x95,0x86,0x8e,0x6f,0x90,0x95,0x8a,0x87,0x8a,0x84,0x82,0x95,0x8a,0x90,0x8f,0x95,0x74,0x95,0x82,0x95,0x96,0x94,0x49,0x95,0x93,0x96,0x86,0x4a]

fileprivate func layerLeading(lab num: UInt8) -> UInt8 {
    let value = Int(num) + 223
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "getSystemNotificationtStatus(false)" :*/
fileprivate let noti_cameraPath:[UInt8] = [0x29,0x65,0x73,0x6c,0x61,0x66,0x28,0x73,0x75,0x74,0x61,0x74,0x53,0x74,0x6e,0x6f,0x69,0x74,0x61,0x63,0x69,0x66,0x69,0x74,0x6f,0x4e,0x6d,0x65,0x74,0x73,0x79,0x53,0x74,0x65,0x67]

/*: "mfBean" :*/
fileprivate let showDayText:[UInt8] = [0x40,0x4b,0x6f,0x48,0x4c,0x43]

private func equalName(key num: UInt8) -> UInt8 {
    return num ^ 45
}

/*: "%.2f" :*/
fileprivate let noti_strokeValue:String = "image make file view height%.2f"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RubricViewController+JSEvent.swift
//  AbroadTalking
//
//  Created by young on 2022/9/6.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

// 通知三方H5刷新金币
//: public let WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION = NSNotification.Name(rawValue: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION")
public let notiConnectMsg = NSNotification.Name(rawValue: String(bytes: mainLengthLargeValue.map{equalLocal(color: $0)}, encoding: .utf8)!)

//: extension TalkingWebViewController {
extension RubricViewController {
    /// 处理JS事件
    /// - Parameters:
    ///   - message: 消息
    //: func HandleScriptMessage(_ message: WKScriptMessage) {
    func dedication(_ message: WKScriptMessage) {
        //: let type = WebMethodType(rawValue: message.name)
        let type = GestureOutputStreamable(rawValue: message.name)
        //: let json = JSON(message.body)
        let json = JSON(message.body)
        //: switch type {
        switch type {
        //: case .MethodType_Exit:
        case .MethodType_Exit:
            //: self.func__closeAction(sender: nil)
            self.nameSender(sender: nil)

        //: case .MethodType_needLogin:
        case .MethodType_needLogin:
            //: needLogin()
            pushMain()

        //: case .MethodType_apPay:
        case .MethodType_apPay:
            //: applePay(productId: json["goodsId"].stringValue, source: json["source"].intValue, payType: .Pay)
            equalEffect(productId: json[(mainTextLabelValue.replacingOccurrences(of: "icon", with: "go") + String(k_viewUrl.suffix(5)))].stringValue, source: json[(kHideName.lowercased() + String(appImageMessage))].intValue, payType: .Pay)

        //: case .MethodType_apPay_subscribe:
        case .MethodType_apPay_subscribe:
            //: if json["type"].stringValue == "6" {
            if json[(String(showAddName))].stringValue == "6" {
                //: isVideoCallRechargeHalfPage = true
                isVideoCallRechargeHalfPage = true
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                showScreenMessage = noti_timeUrl
                //: } else if json["type"].stringValue == "7" { // 点击订阅弹窗的订阅按钮
            } else if json[(String(showAddName))].stringValue == "7" { // 点击订阅弹窗的订阅按钮
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                showScreenMessage = noti_timeUrl
            }
            //: applePay_subscribe(type: json["type"].stringValue, productId: json["goodsId"].stringValue, payType: .Subscribe)
            product(type: json[(String(showAddName))].stringValue, productId: json[(mainTextLabelValue.replacingOccurrences(of: "icon", with: "go") + String(k_viewUrl.suffix(5)))].stringValue, payType: .Subscribe)

        //: case .MethodType_changeMenuButton:
        case .MethodType_changeMenuButton:
            //: changeRightMenuButton(title: json["title"].stringValue, url: json["url"].stringValue)
            cap(title: json[(String(user_customMsg))].stringValue, url: json[(noti_textAcceptData.replacingOccurrences(of: "image", with: "u"))].stringValue)

        //: case .MethodType_refreshCoin:
        case .MethodType_refreshCoin:
            //: refreshCoin(price: Double(json["money"].stringValue) ?? 0, payMode: "Other", scene: json["scene"].stringValue)
            inexpensiveness(price: Double(json[(show_signKey.replacingOccurrences(of: "line", with: "on"))].stringValue) ?? 0, payMode: (String(constDataActionValue)), scene: json[(app_domainUrl.replacingOccurrences(of: "full", with: "en"))].stringValue)

        //: case .MethodType_appearsOnGiftBroadcast:
        case .MethodType_appearsOnGiftBroadcast:
            //: appearsOnGiftBroadcast(show: json["show"].boolValue)
            behindTarget(show: json[(userCellStr.replacingOccurrences(of: "shared", with: "o"))].boolValue)

        //: case.MethodType_ToUrl:
        case .MethodType_ToUrl:
            //: openTourl(url: json.rawString() ?? "")
            alongOf(url: json.rawString() ?? "")

        //: case .MethodType_customerService:
        case .MethodType_customerService:
            //: RangePushManager.share.func__pushToPriveteChatVC(chatID: PublisherReactiveCompatible.getCustomerServiceID())
            RangePushManager.share.liveInsideCompletion(chatID: PublisherReactiveCompatible.appId())

        //: case.setRightMenuButton:
        case .setRightMenuButton:
            //: break
            break

        //: case .MethodType_taskCenter:
        case .MethodType_taskCenter:
            //: let target = json["target"].intValue
            let target = json[(String(appMagnitudeervalStr))].intValue
            //: self.reloadInputViews()
            self.reloadInputViews()
            //: switch target {
            switch target {
            //: case 1:
            case 1: /// 签到领金币页
                //: RangePushManager.share.pushOn()
                RangePushManager.share.pushOn()

            //: case 2:
            case 2: /// 个人信息编辑页面
                //: let vc = TalkingEditProfilesVC.init()
                let vc = ToActionThen()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)

            //: case 3:
            case 3: /// 真人认证页面
                //: verifyBtnClick()
                makeFor()
            //: case 4:
            case 4: /// 发布动态页面
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                keyEqual(type: ListSignedNumber.Moment.rawValue)
                /// 防止动态首页广播还没创建
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    //: NotificationCenter.default.post(name: FREEBTN_UPLOAD_POST_NOTIFICATION,
                    NotificationCenter.default.post(name: showTitleMsg,
                                                    //: object: nil,
                                                    object: nil,
                                                    //: userInfo: nil)
                                                    userInfo: nil)
                }
            //: case 5:
            case 5: /// 我的页面
                //: tabberSelete(type: TabBarItemType.Account.rawValue)
                keyEqual(type: ListSignedNumber.Account.rawValue)
            //: case 6:
            case 6: /// 首页
                //: tabberSelete(type: TabBarItemType.Social.rawValue)
                keyEqual(type: ListSignedNumber.Social.rawValue)
            //: case 7:
            case 7: /// 消息列表页
                //: tabberSelete(type: TabBarItemType.Message.rawValue)
                keyEqual(type: ListSignedNumber.Message.rawValue)
            //: case 8:
            case 8: /// Greet setting页
                //: let vc = TalkingAutoGreetSettingVC()
                let vc = AspectReactiveCompatible()
                //: navigationController?.pushViewController(vc, animated: true)
                navigationController?.pushViewController(vc, animated: true)
            //: case 9:
            case 9: /// 动态列表页
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                keyEqual(type: ListSignedNumber.Moment.rawValue)
            //: default:
            default:
                //: break
                break
            }
        //: case .MethodType_systemNotification:
        case .MethodType_systemNotification:
            //: let type = json["type"].intValue
            let type = json[(String(showAddName))].intValue
            //: changeNotifaStatus(type: type)
            exceptLike(type: type)

        //: case .MethodType_refreshPoint:
        case .MethodType_refreshPoint:
            //: refreshPoint()
            destroyTag()

        //: case .MethodType_onEvent:
        case .MethodType_onEvent:
            //: reportEvent(eventName: json["eventName"].stringValue, jsonStr: json["jsonString"].stringValue)
            searchion(eventName: json[(String(appFillPath))].stringValue, jsonStr: json[(String(data_modelMessage))].stringValue)

        //: case .MethodType_refreshCoinAll:
        case .MethodType_refreshCoinAll:
            //: req_refreshCoinAll(coin: json["coin"].stringValue)
            blockIn(coin: json[(String(notiBlockPath))].stringValue)

        //: case .MethodType_refreshPackage:
        case .MethodType_refreshPackage:
            //: req_refreshPackage()
            numberimateFrom()

        //: case .MethodType_vibrate:
        case .MethodType_vibrate:
            //: phoneVibrate()
            enableNeed()

        //: case .MethodType_goFreeVideo:
        case .MethodType_goFreeVideo:
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
            ManagingDirectorThen.visible { _, _, _ in
                //: if PunctuateThen.share.loginUserMode.jumpType == 1 {
                if PunctuateThen.share.loginUserMode.jumpType == 1 {
                    //: self.func__closeAction(sender: nil)
                    self.nameSender(sender: nil)
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                        //: RangePushManager.share.func__pushToRandomVideoVC(isBeginRand: false)
                        RangePushManager.share.metadataRand(isBeginRand: false)
                    }
                }
            }

        //: case .MethodType_goMessageDetail:
        case .MethodType_goMessageDetail:
            //: RangePushManager.share.func__pushToPriveteChatVC(chatID: json["uid"].stringValue)
            RangePushManager.share.liveInsideCompletion(chatID: json[(String(k_policyTitle))].stringValue)

        //: case .MethodType_goPersonInfo:
        case .MethodType_goPersonInfo:
            //: RangePushManager.share.func__pushToUserDetailVC(uid: json["uid"].stringValue)
            RangePushManager.share.clickOfUid(uid: json[(String(k_policyTitle))].stringValue)

        //: case .MethodType_showRechargeDialog:
        case .MethodType_showRechargeDialog:
            //: RangePushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            RangePushManager.share.isoclinicEvent(webViewType: .RechargeHalfPage)

        //: case .OpenRetainPopup:
        case .OpenRetainPopup:
            //: self.func__closeAction(sender: nil)
            self.nameSender(sender: nil)
        //: case .openBrowserWithUrl:
        case .openBrowserWithUrl:
            //: openToOutUrl(url: json.rawString() ?? "")
            analogDigitalConverterShow(url: json.rawString() ?? "")
        //: case .none:
        case .none:
            //: printLog(message: "未实现的js事件：\(message.name)")
            printLog(message: (String(main_tagText.suffix(7)) + String(user_colorTitleViewPath)) + "\(message.name)")
        }
    }

    // MARK: - Event

    /// 苹果订阅
    /// - Parameters:
    ///   - type: 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    ///   - productId: 商品Id
    ///   - payType: 支付类型
    //: private func applePay_subscribe(type: String, productId: String, payType: ApplePayType) {
    private func product(type _: String, productId: String, payType: FourthType) {
        //: applePay(productId: productId, payType: payType)
        equalEffect(productId: productId, payType: payType)
    }

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: private func applePay(productId: String, source: Int = -1, payType: ApplePayType) {
    private func equalEffect(productId: String, source: Int = -1, payType: FourthType) {
        //: if PunctuateThen.share.loginUid.isEmptyString {
        if PunctuateThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: FirebaseAnalyticsManager.share.logEvent(name: "PurchaseClick")
        PanStartAnalyticsManager.share.windowContent(name: (String(userButtonVideoFalseUrl.suffix(4)) + String(dataKitValue)))

        //: ProgressHUD.show()
        DirectThen.levelHandle()
        //: var index = isVideoCallRechargeHalfPage ? 1: 0
        var index = isVideoCallRechargeHalfPage ? 1 : 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
        VoiceReactiveCompatible.shared.titleEnterHandle(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
            //: ProgressHUD.dismiss()
            DirectThen.cutWith()
            //: DispatchQueue.main.async { [self] in
            DispatchQueue.main.async { [self] in
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us".localized)
                    showAlert()
                //: case .veritySucceed:
                case .veritySucceed:
                    //: appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    makeType(price: reportMoney, payType: payType)
                //: case .renewSucceed:
                case .renewSucceed:
                    //: let scene = (payType == .Subscribe) ? "1":"2"
                    let scene = (payType == .Subscribe) ? "1" : "2"
                    //: refreshCoin(price: reportMoney, payMode: "Apple", scene: scene)
                    inexpensiveness(price: reportMoney, payMode: (String(userToId)), scene: scene)
                //: default:
                default:
                    //: printLog(message: " apple支付充值失败：\(status.rawValue)")
                    printLog(message: (String(app_inputData) + kShoveFormat) + "\(status.rawValue)")
                }
            }
        }
    }
    
    func showAlert() {
        DirectThen.stateToast((String(main_modeMessage) + String(user_intimateKey.prefix(3)) + String(app_styleData.suffix(6)) + "Go to ") + "\"" + (String(k_noMsg.prefix(2)) + String(main_nameMessage)) + "\"" + (String(data_equalId) + String(appFatalUrl)).localized)
    }

    /// 设置导航右侧按钮
    /// - Parameters:
    ///   - title: 标题
    ///   - url: 地址
    //: private func changeRightMenuButton(title: String, url: String) {
    private func cap(title: String, url: String) {
        //: let rightBtn = TalkingButton()
        let rightBtn = StartImageButton()
        //: rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        //: rightBtn.setTitle(title, for: .normal)
        rightBtn.setTitle(title, for: .normal)
        //: rightBtn.setTitleColor(.RGBA(51, 51, 51, 1), for: .normal)
        rightBtn.setTitleColor(.loadRgba(51, 51, 51, 1), for: .normal)
        //: rightBtn.addTarget(self, action: #selector(rightButtonClick), for: .touchUpInside)
        rightBtn.addTarget(self, action: #selector(titleure), for: .touchUpInside)
        //: rightBtn.sizeToFit()
        rightBtn.sizeToFit()
        //: rightBtn.userInfo = ["url": url]
        rightBtn.userInfo = [(noti_textAcceptData.replacingOccurrences(of: "image", with: "u")): url]
        //: self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
    }

    //: @objc func rightButtonClick(button: TalkingButton) {
    @objc func titleure(button: StartImageButton) {
        //: guard let urlString = button.userInfo?["url"] as? String else {
        guard let urlString = button.userInfo?[(noti_textAcceptData.replacingOccurrences(of: "image", with: "u"))] as? String else {
            //: return
            return
        }

        //: self.urlStr = urlString
        self.urlStr = urlString
        //: func__startRequest()
        eigenvalue()
    }

    /// 退出登录
    //: private func needLogin() {
    private func pushMain() {
        //: guard Int(PunctuateThen.share.loginUid) ?? 0 > 0 else {
        guard Int(PunctuateThen.share.loginUid) ?? 0 > 0 else {
            //: self.webView.reload()
            self.webView.reload()
            //: return
            return
        }

        //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION,
        NotificationCenter.default.post(name: k_deviceKey,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
    }

    /// 苹果 支付/订阅 成功验证通过
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payType: 支付类型
    //: func appleIAPVeritySucceed(price: Double, payType: ApplePayType) {
    func makeType(price: Double, payType: FourthType) {
        //: self.webView.evaluateJavaScript("payResultCallback();") { data, error in
        self.webView.evaluateJavaScript((String(user_sectionName) + constActionId.replacingOccurrences(of: "shared", with: ";"))) { _, _ in
        }
        //: let scene = (payType == .Subscribe) ? "1":"2"
        let scene = (payType == .Subscribe) ? "1" : "2"
        //: refreshCoin(price: price, payMode: "Apple", scene: scene)
        inexpensiveness(price: price, payMode: (String(userToId)), scene: scene)
    }

    /// 支付/订阅 成功刷新个人信息【Apple/Other】
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payMode: 类型
    ///   - scene: 1：订阅，2：内购
    //: private func refreshCoin(price: Double, payMode: String, scene: String) {
    private func inexpensiveness(price: Double, payMode: String, scene: String) {
        //: NotificationCenter.default.post(name: UPDATE_USER_GETMYINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: userReasonMessage, object: nil)
        //: NotificationCenter.default.post(name: UPDATE_USER_GETINFOCOLUMN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: main_appAgentTitle, object: nil)
        //: NotificationCenter.default.post(name: REFRESH_INDEX_GETCONFIG_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: app_lengthData, object: nil)
        //: NotificationCenter.default.post(name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: constSkipPath, object: nil)
        // 成功埋点
        //: if scene == "1" {
        if scene == "1" {
            //: TalkingAdjustManager.share.addSubscribeEvent(price: price, currency: "USD")
            ExamineThen.share.priceInfo(price: price, currency: (data_giftContent.replacingOccurrences(of: "crop", with: "D")))
            //: } else {
        } else {
            //: TalkingAdjustManager.share.addPurchasedEvent(price: price, currency: "USD")
            ExamineThen.share.viewCurrency(price: price, currency: (data_giftContent.replacingOccurrences(of: "crop", with: "D")))
        }
        //: FirebaseAnalyticsManager.share.trackPurchase(payType: payMode, price: price, currency: "USD")
        PanStartAnalyticsManager.share.currency(payType: payMode, price: price, currency: (data_giftContent.replacingOccurrences(of: "crop", with: "D")))

        // 半屏充值成功埋点
        //: if RECHARGE_CLICK_EVENT.count > 0 {
        if showScreenMessage.count > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: RECHARGE_CLICK_EVENT, parameterStr: ["amount": price])
            const_screenFormat.infoEqual(eventID: showScreenMessage, parameterStr: [String(bytes: data_countStr.map{mainTo(remove: $0)}, encoding: .utf8)!: price])
        }

        // 支付成功发送通知，调用三方js事件
        //: NotificationCenter.default.post(name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: notiConnectMsg, object: nil)
    }

    /// 广播设置
    /// - Parameter show: 是否上广播
    //: private func appearsOnGiftBroadcast(show: Bool) {
    private func behindTarget(show _: Bool) {}

    //: private func verifyBtnClick() {
    private func makeFor() {
        //: if PunctuateThen.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
        if PunctuateThen.share.loginUserMode.isTPAuth == AuthColumnConvertible.isSuccessed.rawValue {
            //: return
            return
                //: } else if PunctuateThen.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
        } else if PunctuateThen.share.loginUserMode.isTPAuth == AuthColumnConvertible.unknown.rawValue {
            //: RangePushManager.share.func__pushUserVerifyController(toast: nil)
            RangePushManager.share.controller(toast: nil)
            //: } else if PunctuateThen.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
        } else if PunctuateThen.share.loginUserMode.isTPAuth == AuthColumnConvertible.isOnGoing.rawValue {
            //: let vc = TalkingFinalVerificationVC()
            let vc = TopViewController()
            //: RangePushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
            RangePushManager.share.retrieve()?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: RangePushManager.share.func__pushUserVerifyController(toast: nil)
            RangePushManager.share.controller(toast: nil)
        }
    }

    /// 底部tabbar切换
    //: private func tabberSelete(type: Int) {
    private func keyEqual(type: Int) {
        //: guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        //: let vc = self.tabBarController?.viewControllers?[type]
        let vc = self.tabBarController?.viewControllers?[type]
        //: self.tabBarController?.tabBar.isHidden = false
        self.tabBarController?.tabBar.isHidden = false
        //: self.tabBarController?.selectedIndex = type
        self.tabBarController?.selectedIndex = type
        //: self.tabBarController?.selectedViewController = vc
        self.tabBarController?.selectedViewController = vc
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 获取系统通知状态
    //: private func changeNotifaStatus(type: Int) {
    private func exceptLike(type: Int) {
        //: if type == 0 {
        if type == 0 {
            //: if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
            if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
                //: if #available(iOS 10.0, *) {
                if #available(iOS 10.0, *) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    //: } else {
                } else {
                    //: if UIApplication.shared.canOpenURL(url) {
                    if UIApplication.shared.canOpenURL(url) {
                        //: UIApplication.shared.openURL(url)
                        UIApplication.shared.openURL(url)
                    }
                }
            }
            //: } else if type == 1 {
        } else if type == 1 {
            //: AppManagerRequest.func__requestUserConfig { succeed, result, errorModel in }
            ManagingDirectorThen.makeEvent { _, _, _ in }
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in }
            ManagingDirectorThen.visible { _, _, _ in }
            //: TalkingPermissionTool.checkPushNotification { isShow in
            CustomPermissionTool.selfRegard { isShow in
                //: DispatchQueue.main.sync {
                DispatchQueue.main.sync {
                    //: let str = isShow ? "getSystemNotificationtStatus(true)" : "getSystemNotificationtStatus(false)"
                    let str = isShow ? String(bytes: constViewName.map{layerLeading(lab: $0)}, encoding: .utf8)! : String(bytes: noti_cameraPath.reversed(), encoding: .utf8)!
                    //: self.webView.evaluateJavaScript(str) { data, error in
                    self.webView.evaluateJavaScript(str) { _, _ in
                    }
                }
            }
        }
    }

    //: private func refreshPoint() {
    private func destroyTag() {
        //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
        ManagingDirectorThen.visible { _, _, _ in
            //: if TalkingLiveManager.shared().isLive {
            if LogLiveManager.anUp().isLive {
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": 0.0])
                NotificationCenter.default.post(name: user_screenUrl, object: nil, userInfo: [String(bytes: showDayText.map{equalName(key: $0)}, encoding: .utf8)!: 0.0])
            }
        }
    }

    /// 刷新金币
    /// - Parameter coin: 传' ', 刷新接口；传其他，进行相加（含负数））
    //: private func req_refreshCoinAll(coin: String) {
    private func blockIn(coin: String) {
        //: let value: Double = Double(coin) ?? 0
        let value = Double(coin) ?? 0
        //: if value == 0 {
        if value == 0 { // 刷新接口
            //: AppManagerRequest.func__getInfoColumn { succeed, result, errorModel in
            ManagingDirectorThen.exceptViewCompletion { _, _, _ in
            }
            //: } else {
        } else { // 加减
            //: let newCoin = Double(PunctuateThen.share.loginUserMode.mf_coin)! + value
            let newCoin = Double(PunctuateThen.share.loginUserMode.mf_coin)! + value
            //: PunctuateThen.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
            PunctuateThen.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
        }
    }

    /// 刷新礼物背包
    //: private func req_refreshPackage() {
    private func numberimateFrom() {
        //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: dataTurnContent, object: nil)
    }

    /// 事件上报
    /// - Parameters:
    ///   - eventName: 事件名称
    ///   - jsonStr: 事件参数
    //: private func reportEvent(eventName: String, jsonStr: String) {
    private func searchion(eventName: String, jsonStr: String) {
        //: uploadRecord.uploadRecordEvent(eventID: eventName, toUid: "", jsonStr: jsonStr)
        const_screenFormat.occurrenceIn(eventID: eventName, toUid: "", jsonStr: jsonStr)
    }

    /// 手机震动
    //: private func phoneVibrate() {
    private func enableNeed() {
        //: AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
        AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
    }
}
