
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_centerExceptMsg:[UInt8] = [0x1f,0x24,0x1f,0x2a,0xde,0x19,0x25,0x1a,0x1b,0x28,0xf0,0xdf,0xd6,0x1e,0x17,0x29,0xd6,0x24,0x25,0x2a,0xd6,0x18,0x1b,0x1b,0x24,0xd6,0x1f,0x23,0x26,0x22,0x1b,0x23,0x1b,0x24,0x2a,0x1b,0x1a]

fileprivate func statusPath(table num: UInt8) -> UInt8 {
    let value = Int(num) - 182
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Load..." :*/
fileprivate let user_shareEqualFormat:[Character] = ["L","o","a","d",".","."]
fileprivate let dataThoughtPath:[Character] = ["."]

/*: "Skip" :*/
fileprivate let noti_pathRowCommentData:[Character] = ["S","k","i","p"]

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let user_cancelAppMsg:[UInt8] = [0x29,0x27,0x65,0x67,0x72,0x61,0x68,0x63,0x65,0x72,0x27,0x28,0x73,0x4a,0x6f,0x54,0x65,0x76,0x69,0x74,0x61,0x4e,0x2e,0x6c,0x6f,0x6f,0x54,0x70,0x74,0x74,0x48]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let constNameMessage:[UInt8] = [0xee,0xf0,0xf7,0xfd,0xf6,0xee,0xb7,0xf6,0xf7,0xc9,0xf8,0xfe,0xfc,0xca,0xf1,0xf6,0xee,0xbf,0xbf,0xf6,0xf7,0xc9,0xf8,0xfe,0xfc,0xca,0xf1,0xf6,0xee,0xb1,0xb0,0xa2]

private func viewGiftTitle(bar num: UInt8) -> UInt8 {
    return num ^ 153
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let const_lastMessage:String = "jsEvefield add make"
fileprivate let showContentText:[Character] = ["P","a","g","e","S","h","o","w",")",":"," "]

/*: --- :*/
fileprivate let notiBarFormat:[Character] = ["-","-","-"]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let constViewValue:[UInt8] = [0x3b,0x29,0x28,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "jsEvent(onPageHide):  :*/
fileprivate let kIconId:String = "jsEvmore result of image"
fileprivate let show_nameUrl:String = "at text label indexPage"
fileprivate let mainInfoMsg:[Character] = ["H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let noti_bagMessage:String = "TITLE"

/*: "canGoBack" :*/
fileprivate let user_toKey:String = "self in view self ifcanGo"
fileprivate let constRequestStr:String = "Backuser present"

/*: "PHPSESSID= :*/
fileprivate let notiBirthAngleFormat:[Character] = ["P","H"]
fileprivate let userEqualUrl:[Character] = ["P","S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let kLayerMessage:String = "to image;uid="

/*: "Cookie" :*/
fileprivate let constGiftUrl:String = "to equal self indexCookie"

/*: "nav_back_black_nor" :*/
fileprivate let notiBottomToMsg:String = "nav_else false center content"
fileprivate let notiPlayName:[Character] = ["k","_","n","o","r"]

/*: "ico_intimate_list_sm" :*/
fileprivate let noti_tableText:[Character] = ["i","c","o","_","i","n","t","i"]
fileprivate let appLeadingAddFormat:String = "color context centermate_l"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let app_shareTitle:[UInt8] = [0x8f,0x84,0x88,0x9e,0x86,0x8e,0x85,0x9f,0xc5,0x8c,0x8e,0x9f,0xae,0x87,0x8e,0x86,0x8e,0x85,0x9f,0x98,0xa9,0x92,0xbf,0x8a,0x8c,0xa5,0x8a,0x86,0x8e,0xc3,0xcc,0x89,0x84,0x8f,0x92,0xcc,0xc2,0xb0,0xdb,0xb6,0xc5,0x98,0x9f,0x92,0x87,0x8e,0xc5,0x89,0x8a,0x88,0x80,0x8c,0x99,0x84,0x9e,0x85,0x8f,0xd6,0xcc,0x99,0x8c,0x89,0x8a,0xc3,0xdb,0xc7,0xdb,0xc7,0xdb,0xc7,0xdb,0xc2,0xcc]

/*: "Alert" :*/
fileprivate let appQuickTitle:[Character] = ["A","l","e","r","t"]

/*: "OK" :*/
fileprivate let k_equalKey:String = "Ogift"

/*: "Cancle" :*/
fileprivate let const_colorMsg:String = "let color status errorCancle"

/*: "Done" :*/
fileprivate let kAtVideoStr:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let userLabId:String = "js cfalse handle return frame"
fileprivate let constManagerStatusViewTitle:String = "action label talk color extensionhod n"

/*: , message =  :*/
fileprivate let show_userUrl:[Character] = [","," ","m","e","s","s"]
fileprivate let user_indexStr:String = "to self label size totalage = "

/*: "http://" :*/
fileprivate let mainInfoContent:String = "white viewhttp://"

/*: "https://" :*/
fileprivate let showFilterLineStr:String = "htopp"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let userTargetStr:[UInt8] = [0xec,0xfb,0xe8,0xba,0xf9,0xf5,0xf5,0xf1,0xf3,0xff,0xd4,0xfb,0xf7,0xff,0xe9,0xba,0xa7,0xba,0xfe,0xf5,0xf9,0xef,0xf7,0xff,0xf4,0xee,0xb4,0xf9,0xf5,0xf5,0xf1,0xf3,0xff,0xb4,0xe9,0xea,0xf6,0xf3,0xee,0xb2,0xbd,0xa1,0xba,0xbd,0xb3,0xb4,0xf7,0xfb,0xea,0xb2,0xfc,0xef,0xf4,0xf9,0xee,0xf3,0xf5,0xf4,0xb2,0xf9,0xf5,0xf5,0xf1,0xf3,0xff,0xb3,0xba,0xe1,0xba,0xe8,0xff,0xee,0xef,0xe8,0xf4,0xba,0xf9,0xf5,0xf5,0xf1,0xf3,0xff,0xb4,0xe9,0xea,0xf6,0xf3,0xee,0xb2,0xbd,0xa7,0xbd,0xb3,0xc1,0xaa,0xc7,0xba,0xe7,0xba,0xb3,0xa1,0x90]

private func cellTo(hop num: UInt8) -> UInt8 {
    return num ^ 154
}

/*: "uid= :*/
fileprivate let noti_universalData:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let data_rawUrl:String = ";domain=index place layer star manager"

/*: ;path=/" :*/
fileprivate let appReName:String = ";path=/element voice"

/*: "document.cookie=' :*/
fileprivate let main_makeInfoVarValue:String = "return skip let bar fataldocument."
fileprivate let data_playPath:String = "cookie=\'"

/*: ';\n" :*/
fileprivate let noti_itemValue:[Character] = ["\'",";","\n"]

/*: ;domin= :*/
fileprivate let mainAppData:String = ";domin=gift table let extension scale"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let data_starImagePath:[UInt8] = [0x27,0x3d,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x7b,0x20,0x29,0x31,0x2d,0x20,0x3d,0x3d,0x20,0x29,0x27,0x44,0x49,0x53,0x53,0x45,0x53,0x50,0x48,0x50,0x27,0x28,0x66,0x4f,0x78,0x65,0x64,0x6e,0x69,0x2e,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x20,0x66,0x69]

/*: '; };\n" :*/
fileprivate let kViewText:String = "\'; };\n"

/*: '" :*/
fileprivate let kEqualPath:String = "\'"

/*: "h5Domain =  :*/
fileprivate let user_myKey:String = "h5Doremove show show user cancel"
fileprivate let constViewTopModelFormat:[Character] = [" ","="," "]

/*: "getAllCookies =  :*/
fileprivate let user_interactionPath:[Character] = ["g","e","t","A","l","l"]
fileprivate let constNoUpTitle:String = "self followCook"

/*: "PHPSESSID" :*/
fileprivate let dataNumberPath:String = "PcanSESSID"

/*: "uid" :*/
fileprivate let user_showKey:String = "uname"

/*: "appCookies =  :*/
fileprivate let user_cameraMaxMsg:String = "hidden textappC"
fileprivate let showFileMsg:String = "cell=cell"

/*: "UID" :*/
fileprivate let show_viewFormat:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let const_deviceMsg:[Character] = ["n","a"]
fileprivate let app_indexKey:[Character] = ["v","i","g","a","t","o","r",".","u","s","e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let noti_appFormat:[Character] = ["x","u","n","w","u"]
fileprivate let main_hiddenFormat:[Character] = ["_","i"]
fileprivate let mainScaleValue:[Character] = ["p","h","o","n","e"]

/*: "  :*/
fileprivate let show_countFormat:[Character] = [" "]

/*: " xunwu_device_id/ :*/
fileprivate let kReplyName:[Character] = [" ","x","u","n","w","u","_","d","e","v","i","c","e","_","i","d","/"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let showPartyButtonText:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"," ","x","u","n","w","u","_","v","e"]
fileprivate let show_sendValue:[Character] = ["r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let data_textErrorFileTitle:String = " xunwto equal label"
fileprivate let showPrettyStatusPath:String = "u_pview device"
fileprivate let notiSpaceHiddenMsg:String = "content text return currentackage/"

/*:  xunwu_bundleId/ :*/
fileprivate let kCenterValue:[Character] = [" ","x","u","n","w","u"]
fileprivate let dataWaitKey:String = "pop resume mode model var_bund"

/*:   webkit/1" :*/
fileprivate let appPathName:String = "model equal language  webk"
fileprivate let userStatusMsg:String = "attribute to stringit/1"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RubricViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class RubricViewController: CountimateRecognizerDelegate {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: SearchConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: DataPlumeBidirectionalCollection?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: DataPlumeBidirectionalCollection?, webConfig: SearchConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_centerExceptMsg.map{statusPath(table: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        cellModel()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        outsideHide()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(user_shareEqualFormat) + String(dataThoughtPath))
        //: addNotification()
        liveNotification()
        //: func__checkConfig()
        coequal()
        //: func__setUpWebView()
        equalTable()
        //: func__registerObserver()
        kindPlayer()
        //: func__beginStartRequest()
        liveIn()
        //: func_checkAppleIAP()
        giftStreet()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: mainSaltErrContent, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            moveConfig(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.readRemove(), .font: UIFont.clickLarge(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(noti_pathRowCommentData)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension RubricViewController {
    /// 添加通知
    //: private func addNotification() {
    private func liveNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(turn),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: notiConnectMsg,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(cellModel),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func turn() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: user_cancelAppMsg.reversed(), encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func cellModel() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: constNameMessage.map{viewGiftTitle(bar: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(const_lastMessage.prefix(5)) + "nt(on" + String(showContentText)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func outsideHide() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: constViewValue.reversed(), encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(kIconId.prefix(4)) + "ent(on" + String(show_nameUrl.suffix(4)) + String(mainInfoMsg)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension RubricViewController {
    //: func func__registerObserver() {
    func kindPlayer() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (noti_bagMessage.lowercased()))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(user_toKey.suffix(5)) + String(constRequestStr.prefix(4))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(tableLoad), name: k_bottomImageId, object: nil)
    }

    //: func func__setUpWebView() {
    func equalTable() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        halfHide()
        //: func__addBridgeMethod()
        funcMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.sizeColor()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            DirectThen.routeGift(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(constLiveErrorMessage)
            }
        }
    }

    //: func func__beginStartRequest() {
    func liveIn() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.eigenvalue()
            }
            //: } else {
        } else {
            //: func__startRequest()
            eigenvalue()
        }
    }

    //: func func__startRequest() {
    func eigenvalue() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = PunctuateThen.share.loginUid
        let uid = PunctuateThen.share.loginUid
        //: let ssid = PunctuateThen.share.loginSessionId
        let ssid = PunctuateThen.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(notiBirthAngleFormat) + String(userEqualUrl)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(kLayerMessage.suffix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(constGiftUrl.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        host()
        //: func__initRightButton()
        lap()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func giftStreet() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            VoiceReactiveCompatible.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.makeType(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            VoiceReactiveCompatible.shared.spring()
        }
    }

    //: func func__initLeftButton() {
    func trivia() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.thoughtImageBundleSizeText(name: (String(notiBottomToMsg.prefix(4)) + "back_blac" + String(notiPlayName))), style: .plain, target: self, action: #selector(nameSender(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func lap() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(DataPlumeBidirectionalCollection.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.thoughtImageBundleSizeText(name: (String(noti_tableText) + String(appLeadingAddFormat.suffix(6)) + "ist_sm")), style: .plain, target: self, action: #selector(viewAction))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func nameSender(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        hideMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.occurrentWith() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: RubricViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! RubricViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! RubricViewController).cellModel()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func coequal() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func targetSucess(sucess: Bool) {
        //: ProgressHUD.dismiss()
        DirectThen.cutWith()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func host() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: app_shareTitle.map{$0^235}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func alongOf(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: RangePushManager.share.func__pushToWebVC(urlStr: url)
            RangePushManager.share.userBlade(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func analogDigitalConverterShow(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func tableLoad() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: k_bottomImageId, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func viewAction() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = AspectViewDelegate(frame: CGRect(x: 0, y: 0, width: kQueryValue, height: notiKeyTitle))
        //: view.show()
        view.pullUpShort()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension RubricViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            DirectThen.routeGift(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            targetSucess(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        trivia()
        //: func__clearJSBgColor()
        host()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            targetSucess(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            targetSucess(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(appQuickTitle)), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(const_colorMsg.suffix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(kAtVideoStr)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension RubricViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(userLabId.prefix(4)) + "all met" + String(constManagerStatusViewTitle.suffix(5)) + "ame = ") + "\(message.name)" + (String(show_userUrl) + String(user_indexStr.suffix(6))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.dedication(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func funcMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in GestureOutputStreamable.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func hideMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in GestureOutputStreamable.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension RubricViewController {
    //: func func__addUserScript() {
    func makeCalculate() {
        //: var htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(mainInfoContent.suffix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(mainInfoContent.suffix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((showFilterLineStr.replacingOccurrences(of: "top", with: "tt") + "s://")) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (showFilterLineStr.replacingOccurrences(of: "top", with: "tt") + "s://"), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = PunctuateThen.share.loginUserMode.userID
        let tempUid: String = PunctuateThen.share.loginUserMode.userID
        //: let tempSessionID: String = PunctuateThen.share.loginSessionId
        let tempSessionID: String = PunctuateThen.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: userTargetStr.map{cellTo(hop: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(noti_universalData)) + "\(tempUid)" + (String(data_rawUrl.prefix(8))) + "\(htmlDomain)" + (String(appReName.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(main_makeInfoVarValue.suffix(9)) + data_playPath) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(notiBirthAngleFormat) + String(userEqualUrl)) + "\(tempSessionID)" + (String(mainAppData.prefix(7))) + "\(htmlDomain)" + (String(appReName.prefix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(main_makeInfoVarValue.suffix(9)) + data_playPath) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: data_starImagePath.reversed(), encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(noti_universalData)) + "\(tempUid)" + (String(data_rawUrl.prefix(8))) + "\(htmlDomain)" + (String(appReName.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(main_makeInfoVarValue.suffix(9)) + data_playPath) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(noti_universalData)) + "\(tempSessionID)" + (String(data_rawUrl.prefix(8))) + "\(htmlDomain)" + (String(appReName.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(main_makeInfoVarValue.suffix(9)) + data_playPath) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func moveConfig(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
            let htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(user_myKey.prefix(4)) + "main" + String(constViewTopModelFormat)) + "\(htmlDomain)")
            //: let userID: String = PunctuateThen.share.loginUserMode.userID
            let userID: String = PunctuateThen.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(user_interactionPath) + String(constNoUpTitle.suffix(4)) + "ies = ") + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (dataNumberPath.replacingOccurrences(of: "can", with: "HP")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (user_showKey.replacingOccurrences(of: "name", with: "id")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.liveIn()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.specialSessionData(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func specialSessionData(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
                let htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(user_myKey.prefix(4)) + "main" + String(constViewTopModelFormat)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: LayerAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(user_cameraMaxMsg.suffix(4)) + "ookies" + showFileMsg.replacingOccurrences(of: "cell", with: " ")) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(show_viewFormat)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (user_showKey.replacingOccurrences(of: "name", with: "id"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (dataNumberPath.replacingOccurrences(of: "can", with: "HP")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (dataNumberPath.replacingOccurrences(of: "can", with: "HP"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.liveIn()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func halfHide() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(const_deviceMsg) + String(app_indexKey))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(const_deviceMsg) + String(app_indexKey)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(noti_appFormat) + String(main_hiddenFormat) + String(mainScaleValue))) {
                //: let str = getUserAgentString()
                let str = stateAction()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.enterBlock { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(kReplyName)) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func stateAction() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(showPartyButtonText) + String(show_sendValue)) + "\(constKeyFormat)" + (String(data_textErrorFileTitle.prefix(5)) + String(showPrettyStatusPath.prefix(3)) + String(notiSpaceHiddenMsg.suffix(7))) + "\(show_systemValue)" + (String(kCenterValue) + String(dataWaitKey.suffix(5)) + "leId/") + "\(show_screenNoData)" + (String(appPathName.suffix(6)) + String(userStatusMsg.suffix(4)))
    }
}
