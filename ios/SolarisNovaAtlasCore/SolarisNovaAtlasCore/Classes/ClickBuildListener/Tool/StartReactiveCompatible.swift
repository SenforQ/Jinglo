
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let userButtonFormat:[Character] = ["m","f","/","m","o","m"]
fileprivate let const_blockFormat:String = "eninfo"
fileprivate let user_labelId:[Character] = ["/","l","i","s","t"]

/*: "mf/moment/moments" :*/
fileprivate let const_positionKey:String = "mf/mevent return end class"
fileprivate let showColorFormat:String = "live run self value to/mome"
fileprivate let mainAllTitle:String = "nbackground"

/*: "mf/moment/like" :*/
fileprivate let const_arrayEmptySuperValue:[Character] = ["m"]
fileprivate let main_cellStr:String = "f/modata make intimate make"
fileprivate let showSendStr:String = "likinfo"

/*: "mf/crush/send" :*/
fileprivate let data_textPath:[Character] = ["m","f","/","c","r","u","s","h"]
fileprivate let data_labMsg:String = "/sendchange line manager equal hide"

/*: "mf/moment/view" :*/
fileprivate let kMagnitudeeractionData:String = "constraint"
fileprivate let dataLabelContent:[Character] = ["f","/","m","o","m","e","n","t","/","v","i","e","w"]

/*: "mf/momentReply/list" :*/
fileprivate let data_ofMessage:[Character] = ["m","f","/","m","o"]
fileprivate let kContentMessage:String = "imageent"
fileprivate let noti_colorMsg:String = "Replshow target join self tap"

/*: "mf/momentReply/save" :*/
fileprivate let k_evenHouseTitle:String = "title view case mode tomf/moment"
fileprivate let const_managerText:[Character] = ["R","e","p","l","y","/","s","a","v","e"]

/*: "mf/moment/videoSign" :*/
fileprivate let dataButtonKey:[Character] = ["m","f","/","m","o","m","e","n","t","/"]
fileprivate let constPlayerValue:String = "V"
fileprivate let const_conversationId:[Character] = ["i","d","e","o","S","i","g","n"]

/*: "dynamicPic.zip" :*/
fileprivate let const_circleMessage:String = "dylocationam"
fileprivate let app_labModePath:[Character] = ["i","c","P","i"]
fileprivate let user_valueUrl:String = "c.zipleading height view true max"

/*: "pic" :*/
fileprivate let appMakeButtonValue:[UInt8] = [0xbc,0xa5,0xaf]

private func selectToLab(info num: UInt8) -> UInt8 {
    return num ^ 204
}

/*: "content" :*/
fileprivate let appUserMsg:String = "numberontent"

/*: "mf/moment/save" :*/
fileprivate let k_beforeId:String = "mf/momeheight model"
fileprivate let k_videoName:[Character] = ["n","t","/","s","a","v","e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let noti_afterData:[UInt8] = [0xe2,0xc7,0xd0,0xde,0xdb,0xc1,0xda,0x92,0xe1,0xc7,0xd1,0xd1,0xd7,0xc1,0xc1,0xd4,0xc7,0xde,0x9e,0x92,0xf5,0xdd,0x92,0xc6,0xdd,0x92,0xff,0xcb,0x92,0xf6,0xcb,0xdc,0xd3,0xdf,0xdb,0xd1,0xc1,0x92,0xc6,0xdd,0x92,0xc4,0xdb,0xd7,0xc5]

private func equalStatus(min num: UInt8) -> UInt8 {
    return num ^ 178
}

/*: "mf/moment/saveVideo" :*/
fileprivate let appTitleFailureReturnData:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let main_indexName:String = "t/satrue self true cur let"
fileprivate let data_backContent:String = "veVideoequal of self share temp"

/*: .png" :*/
fileprivate let k_firstId:String = ".pngfatal if"

/*: "dynamicPic" :*/
fileprivate let userCardPath:[Character] = ["d","y","n","a","m"]
fileprivate let app_imageUrl:[Character] = ["i","c","P","i","c"]

/*: "freeImage" :*/
fileprivate let const_managerData:String = "hockey share black handlefreeI"

/*: "mf/momentReply/replylist" :*/
fileprivate let noti_viewUrl:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","r","e","p","l","y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let userOnFormat:String = "draw selfmf/mo"
fileprivate let k_scaleMessage:String = "background"

/*: "mf/momentReply/delete" :*/
fileprivate let kPickViewFormat:String = "mf/momake enable"
fileprivate let show_atData:String = "view addeply/d"
fileprivate let show_makeFormat:String = "actionlactiontaction"

/*: "mf/moment/pin" :*/
fileprivate let kColorTitle:String = "table let progress directionmf/m"
fileprivate let userComeValue:String = "make size/pin"

/*: "mf/moment/remindNum" :*/
fileprivate let constLocationKey:[Character] = ["m","f","/","m","o","m","e","n","t","/","r"]
fileprivate let showInsideFormat:[Character] = ["e","m"]
fileprivate let app_dataFormat:[Character] = ["i","n","d","N","u","m"]

/*: "mf/moment/remindList" :*/
fileprivate let k_appearId:[Character] = ["m","f","/","m","o","m","e","n","t","/"]
fileprivate let constSpaceKey:String = "reslight"
fileprivate let dataRunningName:[Character] = ["i","n","d","L","i","s","t"]

/*: "page" :*/
fileprivate let data_frameTouchUrl:[UInt8] = [0x8e,0x9f,0x99,0x9b]

private func selfShow(row num: UInt8) -> UInt8 {
    return num ^ 254
}

/*: "cos/getCredential" :*/
fileprivate let dataLiveMessage:String = "true returncos/get"
fileprivate let k_voiceTitle:String = "Credepush collection color click"
fileprivate let mainShareText:String = "ntilabl"

/*: "type" :*/
fileprivate let kCountervalData:[Character] = ["t","y","p","e"]

/*: "user/reportUserInfo" :*/
fileprivate let notiPathTitle:[Character] = ["u","s","e","r","/","r","e","p","o","r","t","U","s","e","r","I"]
fileprivate let notiBackgroundEmptyMsg:String = "succeedfo"

/*: "uid" :*/
fileprivate let const_requestMsg:String = "playerd"

/*: "deviceId" :*/
fileprivate let k_styleUrl:String = "dleading"
fileprivate let mainIndexTitle:String = "empty viewviceId"

/*: "deviceName" :*/
fileprivate let main_sendText:String = "live"
fileprivate let userCornerPath:[Character] = ["e","v","i","c","e","N","a","m","e"]

/*: "iphone" :*/
fileprivate let kSizeFormat:[Character] = ["i","p","h","o","n","e"]

/*: "fileUrl" :*/
fileprivate let k_dayPath:[Character] = ["f"]
fileprivate let userBottomFormat:String = "ileUrluser app"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StartReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class StartReactiveCompatible: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func drown(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(userButtonFormat) + const_blockFormat.replacingOccurrences(of: "info", with: "t") + String(user_labelId))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func popOut(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(const_positionKey.prefix(4)) + "oment" + String(showColorFormat.suffix(5)) + mainAllTitle.replacingOccurrences(of: "background", with: "ts"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func deadlineCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(const_arrayEmptySuperValue) + String(main_cellStr.prefix(4)) + "ment/" + showSendStr.replacingOccurrences(of: "info", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func pinLog(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(data_textPath) + String(data_labMsg.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func momentServiceProtrude(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (kMagnitudeeractionData.replacingOccurrences(of: "constraint", with: "m") + String(dataLabelContent))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func saveSize(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(data_ofMessage) + kContentMessage.replacingOccurrences(of: "image", with: "m") + String(noti_colorMsg.prefix(4)) + "y/list")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func targetOn(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(k_evenHouseTitle.suffix(9)) + String(const_managerText))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func params(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(dataButtonKey) + constPlayerValue.lowercased() + String(const_conversationId))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func fireAll(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        outsideUser()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            okImage(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((const_circleMessage.replacingOccurrences(of: "location", with: "n") + String(app_labModePath) + String(user_valueUrl.prefix(5))))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: appMakeButtonValue.map{selectToLab(info: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(appUserMsg.replacingOccurrences(of: "number", with: "c"))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = SubRequestModel()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(k_beforeId.prefix(7)) + String(k_videoName))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.occupant(showMsg: String(bytes: noti_afterData.map{equalStatus(min: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.outsideUser()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func writeUp(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(appTitleFailureReturnData) + String(main_indexName.prefix(4)) + String(data_backContent.prefix(7)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func okImage(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.collectionIntervalimate()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(k_firstId.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((String(userCardPath) + String(app_imageUrl)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func outsideUser() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(const_managerData.suffix(5)) + "mage"))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func bindSource(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(noti_viewUrl))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func addBe(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(userOnFormat.suffix(5)) + "ment/dele" + k_scaleMessage.replacingOccurrences(of: "background", with: "te"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func iconBind(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(kPickViewFormat.prefix(5)) + "mentR" + String(show_atData.suffix(6)) + show_makeFormat.replacingOccurrences(of: "action", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func viewAwake(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(kColorTitle.suffix(4)) + "oment" + String(userComeValue.suffix(4)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func fictitiousName(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(constLocationKey) + String(showInsideFormat) + String(app_dataFormat))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func sectionCompletion(page: Int, completion: @escaping (_ data: [ToHandyJSON]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(k_appearId) + constSpaceKey.replacingOccurrences(of: "slight", with: "m") + String(dataRunningName))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: data_frameTouchUrl.map{selfShow(row: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [ToHandyJSON] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<ToHandyJSON>.deserialize(from: arr as? Array) as? [ToHandyJSON] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func facePicTheme(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(dataLiveMessage.suffix(7)) + String(k_voiceTitle.prefix(5)) + mainShareText.replacingOccurrences(of: "lab", with: "a"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(String(kCountervalData)): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func logAt(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestServer = PunctuateThen.share.appConfigMode.reportDomain
        reqModel.requestServer = PunctuateThen.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(notiPathTitle) + notiBackgroundEmptyMsg.replacingOccurrences(of: "succeed", with: "n"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = PunctuateThen.share.loginUserMode.userID
        params[(const_requestMsg.replacingOccurrences(of: "player", with: "ui"))] = PunctuateThen.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(k_styleUrl.replacingOccurrences(of: "leading", with: "e") + String(mainIndexTitle.suffix(6)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(main_sendText.replacingOccurrences(of: "live", with: "d") + String(userCornerPath))] = (String(kSizeFormat))
        //: params["type"] = 1
        params[(String(kCountervalData))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(k_dayPath) + String(userBottomFormat.prefix(6)))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
