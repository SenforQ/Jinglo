
//: Declare String Begin

/*: "http://" :*/
fileprivate let constStatementPath:String = "http://time label self view"

/*: "https://" :*/
fileprivate let kUserPath:String = "hfilter"
fileprivate let mainPanKey:[Character] = ["t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let k_appKey:String = "http:/var inside map"
fileprivate let kMakeMessage:[Character] = ["/"]

/*: "serverSelctIndex =  :*/
fileprivate let app_blueUrl:[Character] = ["s","e","r","v","e","r","S","e","l","c"]
fileprivate let user_whiteId:String = "for mode center under errortIndex = "

/*: "serverUrlStr =  :*/
fileprivate let kColorName:[Character] = ["s","e","r","v","e","r","U","r","l","S","t","r"," "]
fileprivate let constSourceKey:String = "= "

/*: "app. :*/
fileprivate let appSizeTitle:String = "touch add observer awake withapp."

/*: .com" :*/
fileprivate let constCoverMessageStr:String = ".comtemp circle comment change title"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let mainTitleUrl:String = "mf/cat name"
fileprivate let const_priceId:String = "color self leveloom/s"
fileprivate let k_genderMContentUrl:[Character] = ["e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let main_nameUrl:[Character] = ["r","e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y","N","o"]
fileprivate let app_endFullQuantityId:String = "tiview"

/*: "user/appleLogin" :*/
fileprivate let main_mutualFormat:String = "privacy equal info raw currentuser/a"
fileprivate let userScaleUrl:String = "gmid"

/*: "IM/getMyBlackList" :*/
fileprivate let constCountMsg:[Character] = ["I","M","/","g","e","t","M","y","B","l"]
fileprivate let constStatusFormat:String = "let withackList"

/*: "mf/business/sendMsg" :*/
fileprivate let app_frameId:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e","n","d","M"]
fileprivate let dataConversationFormat:String = "appear"

/*: "mf/business/sendGift" :*/
fileprivate let constMessageTitle:String = "mf/buas image save up"
fileprivate let data_labStr:String = "ss/sefor event"
fileprivate let app_colorTitle:String = "reason class user request addndGift"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let appShowEventTitle:String = "mf/cstyle load layer any"
fileprivate let dataHomeLeadingName:String = "time block use showoom/s"
fileprivate let k_textStreamStr:String = "var m equalendMsg"

/*: "en" :*/
fileprivate let show_managerMsg:String = "eto"

/*: "ar" :*/
fileprivate let main_sStr:[Character] = ["a","r"]

/*: "es" :*/
fileprivate let k_indexAddUrl:String = "tills"

/*: "pt" :*/
fileprivate let const_succeedUrl:[Character] = ["p","t"]

/*: "GJ21FB" :*/
fileprivate let constAppPath:[Character] = ["G","J","2","1","F","B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let showVoicePath:String = "完list检测,"

/*: "接口请求失败" :*/
fileprivate let showModelFromValue:[Character] = ["接","口","请","\u{6c42}","失","\u{8d25}"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let const_numberContent:String = "完成了检测"
fileprivate let k_imageData:[Character] = ["r","v"]
fileprivate let user_aboveName:[Character] = ["e","r"," ","u","r","l"]

/*: "HTTPProxy" :*/
fileprivate let app_itemPathValue:String = "cell"
fileprivate let appManagerContent:String = "TTPProxynorthwest if section"

/*: "__SCOPED__" :*/
fileprivate let kIndexData:[Character] = ["_","_","S","C"]
fileprivate let constErrorMsg:[Character] = ["O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let constExecuteName:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let user_playValue:String = "tsession"

/*: "ipsec" :*/
fileprivate let noti_mainContent:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let constQueryedText:String = "clickp"

/*: "func__updateTalkingCookies =  :*/
fileprivate let showToUrl:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C","o"]
fileprivate let const_bottomContent:String = "okies = bar info app share"

/*: "https:// :*/
fileprivate let notiNameId:[Character] = ["h"]
fileprivate let user_iconTitle:String = "ttps://data guard in name"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let user_failureId:[UInt8] = [0x5d,0x4e,0x55,0x58,0x64,0x64,0x5f,0x5e,0x57,0x5e,0x4f,0x5e,0x68,0x5a,0x56,0x5e,0x78,0x54,0x54,0x50,0x52,0x5e,0x48,0x6c,0x52,0x4f,0x53,0x6e,0x49,0x57,0x1b,0x6,0x1b]

private func imageBlock(data num: UInt8) -> UInt8 {
    return num ^ 59
}

/*: "UID" :*/
fileprivate let dataAtKey:String = "UIview"

/*: "PHPSESSID" :*/
fileprivate let showThoughtPath:String = "sizeHsizeSE"
fileprivate let data_layerAllPath:[Character] = ["S","S","I","D"]

/*: "func__saveAllCookies =  :*/
fileprivate let dataLabName:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l"]
fileprivate let k_formatBottomData:[Character] = ["l","C","o"]
fileprivate let show_changeMessage:[Character] = ["o","k","i","e","s"," ","="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let dataDeleteUserValue:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o","o","k"]
fileprivate let dataFrameMessage:[Character] = ["i","e","s"," "]
fileprivate let k_atMediumFormat:String = "manager make account if no error"

/*: "🍪 删除 :*/
fileprivate let show_imageUrl:[Character] = ["🍪"," ","删","除"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LayerAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum TrademarkVoiceMixedBagTextConvertible: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class LayerAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = LayerAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        screenUp()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(constStatementPath.prefix(7)))) || newValue.hasPrefix((kUserPath.replacingOccurrences(of: "filter", with: "t") + String(mainPanKey))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if user_pointButtonNameUrl && !mainErrorObjectPath {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(k_appKey.prefix(6)) + String(kMakeMessage)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(k_appKey.prefix(6)) + String(kMakeMessage)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    showConvert(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func screenUp() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = constDevicePath.integer(forKey: main_halfCellFormat)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(app_blueUrl) + String(user_whiteId.suffix(9))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = display()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(constStatementPath.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if user_pointButtonNameUrl && !mainErrorObjectPath {
            //: serverPrefix = "http://"
            serverPrefix = (String(constStatementPath.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(kColorName) + constSourceKey.capitalized) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func display() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func dismissServerCollection(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(appSizeTitle.suffix(4))) + "\(k_detailData)" + (String(constCoverMessageStr.prefix(4))), (String(appSizeTitle.suffix(4))) + "\(k_detailData)" + (String(constCoverMessageStr.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(mainTitleUrl.prefix(4)) + "hatR" + String(const_priceId.suffix(5)) + String(k_genderMContentUrl)),
         //: "recharge/applePayNotify",
         (String(main_nameUrl) + app_endFullQuantityId.replacingOccurrences(of: "view", with: "fy")),
         //: "user/appleLogin"] + slowPathArr1
         (String(main_mutualFormat.suffix(6)) + "ppleLo" + userScaleUrl.replacingOccurrences(of: "mid", with: "in"))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(constCountMsg) + String(constStatusFormat.suffix(7))),
         //: "mf/business/sendMsg",
         (String(app_frameId) + dataConversationFormat.replacingOccurrences(of: "appear", with: "sg")),
         //: "mf/business/sendGift",
         (String(constMessageTitle.prefix(5)) + "sine" + String(data_labStr.prefix(5)) + String(app_colorTitle.suffix(6))),
         //: "mf/chatRoom/sendMsg"]
         (String(appShowEventTitle.prefix(4)) + "hatR" + String(dataHomeLeadingName.suffix(5)) + String(k_textStreamStr.suffix(6)))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = PublisherReactiveCompatible.getSystemLangCode()
        let lang = PublisherReactiveCompatible.sizeEqual()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .quantityimate(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(constAppPath)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension LayerAddrTool {
    //: func checkServerUrl() {
    func eigenvalueOfAMatrixUrl() {
        //: if PunctuateThen.share.networkStatus != .Unavailable {
        if PunctuateThen.share.networkStatus != .Unavailable {
            //: func_requestHost()
            bottomAdd()
            //: } else {
        } else {
            //: func_starCodeTime()
            funcFromTime()
        }
    }

    //: func func_starCodeTime() {
    func funcFromTime() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.bottomAdd()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func actionTime() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func bottomAdd() {
        //: if PunctuateThen.share.networkStatus == .Unavailable {
        if PunctuateThen.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        actionTime()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        ManagingDirectorThen.gossipingTap { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (showVoicePath.replacingOccurrences(of: "list", with: "成了") + "请求状态正常"))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(showModelFromValue)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.funcFromTime()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (const_numberContent.capitalized + ",需\u{8981}更换se" + String(k_imageData) + String(user_aboveName)))
                    //: self.func__changeHostUrl()
                    self.grievousBodilyHarm()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func grievousBodilyHarm() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        actionTime()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        constDevicePath.set(serverSelctIndex, forKey: main_halfCellFormat)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(constStatementPath.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if user_pointButtonNameUrl, !mainErrorObjectPath {
            //: serverPrefix = "http://"
            serverPrefix = (String(constStatementPath.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension LayerAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func bilge() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(app_itemPathValue.replacingOccurrences(of: "cell", with: "H") + String(appManagerContent.prefix(8)))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func stickingPoint() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(kIndexData) + String(constErrorMsg))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(constExecuteName))) || keyStr.contains((user_playValue.replacingOccurrences(of: "session", with: "un"))) || keyStr.contains((String(noti_mainContent))) || keyStr.contains((constQueryedText.replacingOccurrences(of: "click", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension LayerAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func showConvert(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(showToUrl) + String(const_bottomContent.prefix(8))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = pathLoad()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
        var urlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(constStatementPath.prefix(7))), with: (kUserPath.replacingOccurrences(of: "filter", with: "t") + String(mainPanKey)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((kUserPath.replacingOccurrences(of: "filter", with: "t") + String(mainPanKey))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(notiNameId) + String(user_iconTitle.prefix(7))) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            cameraBuild(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        playerCookies()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func cameraBuild(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: user_failureId.map{imageBlock(data: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func pathLoad() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(constStatementPath.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if user_pointButtonNameUrl, !mainErrorObjectPath {
            //: serverPrefix = "https://"
            serverPrefix = (kUserPath.replacingOccurrences(of: "filter", with: "t") + String(mainPanKey))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(appSizeTitle.suffix(4))) + "\(k_detailData)" + (String(constCoverMessageStr.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func writeClick(note _: Notification) {
        //: if PunctuateThen.share.loginUid.isEmptyString {
        if PunctuateThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            requestCorner()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            asFrom()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func requestCorner() {
        //: var htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (dataAtKey.replacingOccurrences(of: "view", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(dataAtKey.replacingOccurrences(of: "view", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (dataAtKey.replacingOccurrences(of: "view", with: "D")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(dataAtKey.replacingOccurrences(of: "view", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((dataAtKey.replacingOccurrences(of: "view", with: "D"))), h5Cookie.keys.contains((dataAtKey.replacingOccurrences(of: "view", with: "D"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(dataAtKey.replacingOccurrences(of: "view", with: "D"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(dataAtKey.replacingOccurrences(of: "view", with: "D"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath))), h5Cookie.keys.contains((showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func asFrom() {
        //: let htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
        let htmlDomain: String = PunctuateThen.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == PunctuateThen.share.loginUserMode.userID {
            if cookie.name == (dataAtKey.replacingOccurrences(of: "view", with: "D")), cookie.value == PunctuateThen.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (dataAtKey.replacingOccurrences(of: "view", with: "D")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(dataAtKey.replacingOccurrences(of: "view", with: "D"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((dataAtKey.replacingOccurrences(of: "view", with: "D"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(dataAtKey.replacingOccurrences(of: "view", with: "D"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(showThoughtPath.replacingOccurrences(of: "size", with: "P") + String(data_layerAllPath))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func playerCookies() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(dataLabName) + String(k_formatBottomData) + String(show_changeMessage)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            constDevicePath.set(cookieData, forKey: userRecordFormat)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(dataDeleteUserValue) + String(dataFrameMessage) + String(k_atMediumFormat.suffix(6))))
        }
    }

    //: func func__setAppCookies() {
    func keyCookies() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = constDevicePath.object(forKey: userRecordFormat)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func infoTo() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        constDevicePath.removeObject(forKey: userRecordFormat)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
