
//: Declare String Begin

/*: / :*/
fileprivate let main_hiddenKey:String = "app"

/*:  ( :*/
fileprivate let main_modelContent:[Character] = [" ","("]

/*: ; build: :*/
fileprivate let constTableStr:String = "index model view border gift; b"
fileprivate let const_colorFormat:String = "list aspect style center useruild:"

/*: ; iOS  :*/
fileprivate let kModelPath:String = "info equal of; iOS "

/*: ;  :*/
fileprivate let user_giftPath:String = ";view"

/*: )" :*/
fileprivate let const_transformFormat:String = "remove"

/*: "audio" :*/
fileprivate let show_targetFormat:[Character] = ["a","u","d","i","o"]

/*: + :*/
fileprivate let show_thoughtImagePath:[Character] = ["+"]

/*: .wav" :*/
fileprivate let appInputData:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let app_leadingText:String = "audio/waleading icon disappear top self"
fileprivate let constTaskMsg:String = "target"

/*: .jpg" :*/
fileprivate let mainLayerName:String = ".jpgview do view path"

/*: "image/jpeg" :*/
fileprivate let k_colorKey:[Character] = ["i","m","a","g","e"]
fileprivate let noti_sharedMsg:String = "total make for/jpeg"

/*: "://app." :*/
fileprivate let kErrorKey:String = "://apvar left number make model"
fileprivate let user_toPath:String = "p.type load"

/*: "data" :*/
fileprivate let k_effectUrl:[Character] = ["d","a","t","a"]

/*: ":null" :*/
fileprivate let app_modelMsg:String = ":nullto model title lab"

/*: ":{}" :*/
fileprivate let kImageTitle:[Character] = [":","{","}"]

/*: "Request failed, Try again later" :*/
fileprivate let userLineMsg:[UInt8] = [0x50,0x63,0x6f,0x73,0x63,0x71,0x72,0x1e,0x64,0x5f,0x67,0x6a,0x63,0x62,0x2a,0x1e,0x52,0x70,0x77,0x1e,0x5f,0x65,0x5f,0x67,0x6c,0x1e,0x6a,0x5f,0x72,0x63,0x70]

fileprivate func showMy(object num: UInt8) -> UInt8 {
    let value = Int(num) - 254
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "json error" :*/
fileprivate let main_makeValue:String = "app returnjson err"
fileprivate let appTopViewKey:String = "ocolor"

/*: "platform=iphone&version= :*/
fileprivate let app_valueUrl:[Character] = ["p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&","v","e","r","s","i"]
fileprivate let k_resultData:String = "model pic enter block dataon="

/*: &packageId= :*/
fileprivate let main_backgroundQuickJumpStr:[Character] = ["&","p","a","c","k","a","g","e","I","d"]
fileprivate let main_giftStr:String = "feature"

/*: &bundleId= :*/
fileprivate let mainModePath:String = "&bundlhandle left let point"
fileprivate let noti_addToId:[Character] = ["e","I","d","="]

/*: &lang= :*/
fileprivate let mainSizeResultId:String = "&langnormal view to content"
fileprivate let constFromSelectName:String = "gift"

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let appCameraId:String = "?!@#$"
fileprivate let appSelectedValueAddUrl:String = ":;=\'"

/*: "`<>()[]{}/\\| " :*/
fileprivate let const_managerRangeData:String = "`<>()"
fileprivate let appTableText:String = "/\\| "

/*: "/route/slowindex" :*/
fileprivate let notiPlayStr:String = "/roumodel return if model"
fileprivate let mainMakeText:[Character] = ["o","w","i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let dataGiftMsg:String = "image bottom translate center/route/i"
fileprivate let const_titleValue:[Character] = ["n","d","e","x"]

/*: "?path= :*/
fileprivate let const_keyFormat:[Character] = ["?"]
fileprivate let kTaskCellText:String = "path=model case"

/*: "&GJNonceStr= :*/
fileprivate let constEqualId:String = "case self stack the&GJNon"

/*: "& :*/
fileprivate let user_frameData:String = "title"

/*: = :*/
fileprivate let main_labMsg:[Character] = ["="]

/*: "&params= :*/
fileprivate let dataAtMessage:[Character] = ["&","p","a","r","a","m","s","="]

/*: "/ :*/
fileprivate let main_whoId:String = "/"

/*: "? :*/
fileprivate let show_headName:[Character] = ["?"]

/*: "token" :*/
fileprivate let noti_showDataMessage:[Character] = ["t","o","k","e","n"]

/*: "请求成功 :*/
fileprivate let noti_iconContent:[Character] = ["\u{8bf7}","求","\u{6210}","功"]

/*: "AAAAAAAA" :*/
fileprivate let showMakeData:[Character] = ["A","A","A"]
fileprivate let app_mobileName:String = "backgroundbackgroundbackgroundbackgroundbackground"

/*: "AA" :*/
fileprivate let showReadMsg:String = "image"

/*: "BB" :*/
fileprivate let noti_bottomTitle:[Character] = ["B","B"]

/*: "下载完成 -  :*/
fileprivate let mainOfText:String = "下载完成 - "

/*: "/Documents/" :*/
fileprivate let appAddTitle:String = "/Documvideo style for manager point"
fileprivate let show_listFormat:String = "click white status add heightents/"

/*: ".mp4" :*/
fileprivate let constToValue:String = "if else color let view.mp4"

/*: "移动地址: :*/
fileprivate let const_inputTitle:[Character] = ["移","动","\u{5730}","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let dataColorCenterToMessage:String = "后\u{53f0}"
fileprivate let showMonthName:String = "任务下载回\u{6765}"

/*: "下载成功 : :*/
fileprivate let userEqualFormat:[Character] = ["\u{4e0b}","\u{8f7d}","成","功"," ",":"]

/*: "Net Error" :*/
fileprivate let app_pathImageId:String = "size modelNet E"

/*: "下载失败 : :*/
fileprivate let showActualMsg:String = "下载失\u{8d25} :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EqualStyleSessionDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let mainDeviceEnvironmentMessage = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let show_qualityValue = "\(const_onFormat)/\(userToUrl) (\(show_screenNoData)" + (String(constTableStr.suffix(3)) + String(const_colorFormat.suffix(5))) + "\(appStatusTopName)" + (String(kModelPath.suffix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: FeatureErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let showTopStr = EqualStyleSessionDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class EqualStyleSessionDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func toFromLabel(model: SubRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.offObject() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.neighbour(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.upCompletion(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func neighbour(model: SubRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.appModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.nameSelected(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    practicalApplication(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, FeatureErrorResponse(errorCode: ListVarArg.NetError.rawValue, errorMsg: show_limitMessage))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        drawObserver(showMsg: show_limitMessage)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(show_targetFormat)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.contentTitleCell())+\(show_systemValue)" + (String(appInputData)), mimeType: (String(app_leadingText.prefix(8)) + constTaskMsg.replacingOccurrences(of: "target", with: "v")))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(mainLayerName.prefix(4))), mimeType: (String(k_colorKey) + String(noti_sharedMsg.suffix(5))))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    practicalApplication(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, FeatureErrorResponse(errorCode: ListVarArg.NetError.rawValue, errorMsg: show_limitMessage))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        drawObserver(showMsg: show_limitMessage)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func upCompletion(model: SubRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.appModel(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.nameSelected(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: mainErrorObjectPath ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = mainDeviceEnvironmentMessage }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    practicalApplication(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, FeatureErrorResponse(errorCode: ListVarArg.NetError.rawValue, errorMsg: show_limitMessage))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        drawObserver(showMsg: show_limitMessage)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = mainDeviceEnvironmentMessage }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    practicalApplication(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, FeatureErrorResponse(errorCode: ListVarArg.NetError.rawValue, errorMsg: show_limitMessage))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        drawObserver(showMsg: show_limitMessage)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func practicalApplication(model: SubRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            LayerAddrTool.share.showConvert(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if mainErrorObjectPath, model.requestServer.contains((String(kErrorKey.prefix(5)) + String(user_toPath.prefix(2)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.topVoice(key: LayerAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (String(k_effectUrl)) + "\"" + (String(app_modelMsg.prefix(5))), with: "" + "\"" + (String(k_effectUrl)) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !mainErrorObjectPath {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(app_modelMsg.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<SessionModelType>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == ListVarArg.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, FeatureErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    drawObserver(showMsg: responseModel.msg ?? String(bytes: userLineMsg.map{showMy(object: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case ListVarArg.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: k_deviceKey, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, FeatureErrorResponse(errorCode: ListVarArg.NetError.rawValue, errorMsg: (String(main_makeValue.suffix(8)) + appTopViewKey.replacingOccurrences(of: "color", with: "r")).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func appModel(model: SubRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(app_valueUrl) + String(k_resultData.suffix(3))) + "\(constKeyFormat)" + (String(main_backgroundQuickJumpStr) + main_giftStr.replacingOccurrences(of: "feature", with: "=")) + "\(show_systemValue)" + (String(mainModePath.prefix(6)) + String(noti_addToId)) + "\(show_screenNoData)" + (String(mainSizeResultId.prefix(5)) + constFromSelectName.replacingOccurrences(of: "gift", with: "=")) + "\(LayerAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if mainErrorObjectPath, serverUrl.contains((String(kErrorKey.prefix(5)) + String(user_toPath.prefix(2)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if LayerAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(notiPlayStr.prefix(4)) + "te/sl" + String(mainMakeText)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(dataGiftMsg.suffix(8)) + String(const_titleValue))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(const_keyFormat) + String(kTaskCellText.prefix(5))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(constEqualId.suffix(6)) + "ceStr=") + "\(LayerAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.componentKey(key: LayerAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(dataAtMessage)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func nameSelected(model: SubRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(show_qualityValue)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (String(noti_showDataMessage)), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func downwardsTop(model: SubRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.appModel(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(noti_iconContent)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(showMakeData) + app_mobileName.replacingOccurrences(of: "background", with: "A"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (mainOfText.capitalized) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(appAddTitle.prefix(6)) + String(show_listFormat.suffix(5))) + NSDate.contentTitleCell() + (String(constToValue.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(const_inputTitle)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (dataColorCenterToMessage + showMonthName))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = FourthBaseDelegate.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func giftOffMode(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: const_maxFormalText.recordBy(), with: k_detailData)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(userEqualFormat)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, FeatureErrorResponse(errorCode: ListVarArg.Normal.rawValue, errorMsg: (String(app_pathImageId.suffix(5)) + "rror").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, FeatureErrorResponse(errorCode: ListVarArg.NetError.rawValue, errorMsg: (String(app_pathImageId.suffix(5)) + "rror").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (showActualMsg) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func pushBack(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: const_maxFormalText.recordBy(), with: k_detailData)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(userEqualFormat)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, FeatureErrorResponse(errorCode: ListVarArg.Normal.rawValue, errorMsg: (String(app_pathImageId.suffix(5)) + "rror").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, FeatureErrorResponse(errorCode: ListVarArg.NetError.rawValue, errorMsg: (String(app_pathImageId.suffix(5)) + "rror").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (showActualMsg) + "\(errMsg)")
            }
        }
    }
}
