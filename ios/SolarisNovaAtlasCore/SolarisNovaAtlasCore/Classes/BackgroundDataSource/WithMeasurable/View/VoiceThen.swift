
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let app_makeKey:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Followers" :*/
fileprivate let noti_giftStr:String = "Follolet formal button"

/*: "Following" :*/
fileprivate let const_modeCardPath:String = "end right singleFoll"
fileprivate let k_loadName:[Character] = ["g"]

/*: "#E6E6E6" :*/
fileprivate let noti_indexName:String = "#E6E6E6row table leading"

/*: "btn_message_more" :*/
fileprivate let noti_mapSizeId:[Character] = ["b","t","n","_","m","e","s","s","a","g","e","_","m","o"]
fileprivate let kInfoTitle:[Character] = ["r","e"]

/*: "UID:  :*/
fileprivate let data_waitTitle:String = "lab name view to duringUID: "

/*: "level_ :*/
fileprivate let user_actualMessage:String = "level_string container"

/*: "Gift" :*/
fileprivate let appBitAddKey:[UInt8] = [0xdd,0xf3,0xfc,0xee]

/*: "Mike" :*/
fileprivate let user_playValue:[UInt8] = [0xb5,0xd1,0xd3,0xcd]

fileprivate func whiteContent(content num: UInt8) -> UInt8 {
    let value = Int(num) - 104
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Follow" :*/
fileprivate let k_labEqualId:[UInt8] = [0x1b,0x44,0x41,0x41,0x44,0x4c]

fileprivate func untilAt(match num: UInt8) -> UInt8 {
    let value = Int(num) - 213
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "gift_voice_card" :*/
fileprivate let main_touchModelMessage:String = "gift_by top layer bar"
fileprivate let noti_halfStr:String = "cdatad"

/*: "btn_party_voice_nor" :*/
fileprivate let app_locationFormat:String = "btn_plength screen asset label"
fileprivate let userContainerValue:[Character] = ["_","v","o","i","c","e","_","n","o","r"]

/*: "btn_party_voice_pre" :*/
fileprivate let userListData:String = "btn_patransform guard"
fileprivate let noti_shareModeValue:String = "rty_vplayer false icon make add"

/*: "attentionUid" :*/
fileprivate let show_addText:String = "attvalue"
fileprivate let app_indexMessage:String = "section news number keynUid"

/*: "source" :*/
fileprivate let app_voiceName:[Character] = ["s","o","u","r","c"]
fileprivate let constFrameNameValue:String = "now"

/*: "Kick out" :*/
fileprivate let kUserMessage:String = "Kick outuser self intimate"

/*: "Unmute" :*/
fileprivate let constToEqualValue:[Character] = ["U","n","m","u","t","e"]

/*: "Mute" :*/
fileprivate let data_valueName:[Character] = ["M","u","t","e"]

/*: "Kick off" :*/
fileprivate let showToUrl:String = "Kick offadd succeed click view equal"

/*: "Block" :*/
fileprivate let userAreaLabelStr:String = "Blockremove pop color"

/*: "Report" :*/
fileprivate let showStatusMsg:String = "Reportmake app"

/*: "#F6F6F6" :*/
fileprivate let const_netName:[Character] = ["#","F","6","F","6","F"]
fileprivate let app_accountText:String = "6"

/*: "Confirm to kick out the room?" :*/
fileprivate let dataBlackTitle:[Character] = ["C","o","n","f","i","r","m"," ","t","o"," ","k","i","c","k"," ","o","u","t"," ","t","h","e"," ","r","o","o","m","?"]

/*: "Cancel" :*/
fileprivate let showBottomMsg:String = "view manager view selfCancel"

/*: "Sure" :*/
fileprivate let user_tableFormat:String = "Sureas list self"

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let dataCloseMessage:[UInt8] = [0xe3,0x14,0x7,0xc2,0x1b,0x11,0x17,0xc2,0x15,0x17,0x14,0x7,0xc2,0x1b,0x11,0x17,0xc2,0x19,0x3,0x10,0x16,0xc2,0x16,0x11,0xc2,0x5,0x3,0x10,0x5,0x7,0xe,0xc2,0x16,0xa,0x7,0xc2,0xf,0x17,0x16,0x7,0xe1]

fileprivate func othersAm(constraint num: UInt8) -> UInt8 {
    let value = Int(num) - 162
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Confirm" :*/
fileprivate let show_indexName:String = "transform normalConfirm"

/*: "The mute has been lifted~" :*/
fileprivate let notiValueContent:String = "The muself class view burn var"
fileprivate let showModelData:String = " beecase let return guard"
fileprivate let user_atVideoFormat:String = "make m videofted~"

/*: "Confirm to kick off the seat?" :*/
fileprivate let data_intimateContent:String = "self fullConfir"
fileprivate let show_errorText:String = "m toof model"
fileprivate let dataTitleValue:String = "item view frame more info off "

/*: "OK" :*/
fileprivate let app_rangeMsg:String = "Oarray"

/*: "Shielding Success" :*/
fileprivate let app_connectValue:[Character] = ["S","h","i","e","l","d","i","n","g"," "]
fileprivate let appNamePath:String = "Successsend at view height"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VoiceThen.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol GivenNameObjectProtocol: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func worldWideNickname(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func fileAward(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func computerSimulationPosition(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func totalColor(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class VoiceThen: UIView {
    //: var popView: TalkingPopView?
    var popView: UserModelReactiveCompatible?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: StraightKeyRepresentable?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(RangeManager.universalShared().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = AuditoryImageMeasurable()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: GivenNameObjectProtocol?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == PunctuateThen.share.loginUserMode.userID {
        if uid == PunctuateThen.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        everyEvent()
        //: setupSubViewsConstraint()
        setupCellConstraint()
        //: reqUserCardInfo()
        misestimationOn()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: app_makeKey.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .clickLarge(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.hockeyPlayer()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(reflectionVideoSnap), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .clickLarge(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.toEvent()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .clickLarge(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.hockeyPlayer()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .hockeyPlayer()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(noti_giftStr.prefix(5)) + "wers").localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.clickLarge(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .toEvent()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.clickLarge(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .hockeyPlayer()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(const_modeCardPath.suffix(4)) + "owin" + String(k_loadName)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.clickLarge(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .toEvent()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.clickLarge(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(noti_indexName.prefix(7))))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.thoughtImageBundleSizeText(name: (String(noti_mapSizeId) + String(kInfoTitle))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(install), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: MakeMuteView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = MakeMuteView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension VoiceThen {
    //: func reqUserCardInfo() {
    func misestimationOn() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        LogLiveManager.child(uid: self.uid, streamerUid: liveUserId, roomId: RangeManager.universalShared().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<AuditoryImageMeasurable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.person()
            }
        }
    }

    //: func setUserCardData() {
    func person() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(data_waitTitle.suffix(5))) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.blockHalf(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.BundleImageNamed(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.thoughtImageBundleSizeText(name: (String(user_actualMessage.prefix(6))) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            sendReward(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func sendReward(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: appBitAddKey.map{$0^154}, encoding: .utf8)!)
            //: if liveUserId == PunctuateThen.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == PunctuateThen.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: user_playValue.map{whiteContent(content: $0)}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: k_labEqualId.map{untilAt(match: $0)}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.readRemove(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: appBitAddKey.map{$0^154}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.thoughtImageBundleSizeText(name: (String(main_touchModelMessage.prefix(5)) + "voice_" + noti_halfStr.replacingOccurrences(of: "data", with: "ar"))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: user_playValue.map{whiteContent(content: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.thoughtImageBundleSizeText(name: (String(app_locationFormat.prefix(5)) + "arty" + String(userContainerValue))), for: .normal)
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.thoughtImageBundleSizeText(name: (String(userListData.prefix(6)) + String(noti_shareModeValue.prefix(5)) + "oice_pre")), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.atomic(fontSize: 15)
                btn.titleLabel?.font = UIFont.atomic(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(cuttingEdge(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(noti_indexName.prefix(7))))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func cuttingEdge(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: appBitAddKey.map{$0^154}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.fileAward(uid: self.userModel.uid)
            //: self.dismiss()
            self.white()
        //: case "Mike":
        case String(bytes: user_playValue.map{whiteContent(content: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.computerSimulationPosition(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.white()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.worldWideNickname(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.white()
        //: case "Follow":
        case String(bytes: k_labEqualId.map{untilAt(match: $0)}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            utiliser(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func utiliser(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        completionPlay(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.totalColor(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func completionPlay(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(show_addText.replacingOccurrences(of: "value", with: "e") + "ntio" + String(app_indexMessage.suffix(4)))] = uid
        //: dict["source"] = "2"
        dict[(String(app_voiceName) + constFrameNameValue.replacingOccurrences(of: "now", with: "e"))] = "2"
        //: SomebodyRequestTool.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        SomebodyRequestTool.strictAdd(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func reflectionVideoSnap() {
        //: RangePushManager.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        RangePushManager.share.clickOfUid(uid: self.userModel.uid)
        //: self.dismiss()
        self.white()
    }
}

//: extension TalkingVoiceRoomCardView {
extension VoiceThen {
    //: @objc func moreBtnClick() {
    @objc func install() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == PunctuateThen.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == PunctuateThen.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(kUserMessage.prefix(8))).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(constToEqualValue)).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(data_valueName)).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(showToUrl.prefix(8))).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(userAreaLabelStr.prefix(5))).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(showStatusMsg.prefix(6))).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.TopConfig.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = AvailableViewDelegate.upArray(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(const_netName) + app_accountText.capitalized))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func groupUser() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = UserModelReactiveCompatible(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: kQueryValue, height: notiKeyTitle)
        //: popView?.initWithView(view: self)
        popView?.style(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? PublisherReactiveCompatible.getWindow())
        popView?.hadith(view: self.occurrentWith()?.view ?? PublisherReactiveCompatible.exceptAction())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func white() {
        //: popView?.dismissView()
        popView?.conversation()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension VoiceThen: LightViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func postulation(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(kUserMessage.prefix(8))).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            SizeReactiveCompatible.exceptHeader(title: nil, message: (String(dataBlackTitle)).localized, leftBtnTitle: (String(showBottomMsg.suffix(6))).localized, rightBtnTitle: (String(user_tableFormat.prefix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                DirectThen.levelHandle()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                ResurveyReqTool.phonation(targetUid: self.userModel.uid, roomId: RangeManager.universalShared().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    DirectThen.cutWith()
                    //: self.dismiss()
                    self.white()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(constToEqualValue)).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            SizeReactiveCompatible.exceptHeader(title: nil, message: String(bytes: dataCloseMessage.map{othersAm(constraint: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(showBottomMsg.suffix(6))).localized, rightBtnTitle: (String(show_indexName.suffix(7))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                DirectThen.levelHandle()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                ResurveyReqTool.feeModel(targetUid: self.userModel.uid, roomID: RangeManager.universalShared().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    DirectThen.cutWith()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.occupant(showMsg: (String(notiValueContent.prefix(6)) + "te has" + String(showModelData.prefix(4)) + "n li" + String(user_atVideoFormat.suffix(5))).localized)
                    }
                    //: self.dismiss()
                    self.white()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(data_valueName)).localized: // 设置禁言
            //: muteView.showView()
            muteView.upSum()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.white()

        //: case "Kick off".localized: /// 踢下麦
        case (String(showToUrl.prefix(8))).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            SizeReactiveCompatible.exceptHeader(title: nil, message: (String(data_intimateContent.suffix(6)) + String(show_errorText.prefix(4)) + " kick" + String(dataTitleValue.suffix(5)) + "the seat?").localized, leftBtnTitle: (String(showBottomMsg.suffix(6))).localized, rightBtnTitle: (String(user_tableFormat.prefix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                RangeManager.universalShared().currentTable(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.white()
            }

        //: case "Report".localized:
        case (String(showStatusMsg.prefix(6))).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = TitleView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.toColorView(view: nil)
        //: case "Block".localized:
        case (String(userAreaLabelStr.prefix(5))).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            SizeReactiveCompatible.exceptHeader(title: nil, message: const_netMsg, leftBtnTitle: (String(showBottomMsg.suffix(6))).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                SizeReactiveCompatible.obstruct()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                SizeReactiveCompatible.obstruct()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                BackgroundThen.nowadays(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.occupant(showMsg: (String(app_connectValue) + String(appNamePath.prefix(7))).localized)
                        //: self.dismiss()
                        self.white()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension VoiceThen {
    // 添加视图
    //: private func setupSubviews() {
    private func everyEvent() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func setupCellConstraint() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(kQueryValue / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-kQueryValue / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(kQueryValue / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.occurrentWith()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.occurrentWith()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.white()
        }
    }
}
