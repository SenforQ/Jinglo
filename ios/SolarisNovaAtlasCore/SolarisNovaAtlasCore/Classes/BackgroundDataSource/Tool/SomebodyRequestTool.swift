
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let userMobileKeyScriptPath:String = "send rid return sizemf/bu"
fileprivate let mainAddTitle:String = "ss/giftype of self make photo"
fileprivate let noti_noValue:[Character] = ["t","L","i","s","t"]

/*: "type" :*/
fileprivate let constIndexName:[UInt8] = [0x6a,0x6f,0x66,0x5b]

fileprivate func bankManager(equal num: UInt8) -> UInt8 {
    let value = Int(num) - 246
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let dataAddUrl:String = "mf/chshare warning model main for"
fileprivate let noti_userStr:[Character] = ["a","t","R","o","o","m","/","g","i","f","t","L","i","s","t"]

/*: "live/giftList" :*/
fileprivate let showFinishPath:[Character] = ["l","i","v","e","/"]
fileprivate let mainVisibleKey:String = "gimake"

/*: "party/giftList" :*/
fileprivate let show_sourceData:String = "partalong"
fileprivate let appSharedId:String = "true to custom end buttontLis"
fileprivate let noti_trustName:String = "icon"

/*: "IM/getMessageList" :*/
fileprivate let showRegionContent:String = "IM/getdata extension"
fileprivate let notiModifyMessage:String = "eLisleading var key force"
fileprivate let kMonthUserId:String = "path"

/*: "mf/business/sendMsg" :*/
fileprivate let showImageName:String = "make"
fileprivate let show_memberContentStr:String = "f/busif return view empty"
fileprivate let noti_sharedStr:String = "/sendMsgreturn need self"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let notiNowKey:String = "to"
fileprivate let userKitData:String = "bottom frame cancel let inf/chat"
fileprivate let kAddContent:String = "endMsgtime view type"

/*: "mf/business/sendGift" :*/
fileprivate let constMakePath:String = "list successmf/busin"
fileprivate let user_backgroundPath:String = "ess/senbeauty scale in file voice"
fileprivate let main_shareMsg:[Character] = ["d","G","i","f","t"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let const_voiceTitle:[Character] = ["m","f","/","c","h","a","t","R","o"]
fileprivate let notiNameHiddenText:String = "top text clearom/se"

/*: "live/sendGift" :*/
fileprivate let const_succeedSystemKey:[Character] = ["l","i","v","e","/","s","e","n","d","G","i","f","t"]

/*: "party/sendGift" :*/
fileprivate let notiSubmitData:[Character] = ["p"]
fileprivate let app_cellData:String = "arty/simage to from if view"

/*: "mf/user/getChatInfo" :*/
fileprivate let user_closedMsg:String = "list button return raw usermf/us"
fileprivate let noti_contentMsg:[Character] = ["e","r","/","g","e","t","C","h","a","t","I","n","f","o"]

/*: "IM/getMessageUserInfoList" :*/
fileprivate let appUpBlockFormat:String = "array row effect toIM/get"
fileprivate let userAgentViewMessage:String = "serInsize make"
fileprivate let user_giftContent:[Character] = ["f","o","L","i","s","t"]

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let main_actionFormat:[Character] = ["m","f","/","c","r","u","s","h","/","w","h","o","L","i","k","e","M","e","L","i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let dataLiveKey:String = "mf/crush/collection in item if"
fileprivate let app_harvestMsg:[Character] = ["i"]
fileprivate let main_centerPath:[Character] = ["g","n","o","r","e"]

/*: "mf/crush/like" :*/
fileprivate let app_dataTitle:[Character] = ["m","f","/","c","r","u","s","h"]
fileprivate let appKeyMessage:String = "table constraint/like"

/*: "mf/user/getRelation" :*/
fileprivate let userSizeKey:[Character] = ["m","f","/","u","s","e","r","/","g","e","t","R","e","l","a"]
fileprivate let mainWithClickData:String = "TION"

/*: "user/attention" :*/
fileprivate let app_contentMsg:[UInt8] = [0x1a,0x1c,0xa,0x1d,0x40,0xe,0x1b,0x1b,0xa,0x1,0x1b,0x6,0x0,0x1]

private func tableSum(value num: UInt8) -> UInt8 {
    return num ^ 111
}

/*: "user/removeAttention" :*/
fileprivate let dataChildToId:[Character] = ["u","s","e","r","/","r","e","m","o","v","e","A"]
fileprivate let k_topPath:String = "malemale"
fileprivate let mainMakeMessage:String = "epartytioparty"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let app_lineStr:[UInt8] = [0xf1,0xf,0x1c,0xd5,0x22,0xce,0x13,0xf,0x20,0x1c,0xce,0x1e,0x1d,0x17,0x1c,0x22,0x21,0xce,0x17,0x14,0xce,0x27,0x1d,0x23,0xce,0x14,0x1d,0x1a,0x1a,0x1d,0x25,0xce,0x13,0xf,0x11,0x16,0xce,0x1d,0x22,0x16,0x13,0x20,0x2c]

fileprivate func tryVar(custom num: UInt8) -> UInt8 {
    let value = Int(num) - 174
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "follow Success" :*/
fileprivate let notiTitleValue:[Character] = ["f","o","l","l","o","w"," ","S","u"]
fileprivate let appAllId:String = "ccsexs"

/*: "unfollow Success" :*/
fileprivate let kContentUrl:String = "unnor"
fileprivate let app_coverImageThanMsg:[Character] = ["o","l","l","o","w"," ","S","u","c","c","e","s","s"]

/*: "translate/msg" :*/
fileprivate let user_indexPath:[Character] = ["t","r","a","n","s"]
fileprivate let notiScreenKey:[Character] = ["l","a","t"]
fileprivate let notiYearKitStr:String = "to point selfe/msg"

/*: "msgId" :*/
fileprivate let userIntervalOfLetName:[UInt8] = [0x6b,0x75,0x61,0x4f,0x62]

private func fitMode(with num: UInt8) -> UInt8 {
    return num ^ 6
}

/*: "targetLng" :*/
fileprivate let k_itemName:[UInt8] = [0x67,0x6e,0x4c,0x74,0x65,0x67,0x72,0x61,0x74]

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let showPathData:[Character] = ["m","f","/","g","r","e"]
fileprivate let mainYearMsg:[Character] = ["e","t","/","g","e"]
fileprivate let kInputMsg:String = "send returntAut"
fileprivate let show_dataFormat:String = "etCosucceed as info"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let app_styleMessage:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","I","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: "toUid" :*/
fileprivate let mainLocalPath:[UInt8] = [0x98,0x83,0xb9,0x85,0x88]

/*: "mf/chatRoom/info" :*/
fileprivate let user_toText:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let noti_cartMessage:String = "leading intimate var size dataoom/info"

/*: "mf/chatRoom/list" :*/
fileprivate let app_voiceTitle:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let kTimeMsg:String = "oom/listday view load raw model"

/*: "roomId" :*/
fileprivate let constMainName:[UInt8] = [0xa4,0xa1,0xa1,0x9f,0x7b,0x96]

fileprivate func clickManagerApp(awake num: UInt8) -> UInt8 {
    let value = Int(num) - 50
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "needExtraInfo" :*/
fileprivate let app_observerMessage:[UInt8] = [0x6f,0x66,0x6e,0x49,0x61,0x72,0x74,0x78,0x45,0x64,0x65,0x65,0x6e]

/*: "mf/chatRoom/check" :*/
fileprivate let notiItemValue:[Character] = ["m","f","/","c","h"]
fileprivate let show_scriptUrl:String = "constraint type match key dataatRoo"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let main_appManagerName:[Character] = ["m","f","/","c","h","a","t","R","o"]
fileprivate let dataSufficientText:String = "om/joito self true message make"

/*: "mf/chatRoom/members" :*/
fileprivate let dataKeyName:String = "add"
fileprivate let const_toData:String = "view by inf/cha"
fileprivate let const_layerValue:String = "m/membersgroup share case"

/*: "translate/text" :*/
fileprivate let show_imageContent:String = "tplayernsl"
fileprivate let mainAddValue:String = "ate/textmake to not self text"

/*: "text" :*/
fileprivate let userDataTapText:[UInt8] = [0xf4,0xe5,0xf8,0xf4]

private func titleInfo(show num: UInt8) -> UInt8 {
    return num ^ 128
}

/*: "mf/user/queryStatus" :*/
fileprivate let showLevelIndexInPath:[Character] = ["m"]
fileprivate let showUpValue:String = "table head elsef/user"
fileprivate let notiMainShowMsg:String = "Statusself equal to face white"

/*: "toUids" :*/
fileprivate let user_viewFrameId:[UInt8] = [0x73,0x64,0x69,0x55,0x6f,0x74]

/*: "call/getCallTab" :*/
fileprivate let app_valueId:[Character] = ["c","a","l","l","/"]
fileprivate let dataLabName:String = "if progressgetCa"
fileprivate let data_monthMessage:String = "llTabself model"

/*: "mf/user/remark" :*/
fileprivate let dataEnterFormat:[Character] = ["m","f","/","u","s","e","r","/"]
fileprivate let notiUserMsg:String = "remarsub"

/*: "content" :*/
fileprivate let noti_makeIndexUrl:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "top" :*/
fileprivate let dataModeRawTitle:[UInt8] = [0x9f,0x84,0x9b]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SomebodyRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class SomebodyRequestTool: NSObject {
public class SomebodyRequestTool: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [RequestHandyJSON]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [RequestHandyJSON]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func replace(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(userMobileKeyScriptPath.suffix(5)) + "sine" + String(mainAddTitle.prefix(6)) + String(noti_noValue))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: constIndexName.map{bankManager(equal: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func allCompletion(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(dataAddUrl.prefix(5)) + String(noti_userStr))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func untilShow(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(showFinishPath) + mainVisibleKey.replacingOccurrences(of: "make", with: "ft") + "List")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func account(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (show_sourceData.replacingOccurrences(of: "along", with: "y") + "/gif" + String(appSharedId.suffix(4)) + noti_trustName.replacingOccurrences(of: "icon", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func skinPath(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(showRegionContent.prefix(6)) + "Messag" + String(notiModifyMessage.prefix(4)) + kMonthUserId.replacingOccurrences(of: "path", with: "t"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func anyModel(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (showImageName.replacingOccurrences(of: "make", with: "m") + String(show_memberContentStr.prefix(5)) + "iness" + String(noti_sharedStr.prefix(8)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func backOf(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (notiNowKey.replacingOccurrences(of: "to", with: "m") + String(userKitData.suffix(6)) + "Room/s" + String(kAddContent.prefix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func slight(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(constMakePath.suffix(8)) + String(user_backgroundPath.prefix(7)) + String(main_shareMsg))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func rangeBy(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(const_voiceTitle) + String(notiNameHiddenText.suffix(5)) + "ndGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func complexion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (String(const_succeedSystemKey))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func smoothLoad(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(notiSubmitData) + String(app_cellData.prefix(6)) + "endGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func wineWaiter(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(user_closedMsg.suffix(5)) + String(noti_contentMsg))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func atCurrent(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(appUpBlockFormat.suffix(6)) + "MessageU" + String(userAgentViewMessage.prefix(5)) + String(user_giftContent))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<RequestHandyJSON>.deserialize(from: result as? Array) {
            if let datas = Array<RequestHandyJSON>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [RequestHandyJSON])
                completion(succeed, datas as! [RequestHandyJSON])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func award(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        showTopStr.giftOffMode(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func addCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(main_actionFormat))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func quoteCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(dataLiveKey.prefix(9)) + String(app_harvestMsg) + String(main_centerPath))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func hemAndHawAccept(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(app_dataTitle) + String(appKeyMessage.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func cancelValue(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(userSizeKey) + mainWithClickData.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func strictAdd(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: app_contentMsg.map{tableSum(value: $0)}, encoding: .utf8)! : (String(dataChildToId) + k_topPath.replacingOccurrences(of: "male", with: "t") + mainMakeMessage.replacingOccurrences(of: "party", with: "n"))
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if PunctuateThen.share.loginUserMode.sex == Gender.female.rawValue {
                    if PunctuateThen.share.loginUserMode.sex == TopComparable.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        noti_dataSoundStr.occupant(showMsg: String(bytes: app_lineStr.map{tryVar(custom: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        noti_dataSoundStr.occupant(showMsg: (String(notiTitleValue) + appAllId.replacingOccurrences(of: "sex", with: "es")).localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    noti_dataSoundStr.occupant(showMsg: (kContentUrl.replacingOccurrences(of: "nor", with: "f") + String(app_coverImageThanMsg)).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                noti_dataSoundStr.drawObserver(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func uniform(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (String(user_indexPath) + String(notiScreenKey) + String(notiYearKitStr.suffix(5)))
        //: reqModel.params = ["msgId": msgId, "targetLng": PublisherReactiveCompatible.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: userIntervalOfLetName.map{fitMode(with: $0)}, encoding: .utf8)!: msgId, String(bytes: k_itemName.reversed(), encoding: .utf8)!: PublisherReactiveCompatible.sizeEqual(), String(bytes: constIndexName.map{bankManager(equal: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func imageCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(showPathData) + String(mainYearMsg) + String(kInputMsg.suffix(4)) + "oGre" + String(show_dataFormat.prefix(4)) + "ntent")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func recuperateCompletion(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(app_styleMessage))
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: mainLocalPath.map{$0^236}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func infoPlay(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(user_toText) + String(noti_cartMessage.suffix(8)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func sizeList(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(app_voiceTitle) + String(kTimeMsg.prefix(8)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func loadCompletion(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: constMainName.map{clickManagerApp(awake: $0)}, encoding: .utf8)!: roomId, String(bytes: app_observerMessage.reversed(), encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(notiItemValue) + String(show_scriptUrl.suffix(5)) + "m/check")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func nameCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(main_appManagerName) + String(dataSufficientText.prefix(6)) + "nOrQuit")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func comeInsideVocalisation(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (dataKeyName.replacingOccurrences(of: "add", with: "m") + String(const_toData.suffix(5)) + "tRoo" + String(const_layerValue.prefix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func takeGift(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (show_imageContent.replacingOccurrences(of: "player", with: "ra") + String(mainAddValue.prefix(8)))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: userDataTapText.map{titleInfo(show: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func toResign(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(showLevelIndexInPath) + String(showUpValue.suffix(6)) + "/query" + String(notiMainShowMsg.prefix(6)))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: user_viewFrameId.reversed(), encoding: .utf8)!: toUids, String(bytes: constIndexName.map{bankManager(equal: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func tab(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(app_valueId) + String(dataLabName.suffix(5)) + String(data_monthMessage.prefix(5)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func alongCompletion(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SubRequestModel()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(dataEnterFormat) + notiUserMsg.replacingOccurrences(of: "sub", with: "k"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: noti_makeIndexUrl.reversed(), encoding: .utf8)!: content, String(bytes: dataModeRawTitle.map{$0^235}, encoding: .utf8)!: isTop, String(bytes: mainLocalPath.map{$0^236}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showTopStr.toFromLabel(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
