
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let mainKeyMessage:[Character] = ["S","O","C","K","E"]
fileprivate let app_requestMessageMsg:String = "T_ADDnumber voice"
fileprivate let app_cornerFormat:String = "path data modeR_KEY"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let noti_modelContent:[UInt8] = [0x28,0x24,0x18,0x20,0x1a,0x29,0x34,0x2b,0x1e,0x19,0x1a,0x24,0x18,0x1d,0x16,0x29,0x34,0x27,0x1a,0x26,0x2a,0x1a,0x28,0x29,0x18,0x16,0x21,0x21,0x34,0x23,0x24,0x29,0x1e,0x1b,0x1e,0x18,0x16,0x29,0x1e,0x24,0x23]

fileprivate func atUp(next num: UInt8) -> UInt8 {
    let value = Int(num) - 213
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let notiEqualKey:[UInt8] = [0x35,0x22,0x24,0x2e,0x31,0x22,0x38,0x21,0x2b,0x28,0x26,0x33,0x34,0x24,0x35,0x22,0x22,0x29,0x38,0x29,0x28,0x33,0x2e,0x21,0x2e,0x24,0x26,0x33,0x2e,0x28,0x29]

/*: "isCalling" :*/
fileprivate let const_instanceId:String = "cell data view viewisCal"
fileprivate let userTopValue:[Character] = ["l","i","n","g"]
 
/*: "SOCKET 连接成功." :*/
fileprivate let kFromMessage:String = "so"
fileprivate let dataInfoId:String = "CKcolorT"

/*: "SOCKET 连接失败, error: :*/
fileprivate let appUserKey:String = "cellCK"
fileprivate let showNextTitle:String = "\u{5931}败, e"

/*: ." :*/
fileprivate let noti_withText:String = "."

/*: "SOCKET 失败 :*/
fileprivate let appTitleOriginName:String = "socke"
fileprivate let const_lastRenderMessage:String = "T \u{5931}败"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let appCanPolicyId:String = "\u{6b21}, 切换过"
fileprivate let data_tablePath:String = "t\u{57df}名重新尝"

/*: "SOCKET 已关闭, code: :*/
fileprivate let show_addPath:String = "levelOCKET"
fileprivate let userSlidePicPathText:String = "thought view view equal insert, co"
fileprivate let userRenderValue:[Character] = ["d","e",":"]

/*: , reason: :*/
fileprivate let show_textId:String = "in back try end, reason:"

/*: "ws:// :*/
fileprivate let notiActiveValue:String = "ws://case self equal info stretch"

/*: /?sid= :*/
fileprivate let appDoingText:String = "/?sid=make item"

/*: &uid= :*/
fileprivate let data_stopMessage:String = "&uid=let model import in collect"

/*: &platform=iphone&version= :*/
fileprivate let notiNorValue:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e","&"]
fileprivate let kTopMessage:String = "vlabel"
fileprivate let mainScalePath:String = "rsion=accept of"

/*: &packageId= :*/
fileprivate let constChangeModelUserName:[Character] = ["&","p","a","c","k","a","g","e","I","d"]
fileprivate let showToUrl:String = "half"

/*: "1v1server. :*/
fileprivate let dataRouteKey:String = "1v1serverview true var"
fileprivate let userColorMsg:String = "gift"

/*: .com:7766" :*/
fileprivate let user_modelMsg:String = ".com:wrap any custom var"
fileprivate let notiNameUrl:[Character] = ["7","7","6","6"]

/*: "43.129.185.194:7767" :*/
fileprivate let main_contentName:String = "43."
fileprivate let userPathKey:String = "129.1"
fileprivate let kFilterShowStr:String = "4:pin67"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let showModelTitle:[UInt8] = [0xb1,0xad,0xa1,0xa9,0xa3,0xb2,0x7e,0x45,0x1b,0xef,0x45,0x19,0xfa,0xd0,0xc3,0xbf,0xc1,0xc6,0xbf,0xc0,0xc7,0xca,0xc7,0xd2,0xd7,0xa1,0xc6,0xbf,0xcc,0xc5,0xc3,0xc2,0x45,0xf9,0xef,0x43,0xee,0xa,0x4d,0x1a,0xea,0x44,0xf4,0xb,0x45,0x1b,0xef,0x43,0xe3,0x11,0x47,0xf5,0xb,0x8c]

fileprivate func stylusSum(status num: UInt8) -> UInt8 {
    let value = Int(num) - 94
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cmd" :*/
fileprivate let user_managerValueMsg:String = "borderd"

/*: "ping" :*/
fileprivate let data_imageId:[Character] = ["p","i","n","g"]

/*: "callHeartbeat" :*/
fileprivate let user_colorKey:[Character] = ["c","a","l","l","H","e","a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let main_cellData:String = "recieliste"
fileprivate let showIconContent:String = "dDatelet return touch view return"

/*: "liveHeartBeat" :*/
fileprivate let main_imageData:String = "date by else label modelliveHe"

/*: "🔗 关闭 socket" :*/
fileprivate let main_maxMsg:String = "🔗"
fileprivate let userModelMsg:[Character] = [" ","关","闭"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let dataVideoValueKey:[Character] = ["T","h","e"]
fileprivate let noti_sharedMsg:String = " user top today manager point"

/*: "SOCKET  :*/
fileprivate let k_equalLabData:String = "SOCbrightE"
fileprivate let mainAspectFormat:[Character] = ["T"," "]

/*: 重连. 重试次数： :*/
fileprivate let userWithName:String = "重连. "
fileprivate let kLabelId:[Character] = ["\u{91cd}","试","\u{6b21}","数","："]

/*: , 地址： :*/
fileprivate let mainByContent:[Character] = [","," ","\u{5730}","\u{5740}","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let user_randomMessage:[Character] = ["S","O","C","K","E","T"," ","连","接","正","常"," ","不"]
fileprivate let k_shareTitle:String = "需要\u{91cd}\u{65b0}连接."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let app_popKey:[Character] = ["S","O","C","K","E"]
fileprivate let appAfterModelKey:String = "T 正在lab render any for"
fileprivate let user_topUrl:String = "连接 make需"

/*: "&resume=1" :*/
fileprivate let show_arrayActionViewId:String = "&resumlet in put photo transform"
fileprivate let const_contextMsg:[Character] = ["e","=","1"]

/*: "&lang= :*/
fileprivate let noti_ofSelectClearMsg:String = "&lang=return import"

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let notiSectionUrl:String = "sock"
fileprivate let kProgressName:[Character] = ["连","接",","," ","地","址","："]

/*: "onNetDisabled" :*/
fileprivate let k_yourData:String = "onNemask image"
fileprivate let constCommentData:String = "atabled"

/*: "errno" :*/
fileprivate let data_normalValue:String = "eshareshareno"

/*: "data" :*/
fileprivate let notiAddHeadMessage:String = "dawhite"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let user_withModelStr:[Character] = ["S","O","C","K","E","T"," ","\u{8fde}","接","\u{4fdd}"]
fileprivate let show_toFormat:[Character] = ["活","c","m","d",":"," ","p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let main_borderMakeVideoKey:[UInt8] = [0x15,0x11,0x5,0xd,0x7,0x16,0xe2,0x36,0x34,0x36,0x25,0xa6,0x81,0x5f,0xa8,0x76,0x7d,0x25,0x2f,0x26,0xfc,0xe2,0x25,0x23,0x2e,0x2e,0xa,0x27,0x23,0x34,0x36,0x24,0x27,0x23,0x36,0xf0]

fileprivate func comeToGrips(add num: UInt8) -> UInt8 {
    let value = Int(num) + 62
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let showNameId:[UInt8] = [0x2e,0x74,0x61,0x65,0x42,0x74,0x72,0x61,0x65,0x48,0x65,0x76,0x69,0x6c,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x65,0x76,0x69,0x6c,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "onP" :*/
fileprivate let notiWelcomeData:[Character] = ["o","n","P"]

/*: "msg" :*/
fileprivate let const_equalMsg:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let user_tempName:String = "🔗 with理消息"

/*: , errNo: :*/
fileprivate let k_appTitlePath:[Character] = [","," "]
fileprivate let show_barTitle:String = "format bottomerrNo:"

/*: , msg: :*/
fileprivate let kLabToData:String = "view thought, msg:"

/*: , data: :*/
fileprivate let app_reasonId:String = ", data:invite view block of"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let mainContentPath:[Character] = ["S","O","C","K","E","T"," ","\u{63a5}","收","异","常","\u{6d88}"]
fileprivate let show_coverPath:[Character] = ["息","：","c","m","d","N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let notiLogPath:[Character] = ["o","n","R","e","c","e","i","v","e","d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let appAddKey:String = "onVideoMsafety right page effect"
fileprivate let notiNamePath:[Character] = ["a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let mainHiddenStr:String = "case ofonReques"
fileprivate let kAppUrl:String = "tCallextension height"

/*: "onCancelCall" :*/
fileprivate let dataValueCustomMsg:String = "var head all modeonCan"
fileprivate let main_scaleSucceedText:[Character] = ["c","e","l","C","a","l","l"]

/*: "onRefuseCall" :*/
fileprivate let mainInfoKitValue:String = "onRefuseCfirst layer section transform"
fileprivate let noti_bottomIndicatorMsg:[Character] = ["a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let constDomainKey:[Character] = ["o","n","A","c"]
fileprivate let notiWhiteData:[Character] = ["c","e","p","t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let const_exitKey:String = "onStlet image"

/*: "freeCallTimes" :*/
fileprivate let const_edgeServiceStr:String = "FREE"
fileprivate let data_loadValue:String = "imetarget"

/*: "mfCoin" :*/
fileprivate let kPointDataMsg:String = "color observer right policy pagemfCoin"

/*: "onMissCoin" :*/
fileprivate let show_totalStr:String = "cell info cell value coloronMi"

/*: "acceptCall" :*/
fileprivate let showObserverControlHiddenName:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "stopCall" :*/
fileprivate let app_labelId:String = "S"
fileprivate let kManagerKey:[Character] = ["t","o","p","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let userTotaleractionUrl:[Character] = ["r","e","q","u","e","s"]
fileprivate let k_clickFlushMessage:String = "tCallif to color color"

/*: "[Invite you to a video call...]" :*/
fileprivate let showInnerValue:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let data_appearContent:String = "typto"

/*: "[Invite you to a voice call...]" :*/
fileprivate let mainErrorInfoMsg:[UInt8] = [0x8d,0x9f,0xb8,0xa0,0xbf,0xa2,0xb3,0xf6,0xaf,0xb9,0xa3,0xf6,0xa2,0xb9,0xf6,0xb7,0xf6,0xa0,0xb9,0xbf,0xb5,0xb3,0xf6,0xb5,0xb7,0xba,0xba,0xf8,0xf8,0xf8,0x8b]

private func keyTouch(component num: UInt8) -> UInt8 {
    return num ^ 214
}

/*: "uid" :*/
fileprivate let noti_frontGestureMsg:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let dataKitStr:[Character] = ["n","i","c","k","n"]
fileprivate let showRestoreMakeName:[Character] = ["a","m","e"]

/*: "headPic" :*/
fileprivate let userAspectUrl:[Character] = ["h","e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let constRangeTabData:[Character] = ["o","n","C","a","l","l","R","e"]
fileprivate let app_countKey:[Character] = ["s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let const_startKey:String = "topnc"

/*: "refuseCall" :*/
fileprivate let mainListenTotalMessage:String = "REFUS"

/*: "onCallMotivation" :*/
fileprivate let kViewSourceAddMessage:String = "onCalindex pop from"
fileprivate let constGiftNextTitle:String = "lMmake container model define false"
fileprivate let k_normalUrl:String = "OTIV"

/*: "onLiveWarning" :*/
fileprivate let mainUserPlayName:[Character] = ["o","n","L","i","v","e","W","a","r","n"]
fileprivate let userGiftUrl:String = "computerng"

/*: "reason" :*/
fileprivate let dataShareUrl:String = "reasotitle"

/*: "time" :*/
fileprivate let kTotalName:String = "tirequeste"

/*: "onStopLive" :*/
fileprivate let show_nameUserTitle:String = "onStoimage name"

/*: "onSendProgramGift" :*/
fileprivate let notiSizeData:String = "as inonSend"
fileprivate let showFilterId:[Character] = ["P","r","o","g","r","a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let mainModelKey:[Character] = ["o","n"]
fileprivate let noti_cameraSizeStr:String = "title labelGameFs"

/*: "broadStatus" :*/
fileprivate let data_bagKey:[Character] = ["b","r","o","a","d","S","t","a","t","u"]
fileprivate let main_cellMediumTopMsg:[Character] = ["s"]

/*: "onCallChatMsg" :*/
fileprivate let mainToMessage:String = "model elseonCal"
fileprivate let data_pathViewKey:String = "service"

/*: "onNotificationPopupWin" :*/
fileprivate let noti_placeUrl:[Character] = ["o","n","N","o","t","i","f","i","c","a","t","i","o","n","P","o"]
fileprivate let appBottomQuickMsg:[Character] = ["p","u","p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let constShowKey:[Character] = ["o","n","U","s","e","r","B","l","o","c","k"]

/*: "freeCallWin" :*/
fileprivate let appHiddenPath:String = "key appfreeC"
fileprivate let app_slideId:[Character] = ["n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WrittenRecordSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let kNoId = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let app_nextMsg = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let constAppStr = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let constErrorChangeMessage = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kMessageMsg = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let main_bottomId = (String(mainKeyMessage) + String(app_requestMessageMsg.prefix(5)) + String(app_cornerFormat.suffix(5)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let data_successUrl = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let kDetailData = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let dataBarVideoUrl = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let mainTopTitle = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let data_limitTitle = NSNotification.Name(rawValue: String(bytes: noti_modelContent.map{atUp(next: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let dataTableId = NSNotification.Name(rawValue: String(bytes: notiEqualKey.map{$0^103}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol SendToErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func nameUseData(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol VoiceObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func onObject(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func match(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol PurveyorCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func guide(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func behindOmit(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func scrubCheck(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func showAggregationData(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol DataRequestObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func primaryColour(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func groupPic(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func assetEqual(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class WrittenRecordSocketDelegate: NSObject {
    //: enum SocketRetryType: String {
    enum MoveType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currWorkSpaceVC: AnyObject?
    var currWorkSpaceVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: MakeThen.shared.direction == .rightToLeft ? 0 : kQueryValue,
        //: y: StatusBarHeight+190
        y: constLiveErrorMessage + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: MakeThen.shared.direction == .rightToLeft ? 0 : kQueryValue,
        //: y: StatusBarHeight+190
        y: constLiveErrorMessage + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: MakeThen.shared.direction == .rightToLeft ? 0 : kQueryValue,
        //: y: StatusBarHeight+500
        y: constLiveErrorMessage + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: SendToErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: VoiceObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: PurveyorCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: DataRequestObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    
    @objc dynamic var workSpace = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = WrittenRecordSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(titleTheoryNote(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(invite),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(const_instanceId.suffix(5)) + String(userTopValue)))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (QueryEnableReactiveCompatible.shared as! QueryEnableReactiveCompatible).counterdictListUp()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "workSpace")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (QueryEnableReactiveCompatible.shared as! QueryEnableReactiveCompatible).counterdictListUp()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension WrittenRecordSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        GestureLogTool.playChangeImage(msg: (kFromMessage.uppercased() + dataInfoId.replacingOccurrences(of: "color", with: "E") + " 连\u{63a5}成功."))
        //: sendPing()
        hack()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: show_skipClickText,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: main_bottomId)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        channeliseSession()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        GestureLogTool.playChangeImage(msg: (appUserKey.replacingOccurrences(of: "cell", with: "SO") + "ET \u{8fde}\u{63a5}" + showNextTitle + "rror:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = PunctuateThen.share.reachability?.connection, status != .unavailable else {
        guard let status = PunctuateThen.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == constErrorChangeMessage {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = aboveSocket(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            GestureLogTool.playChangeImage(msg: (appTitleOriginName.uppercased() + const_lastRenderMessage) + "\(constErrorChangeMessage)" + (appCanPolicyId + "Socke" + data_tablePath + "试，\u{5730}址：") + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.valueType(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.itemMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        GestureLogTool.playChangeImage(msg: (show_addPath.replacingOccurrences(of: "level", with: "S") + " 已关\u{95ed}" + String(userSlidePicPathText.suffix(4)) + String(userRenderValue)) + "\(code)" + (String(show_textId.suffix(9))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.forwardingOut(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == mainTopTitle {
            //: return
            return
        }
        //: if let status = PunctuateThen.share.reachability?.connection, status != .unavailable {
        if let status = PunctuateThen.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.valueType(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func aboveSocket(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = PunctuateThen.share.appConfigMode.ws
        self.linkAddrArr = PunctuateThen.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = couponArr()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: main_bottomId)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = PunctuateThen.share.loginUid
        let uid = PunctuateThen.share.loginUid
        //: let sid = PunctuateThen.share.loginSessionId
        let sid = PunctuateThen.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(appDoingText.prefix(6))) + "\(sid)" + (String(data_stopMessage.prefix(5))) + "\(uid)" + (String(notiNorValue) + kTopMessage.replacingOccurrences(of: "label", with: "e") + String(mainScalePath.prefix(6))) + "\(constKeyFormat)" + (String(constChangeModelUserName) + showToUrl.replacingOccurrences(of: "half", with: "=")) + "\(show_systemValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func couponArr() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return user_pointButtonNameUrl ? [(String(dataRouteKey.prefix(9)) + userColorMsg.replacingOccurrences(of: "gift", with: ".")) + "\(k_detailData)" + (String(user_modelMsg.prefix(5)) + String(notiNameUrl))] : [(main_contentName.capitalized + userPathKey.capitalized + "85.19" + kFilterShowStr.replacingOccurrences(of: "pin", with: "77"))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension WrittenRecordSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func titleTheoryNote(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            GestureLogTool.playChangeImage(msg: String(bytes: showModelTitle.map{stylusSum(status: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.forwardingOut(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.valueType(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func invite() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.valueType(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.mightHaveBeen()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension WrittenRecordSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func hack() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: kNoId, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.belowAdd(info: [(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm")): (String(data_imageId))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func targetAdd() {
        //: endTalkingPing()
        crossfire()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: app_nextMsg, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.workSpace || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currWorkSpaceVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currWorkSpaceVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.workSpace || (self.occurrentWith()?.isKind(of: KeyPartyDelegate.self) == false && self.occurrentWith()?.isKind(of: ViewErrorDelegate.self) == false) && (self.currWorkSpaceVC?.isKind(of: KeyPartyDelegate.self) == false && self.currWorkSpaceVC?.isKind(of: ViewErrorDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.belowAdd(info: [(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm")): (String(user_colorKey))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func crossfire() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func mightHaveBeen() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(main_cellData.replacingOccurrences(of: "list", with: "v") + String(showIconContent.prefix(5)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < show_objectData {
                //: PunctuateThen.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                PunctuateThen.share.detailModel(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension WrittenRecordSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func pingMethod() {
        //: endLivePing()
        pingCell()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: constAppStr, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.belowAdd(info: [(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm")): (String(main_imageData.suffix(6)) + "artBeat")])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func pingCell() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension WrittenRecordSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func channeliseSession() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = TextReactiveCompatible()
        //: let keyWindow = PublisherReactiveCompatible.getWindow()
        let keyWindow = PublisherReactiveCompatible.exceptAction()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.workSpace
        self.containerView?.isHidden = !self.workSpace
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func popView() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard LogLiveManager.anUp().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard RangeManager.universalShared().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension WrittenRecordSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func forwardingOut(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (main_maxMsg.capitalized + String(userModelMsg)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: mainTopTitle, reason: (String(dataVideoValueKey) + String(noti_sharedMsg.prefix(6)) + "logs out"))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        crossfire()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            popView()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func rawTo() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = WrittenRecordSocketDelegate.shared.aboveSocket(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.valueType(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func valueType(timeInterval: TimeInterval, type: MoveType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            self.writeLog(type: type.rawValue)
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.choiceIn(urlStr: self.connectUrlStr)
        }
    }
    
    private func writeLog(type: String) {
        GestureLogTool.playChangeImage(msg: (k_equalLabData.replacingOccurrences(of: "bright", with: "K") + String(mainAspectFormat)) + "\(type)" + (userWithName.capitalized + String(kLabelId)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func choiceIn(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                GestureLogTool.playChangeImage(msg: (String(user_randomMessage) + k_shareTitle))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                GestureLogTool.playChangeImage(msg: (String(app_popKey) + String(appAfterModelKey.prefix(4)) + user_topUrl.replacingOccurrences(of: "make", with: "不") + "要重新连接."))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.workSpace {
        if self.workSpace {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(show_arrayActionViewId.prefix(6)) + String(const_contextMsg)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(noti_ofSelectClearMsg.prefix(6))) + "\(LayerAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        forwardingOut(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kMessageMsg
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        GestureLogTool.playChangeImage(msg: (notiSectionUrl.uppercased() + "ET 开始" + String(kProgressName)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func belowAdd(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if PunctuateThen.share.reachability?.connection == nil || PunctuateThen.share.reachability?.connection == .unavailable {
        if PunctuateThen.share.reachability?.connection == nil || PunctuateThen.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm")): (String(k_yourData.prefix(4)) + "tDis" + constCommentData.replacingOccurrences(of: "tab", with: "b")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (data_normalValue.replacingOccurrences(of: "share", with: "r")): "\(dataBarVideoUrl)",
                                       //: "data": [:]]
                                       (notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            itemMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm"))] as? String == (String(data_imageId)) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                GestureLogTool.playChangeImage(msg: (String(user_withModelStr) + String(show_toFormat)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm"))] as? String == (String(user_colorKey)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                GestureLogTool.playChangeImage(msg: String(bytes: main_borderMakeVideoKey.map{comeToGrips(add: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm"))] as? String == (String(main_imageData.suffix(6)) + "artBeat") {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                GestureLogTool.playChangeImage(msg: String(bytes: showNameId.reversed(), encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm"))] as? String == (String(data_imageId)) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm")): (String(notiWelcomeData)),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (data_normalValue.replacingOccurrences(of: "share", with: "r")): "\(data_successUrl)",
                                           //: "data": [:]]
                                           (notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                itemMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.valueType(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func itemMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(user_managerValueMsg.replacingOccurrences(of: "border", with: "cm"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(data_normalValue.replacingOccurrences(of: "share", with: "r"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(const_equalMsg))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (user_tempName.replacingOccurrences(of: "with", with: "处") + "\u{ff1a}cmdName:") + "\(cmdName)" + (String(k_appTitlePath) + String(show_barTitle.suffix(6))) + "\(errorNo)" + (String(kLabToData.suffix(6))) + "\(msg)" + (String(app_reasonId.prefix(7))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        orInLive(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            GestureLogTool.playChangeImage(msg: (String(mainContentPath) + String(show_coverPath)) + "\(cmdName)" + (String(k_appTitlePath) + String(show_barTitle.suffix(6))) + "\(errorNo)" + (String(kLabToData.suffix(6))) + "\(msg)" + (String(app_reasonId.prefix(7))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: PunctuateThen.share.loginUserMode.freeCallTimes = 0
                PunctuateThen.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !PunctuateThen.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !PunctuateThen.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                MessageThen.shared.eponym()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.nameUseData(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(notiLogPath)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: constBarContent, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(appAddKey.prefix(8)) + String(notiNamePath)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.onObject(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(mainHiddenStr.suffix(8)) + String(kAppUrl.prefix(5))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.match(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(dataValueCustomMsg.suffix(5)) + String(main_scaleSucceedText)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(mainInfoKitValue.prefix(9)) + String(noti_bottomIndicatorMsg)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(constDomainKey) + String(notiWhiteData)) { // 接收方发送acceptCall回执
            //: self.workSpace = true
            self.workSpace = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            targetAdd()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.groupPic(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(const_exitKey.prefix(4)) + "opCall") { // 接收方发送stopCall回执
            //: self.workSpace = false
            self.workSpace = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            crossfire()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(const_edgeServiceStr.lowercased() + "CallT" + data_loadValue.replacingOccurrences(of: "target", with: "s"))] as? Int {
                //: PunctuateThen.share.loginUserMode.freeCallTimes = freeCallTimes
                PunctuateThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: PunctuateThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            PunctuateThen.share.loginUserMode.mf_coin = json[(notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta"))][(String(kPointDataMsg.suffix(6)))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: k_bottomImageId, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (QueryEnableReactiveCompatible.shared as! QueryEnableReactiveCompatible).dropPackage()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(show_totalStr.suffix(4)) + "ssCoin") { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.showAggregationData(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(showObserverControlHiddenName)) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.workSpace = true
            self.workSpace = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            targetAdd()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.guide(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (app_labelId.lowercased() + String(kManagerKey)) { // 接收方/拨打方 挂断了通话
            //: self.workSpace = false
            self.workSpace = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            crossfire()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.behindOmit(data: data)
            // 更新余额
            //: PunctuateThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            PunctuateThen.share.loginUserMode.mf_coin = json[(notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta"))][(String(kPointDataMsg.suffix(6)))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            rein(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: k_bottomImageId, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (QueryEnableReactiveCompatible.shared as! QueryEnableReactiveCompatible).dropPackage()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(userTotaleractionUrl) + String(k_clickFlushMessage.prefix(5))) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: data_limitTitle, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: PunctuateThen.share.recieve1v1TalkRequest(info: data)
                PunctuateThen.share.detailModel(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(main_cellData.replacingOccurrences(of: "list", with: "v") + String(showIconContent.prefix(5)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: showInnerValue.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta"))][(data_appearContent.replacingOccurrences(of: "to", with: "e"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: mainErrorInfoMsg.map{keyTouch(component: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                FourthBaseDelegate.totaleraction(uid: String(json[(notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta"))][(String(noti_frontGestureMsg))].intValue),
                                                //: title: data["nickname"] as? String,
                                                title: data[(String(dataKitStr) + String(showRestoreMakeName))] as? String,
                                                //: body: body,
                                                body: body,
                                                //: imageUrl: data["headPic"] as? String)
                                                imageUrl: data[(String(userAspectUrl))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                FourthBaseDelegate.shared.timeBgm()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(constRangeTabData) + String(app_countKey)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.workSpace = true
            self.workSpace = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            targetAdd()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(constRangeTabData) + String(app_countKey))] = "1"
            //: PunctuateThen.share.recieve1v1TalkResumeRequest(info: resumeData)
            PunctuateThen.share.restart(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (QueryEnableReactiveCompatible.shared as! QueryEnableReactiveCompatible).counterdictListUp()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (const_startKey.replacingOccurrences(of: "top", with: "ca") + "elCall") { // 拨打方拒绝了通话，接收方收到该指令
            //: self.workSpace = false
            self.workSpace = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            crossfire()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.scrubCheck(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                FourthBaseDelegate.corner(identifier: String(json[(notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta"))][(String(noti_frontGestureMsg))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                FourthBaseDelegate.shared.button()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (mainListenTotalMessage.lowercased() + "eCall") { // 接收方拒绝了通话，拨打方收到该指令
            //: self.workSpace = false
            self.workSpace = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            crossfire()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.primaryColour(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(kViewSourceAddMessage.prefix(5)) + String(constGiftNextTitle.prefix(2)) + k_normalUrl.lowercased() + "ation") { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if PunctuateThen.share.loginUserMode.sex == Gender.female.rawValue {
            if PunctuateThen.share.loginUserMode.sex == TopComparable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.assetEqual?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(mainUserPlayName) + userGiftUrl.replacingOccurrences(of: "computer", with: "i")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            LogLiveManager.miniTime(content: msg, reason: json[(notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta"))][(dataShareUrl.replacingOccurrences(of: "title", with: "n"))].stringValue, time: json[(notiAddHeadMessage.replacingOccurrences(of: "white", with: "ta"))][(kTotalName.replacingOccurrences(of: "request", with: "m"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(show_nameUserTitle.prefix(5)) + "pLive") { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            LogLiveManager.anUp().whoIn()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(notiSizeData.suffix(6)) + String(showFilterId)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.occupant(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(mainModelKey) + String(noti_cameraSizeStr.suffix(6))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(data_bagKey) + String(main_cellMediumTopMsg))] as? Int ?? -1
            //: if PunctuateThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if PunctuateThen.share.appStatus == HiddenHashableRepresentation.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: dataTableId, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: constBarContent, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: dataTableId, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(mainToMessage.suffix(5)) + "lChatM" + data_pathViewKey.replacingOccurrences(of: "service", with: "sg")) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            MoveDanmuManager.underDismiss().afterAdd(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(noti_placeUrl) + String(appBottomQuickMsg)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            MessageThen.shared.changeDic(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(constShowKey)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            LogLiveManager.anUp().whoIn()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            RangeManager.universalShared().proposal()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: k_deviceKey, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension WrittenRecordSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func orInLive(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(userTotaleractionUrl) + String(k_clickFlushMessage.prefix(5))) || cmdName == (String(mainHiddenStr.suffix(8)) + String(kAppUrl.prefix(5))) {
            //: if TalkingLiveManager.shared().isLive == true {
            if LogLiveManager.anUp().isLive == true {
                //: NEED_RESTORE_LIVE = true
                main_partyData = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                LogLiveManager.builder(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                LogLiveManager.anUp().whoIn()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard main_partyData == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (const_startKey.replacingOccurrences(of: "top", with: "ca") + "elCall") || cmdName == (String(dataValueCustomMsg.suffix(5)) + String(main_scaleSucceedText)) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (mainListenTotalMessage.lowercased() + "eCall") || cmdName == (String(mainInfoKitValue.prefix(9)) + String(noti_bottomIndicatorMsg)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (app_labelId.lowercased() + String(kManagerKey)) || cmdName == (String(const_exitKey.prefix(4)) + "opCall")
        {
            //: NEED_RESTORE_LIVE = false
            main_partyData = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = LayerSomebodyView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                LogLiveManager.anUp().next()
            }
            //: alert.show()
            alert.shadowTheme()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func rein(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(const_edgeServiceStr.lowercased() + "CallT" + data_loadValue.replacingOccurrences(of: "target", with: "s"))] as? Int {
            //: PunctuateThen.share.loginUserMode.freeCallTimes = freeCallTimes
            PunctuateThen.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(appHiddenPath.suffix(5)) + "allWi" + String(app_slideId))] as? Bool
        //: if freeCallWin == true && PunctuateThen.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, PunctuateThen.share.loginUserMode.sex == TopComparable.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = ViewObjectProtocol()
            //: view.show()
            view.toBlock()
            //: view.setShowData(data: data)
            view.selectCell(data: data)
        }
    }
}
