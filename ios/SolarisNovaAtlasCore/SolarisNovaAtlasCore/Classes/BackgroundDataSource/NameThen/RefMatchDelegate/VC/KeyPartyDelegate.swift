
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let app_viewData:[UInt8] = [0xce,0xc9,0xce,0xd3,0x8f,0xc4,0xc8,0xc3,0xc2,0xd5,0x9d,0x8e,0x87,0xcf,0xc6,0xd4,0x87,0xc9,0xc8,0xd3,0x87,0xc5,0xc2,0xc2,0xc9,0x87,0xce,0xca,0xd7,0xcb,0xc2,0xca,0xc2,0xc9,0xd3,0xc2,0xc3]

private func conversationTypeLoad(input num: UInt8) -> UInt8 {
    return num ^ 167
}

/*: "uid" :*/
fileprivate let k_cameraData:[UInt8] = [0xbd,0xa1,0xac]

private func appReplyTop(pull num: UInt8) -> UInt8 {
    return num ^ 200
}

/*: "headPic" :*/
fileprivate let noti_pointId:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let appEnableValue:String = "N"
fileprivate let constStatusFormat:String = "icknamview"

/*: "birthday" :*/
fileprivate let appTimeName:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let k_kindTitle:String = "vidmodel"

/*: "cover" :*/
fileprivate let mainTopStr:String = "valuever"

/*: "location" :*/
fileprivate let const_liveNativeMsg:[Character] = ["l","o","c","a","t"]
fileprivate let user_sessionKey:[Character] = ["i","o","n"]

/*: "cmd" :*/
fileprivate let k_appStr:[UInt8] = [0x35,0x3b,0x32]

private func addEdit(sex num: UInt8) -> UInt8 {
    return num ^ 86
}

/*: "refuseCall" :*/
fileprivate let appNamePath:[Character] = ["r","e","f","u","s","e","C","a"]
fileprivate let noti_centerTitle:[Character] = ["l","l"]

/*: "data" :*/
fileprivate let showTextData:[UInt8] = [0x18,0x1d,0x8,0x1d]

private func touchFalse(cell num: UInt8) -> UInt8 {
    return num ^ 124
}

/*: "Call interruption" :*/
fileprivate let data_spacePath:String = "Call face color"
fileprivate let main_giftMsg:String = "RRUP"

/*: "stopCall" :*/
fileprivate let showModeKey:String = "in name frame self cornerstopCa"
fileprivate let const_allowUrl:String = "valuevalue"

/*: "logId" :*/
fileprivate let showCollectionUrl:[UInt8] = [0x93,0x90,0x98,0xb6,0x9b]

private func equalDate(ok num: UInt8) -> UInt8 {
    return num ^ 255
}

/*: "cancelCall" :*/
fileprivate let app_largeTitle:String = "calabelce"
fileprivate let dataCoverStr:String = "interval if make caselCall"

/*: "No answer" :*/
fileprivate let notiPathUserId:String = "succeed andNo answer"

/*: "duration" :*/
fileprivate let user_managerTopMsg:[Character] = ["d"]
fileprivate let mainHiddenLayerName:String = "uramodelon"

/*: "She has hung up" :*/
fileprivate let dataRawMsg:String = "She hascolor string if migration cell"
fileprivate let main_drawName:[Character] = [" ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let main_tagPath:String = "livencel"
fileprivate let main_currentTitle:String = "Sexcolor origin corner video"

/*: "He has hung up" :*/
fileprivate let noti_listFormat:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"]
fileprivate let userAppId:String = "manager user self false i up"

/*: "endType" :*/
fileprivate let noti_makeData:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let dataNorId:String = "info"
fileprivate let show_maxData:String = "topSexgroup as layer up content"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let mainViewLogName:[UInt8] = [0x40,0x67,0x7a,0x7c,0x6f,0x6f,0x60,0x6a,0x60,0x6c,0x67,0x7d,0x29,0x6e,0x66,0x65,0x6d,0x29,0x6a,0x66,0x60,0x67,0x7a,0x25,0x29,0x6a,0x68,0x65,0x65,0x29,0x61,0x68,0x7a,0x29,0x6c,0x67,0x6d,0x6c,0x6d]

private func textElse(image num: UInt8) -> UInt8 {
    return num ^ 9
}

/*: "The other hung up the phone" :*/
fileprivate let user_sexName:String = "each start var inThe o"
fileprivate let dataViewPath:String = "view equal collectionung up "
fileprivate let k_showId:String = "the let share frame"
fileprivate let appShareFormat:String = "PHONE"

/*: "type" :*/
fileprivate let app_cornerPath:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let k_loadMsg:String = "jumpUrlself normal data half let"

/*: "Cancel" :*/
fileprivate let mainRawValue:String = "medium"
fileprivate let showPricePath:String = "namecel"

/*: "Get gold coins" :*/
fileprivate let data_whoMsg:String = "view text social hiddenGet gold"
fileprivate let main_pathMaxFormat:String = "moment color coins"

/*: "addBean" :*/
fileprivate let userAtEmptyPath:String = "addBeapp regular"
fileprivate let main_pathCustomExtraName:[Character] = ["a","n"]

/*: "refuseSex" :*/
fileprivate let main_succeedSpaceTitle:String = "instance log super time sharerefuseSex"

/*: "onAcceptCall" :*/
fileprivate let notiEventStr:String = "onAccepinside manager"
fileprivate let const_modelName:String = "tCallequal current up array click"

/*: "acceptCall" :*/
fileprivate let notiBindText:[Character] = ["a","c","c"]
fileprivate let noti_countFormat:String = "eptCallfrom script phenomenon"

/*: "Calls are cancelled" :*/
fileprivate let constObjectFormat:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n","c","e","l","l"]
fileprivate let k_imagePath:String = "path"

/*: "The call has ended" :*/
fileprivate let dataPointSearchedKey:String = "image device image labThe c"
fileprivate let appAddKey:String = "has endedtarget app make error"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  KeyPartyDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let userAgeStr = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class KeyPartyDelegate: SizeViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ExamineRequestChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: RangePlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: FileMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: LocomoteScreenView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        WrittenRecordSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        WrittenRecordSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        WrittenRecordSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: app_viewData.map{conversationTypeLoad(input: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! DirectionReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.writeConstraint()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            outsideSearch()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            untilModel(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currWorkSpaceVC = nil
            WrittenRecordSocketDelegate.shared.currWorkSpaceVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.cachCounteraction()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        stack()
        //: setupSubViewsConstraint()
        swaddlingClothes()
        //: initNotificationObserver()
        toPlay()
        //: req_callGetUserInfo()
        sumerval()
        //: timeoutRules()
        timeout()
        //: setUpBGMPlayer()
        giftFor()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.attack()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PunctuateThen.share.appConfigMode.disableShootScreen {
            if PunctuateThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = StartView(frame: CGRect(x: 0, y: 0, width: kQueryValue, height: notiKeyTitle))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.workSpace = false
        WrittenRecordSocketDelegate.shared.workSpace = false
        //: self.stopVCAllActivties()
        self.behindTaskTitle()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: FailThen = {
        //: let v = TalkingBGVideoView()
        let v = FailThen()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.deadlineShow(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: FieldManagerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = FieldManagerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: EqualView = {
        //: let v = TalkingVideoWindowView()
        let v = EqualView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = kQueryValue - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if MakeThen.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: constLiveErrorMessage + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.sectionView()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: SubSizeThen = {
        //: let m = TalkingVideoChatManager()
        let m = SubSizeThen()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension KeyPartyDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func sumerval() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        SubSizeThen.poking(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: k_cameraData.map{appReplyTop(pull: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(noti_pointId))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(appEnableValue.lowercased() + constStatusFormat.replacingOccurrences(of: "view", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(appTimeName))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(k_kindTitle.replacingOccurrences(of: "model", with: "e") + "oUrl")].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(mainTopStr.replacingOccurrences(of: "value", with: "co"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(const_liveNativeMsg) + String(user_sessionKey))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.activeFor()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.storage()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func storage() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = TvCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = TvCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.reloadOrCamera(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.formatAt(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension KeyPartyDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func outsideSearch() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        CustomPermissionTool.showDrunk { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.drawObserver(showMsg: constChangeName)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (String(appNamePath) + String(noti_centerTitle)), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: k_cameraData.map{appReplyTop(pull: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.cornerWrite()
                //: return
                return
            }

            //: self.startPreview()
            self.attack()
            //: self.stopBGM()
            self.begin()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = RangePlayer.pathVariation(type: .Response)
            //: self.startBGM()
            self.writeConstraint()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func attack() {
        //: self.videoManager.startPreview()
        self.videoManager.outEqual()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func behindTaskTitle() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.begin()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        MoveDanmuManager.appearLine()
        //: TalkingSocketManager.shared.callerDelegate = nil
        WrittenRecordSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        WrittenRecordSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        WrittenRecordSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func cornerWrite(animated: Bool = true) {
        //: self.stopBGM()
        self.begin()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func sectionView() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.renderView()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.storage()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func strength() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.television()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.storage()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.orientation()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func orientation() {
        //: guard PunctuateThen.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard PunctuateThen.share.loginUserMode.sex == TopComparable.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = LocomoteScreenView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.thePrice()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension KeyPartyDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func toPlay() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(attachText(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(noneSubLess(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: const_textInfoName,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(positionKey(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: show_skipClickText,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        userAgeStr.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func attachText(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            cornerWrite()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func positionKey(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard WrittenRecordSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        WrittenRecordSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        WrittenRecordSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        WrittenRecordSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.postNet()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func noneSubLess(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.storage()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension KeyPartyDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.drawObserver(showMsg: (String(data_spacePath.prefix(5)) + "inte" + main_giftMsg.lowercased() + "tion"))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (String(showModeKey.suffix(6)) + const_allowUrl.replacingOccurrences(of: "value", with: "l")), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: showCollectionUrl.map{equalDate(ok: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (app_largeTitle.replacingOccurrences(of: "label", with: "n") + String(dataCoverStr.suffix(5))), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: showCollectionUrl.map{equalDate(ok: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (String(appNamePath) + String(noti_centerTitle)), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: k_cameraData.map{appReplyTop(pull: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.birth()
                //: TalkingSocketManager.shared.currWorkSpaceVC = nil
                WrittenRecordSocketDelegate.shared.currWorkSpaceVC = nil
            }
            //: popCurrentViewController()
            cornerWrite()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension KeyPartyDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func giftFor() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? EqualStrideable.Call : EqualStrideable.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = RangePlayer.pathVariation(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func writeConstraint() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.modelPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func begin() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.noView()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension KeyPartyDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func timeout() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? constAskErrorId : self.chatModel?.afterView()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            execute()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.execute()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func execute() {
        //: guard !TalkingSocketManager.shared.workSpace else { return }
        guard !WrittenRecordSocketDelegate.shared.workSpace else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.drawObserver(showMsg: (String(notiPathUserId.suffix(9))).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (app_largeTitle.replacingOccurrences(of: "label", with: "n") + String(dataCoverStr.suffix(5))), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: showCollectionUrl.map{equalDate(ok: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (String(appNamePath) + String(noti_centerTitle)), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: k_cameraData.map{appReplyTop(pull: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        WrittenRecordSocketDelegate.shared.belowAdd(info: socketDict)
        //: popCurrentViewController()
        cornerWrite()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension KeyPartyDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func uphold(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: showCollectionUrl.map{equalDate(ok: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(user_managerTopMsg) + mainHiddenLayerName.replacingOccurrences(of: "model", with: "ti"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.sizeNow(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.stage()
            //: createTalkRoom()
            strength()
            //: self.stopBGM()
            self.begin()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func untilModel(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.renderView()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.deadlineShow(show: false)
        //: self.stopBGM()
        self.begin()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.stage()
        //: self.createTalkRoom()
        self.strength()
    }
}

// MARK: - PurveyorCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension KeyPartyDelegate: PurveyorCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func scrubCheck(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(dataRawMsg.prefix(7)) + String(main_drawName)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(main_tagPath.replacingOccurrences(of: "live", with: "ca") + String(main_currentTitle.prefix(3)))].stringValue == TopComparable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(noti_listFormat) + String(userAppId.suffix(3))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.drawObserver(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.birth()
            //: TalkingSocketManager.shared.currWorkSpaceVC = nil
            WrittenRecordSocketDelegate.shared.currWorkSpaceVC = nil
        }

        //: popCurrentViewController()
        cornerWrite()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func guide(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        untilModel(logId: json[String(bytes: showCollectionUrl.map{equalDate(ok: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func behindOmit(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(noti_makeData))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(dataRawMsg.prefix(7)) + String(main_drawName)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(dataNorId.replacingOccurrences(of: "info", with: "s") + String(show_maxData.prefix(6)))].stringValue == TopComparable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(noti_listFormat) + String(userAppId.suffix(3))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if PunctuateThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if PunctuateThen.share.appStatus == HiddenHashableRepresentation.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: mainViewLogName.map{textElse(image: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(user_sexName.suffix(5)) + "ther h" + String(dataViewPath.suffix(7)) + String(k_showId.prefix(4)) + appShareFormat.lowercased()).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.drawObserver(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.birth()
            //: TalkingSocketManager.shared.currWorkSpaceVC = nil
            WrittenRecordSocketDelegate.shared.currWorkSpaceVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.attend()
        //: popCurrentViewController()
        cornerWrite()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func showAggregationData(msg: String, data: [String: Any]) {
        //: guard PunctuateThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard PunctuateThen.share.appStatus == HiddenHashableRepresentation.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(app_cornerPath))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(k_loadMsg.prefix(7)))] as? String else { return }
            //: let httpsUrl = PunctuateThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = PunctuateThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = SearchConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: RangePushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            RangePushManager.share.userBlade(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            SizeReactiveCompatible.exceptHeader(title: nil,
                                          //: message: msg,
                                          message: msg,
                                          //: leftBtnTitle: "Cancel".localized,
                                          leftBtnTitle: (mainRawValue.replacingOccurrences(of: "medium", with: "C") + showPricePath.replacingOccurrences(of: "name", with: "an")).localized,
                                          //: rightBtnTitle: "Get gold coins".localized) {
                                          rightBtnTitle: (String(data_whoMsg.suffix(8)) + String(main_pathMaxFormat.suffix(6))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                SizeReactiveCompatible.obstruct()
                //: } rightBlock: {
            } rightBlock: {
                //: RangePushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                RangePushManager.share.picParams(clickEvent: main_statusName, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func receiveArr(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.makeArr(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func assetEqual(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(userAtEmptyPath.prefix(5)) + String(main_pathCustomExtraName))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.overLicense(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.city(point: addBean)
        }
    }
}

// MARK: - DataRequestObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension KeyPartyDelegate: DataRequestObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func primaryColour(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(dataRawMsg.prefix(7)) + String(main_drawName)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(main_succeedSpaceTitle.suffix(9)))].stringValue == TopComparable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(noti_listFormat) + String(userAppId.suffix(3))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.drawObserver(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.birth()
            //: TalkingSocketManager.shared.currWorkSpaceVC = nil
            WrittenRecordSocketDelegate.shared.currWorkSpaceVC = nil
        }

        //: popCurrentViewController()
        cornerWrite()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func groupPic(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        untilModel(logId: json[String(bytes: showCollectionUrl.map{equalDate(ok: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - SendToErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension KeyPartyDelegate: SendToErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func nameUseData(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(appNamePath) + String(noti_centerTitle)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.drawObserver(showMsg: (String(notiPathUserId.suffix(9))).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.birth()
                //: TalkingSocketManager.shared.currWorkSpaceVC = nil
                WrittenRecordSocketDelegate.shared.currWorkSpaceVC = nil
            }
            //: popCurrentViewController()
            cornerWrite()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(notiEventStr.prefix(7)) + String(const_modelName.prefix(5))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard PunctuateThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PunctuateThen.share.appStatus == HiddenHashableRepresentation.normal.rawValue else { return }
            //: RangePushManager.share.func__jumpToWebRecharge(sufficient: false)
            RangePushManager.share.picParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(notiEventStr.prefix(7)) + String(const_modelName.prefix(5))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.drawObserver(showMsg: msg)
            //: popCurrentViewController()
            cornerWrite()
        }
    }
}

// MARK: - ColoringObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension KeyPartyDelegate: ColoringObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func viewAdd() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (String(notiBindText) + String(noti_countFormat.prefix(7))), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: k_cameraData.map{appReplyTop(pull: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func titleIn() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.drawObserver(showMsg: (String(constObjectFormat) + k_imagePath.replacingOccurrences(of: "path", with: "ed")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (app_largeTitle.replacingOccurrences(of: "label", with: "n") + String(dataCoverStr.suffix(5))), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: showCollectionUrl.map{equalDate(ok: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.drawObserver(showMsg: (String(constObjectFormat) + k_imagePath.replacingOccurrences(of: "path", with: "ed")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (String(appNamePath) + String(noti_centerTitle)), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: k_cameraData.map{appReplyTop(pull: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.drawObserver(showMsg: (String(dataPointSearchedKey.suffix(5)) + "all " + String(appAddKey.prefix(9))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            WrittenRecordSocketDelegate.shared.belowAdd(info: [String(bytes: k_appStr.map{addEdit(sex: $0)}, encoding: .utf8)!: (String(showModeKey.suffix(6)) + const_allowUrl.replacingOccurrences(of: "value", with: "l")), String(bytes: showTextData.map{touchFalse(cell: $0)}, encoding: .utf8)!: [String(bytes: showCollectionUrl.map{equalDate(ok: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        cornerWrite()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func birthday(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.useForPortraitCamera(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func reciprocationUserRandomLine(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.deadlineShow(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.makeObject(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.storage()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func shadow() {
        //: TalkingSocketManager.shared.currWorkSpaceVC = self
        WrittenRecordSocketDelegate.shared.currWorkSpaceVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = FileMiniView.videoComment()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.burn()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.meet()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.occurrentWith()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        cornerWrite()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func beastExecute() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension KeyPartyDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func stack() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.infoView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func swaddlingClothes() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
