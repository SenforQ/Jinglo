
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let k_aspectHiddenId:String = "IM\u{9000}出登录"
fileprivate let app_imageQuantityimateData:String = "de:background for"

/*: , desc: :*/
fileprivate let const_viewText:String = ", "
fileprivate let data_downUrl:String = "desc:comment object"

/*: ." :*/
fileprivate let noti_sizeUrl:[Character] = ["."]

/*: "IM登录失败，code: :*/
fileprivate let showDateTitle:[Character] = ["I","M","登","\u{5f55}","失","败","，","c","o","d","e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let notiViewName:[Character] = ["I","M","获","取","未","读","消","息","\u{603b}","\u{6570}","失"]
fileprivate let noti_nameId:String = "败，code:cell message key over kit"

/*: "crushBadge" :*/
fileprivate let appFrameUrl:String = "touchus"
fileprivate let kArrayUrl:String = "background viewhBadge"

/*: "badNumber" :*/
fileprivate let main_removeUrl:[UInt8] = [0x8a,0x89,0x8c,0x76,0x9d,0x95,0x8a,0x8d,0x9a]

fileprivate func voiceCellSize(result num: UInt8) -> UInt8 {
    let value = Int(num) + 216
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let showBeginFormat:String = "framelaza"

/*: "roomNum" :*/
fileprivate let data_tableTapLabelUrl:[Character] = ["r","o","o","m","N","u"]
fileprivate let appViewPath:String = "count"

/*: "num" :*/
fileprivate let data_leadingStr:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let user_messagePath:String = "array toonlyO"
fileprivate let noti_pointName:[Character] = ["o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let app_makeTitle:String = "LIVE-Cfeature income var view"
fileprivate let notiTextTagKey:String = "HAobject"

/*: "PARTY-CHAT" :*/
fileprivate let main_barStr:String = "PARTY-index make of cell"

/*: "extra" :*/
fileprivate let main_toUrl:String = "EXTRA"

/*: "msgType" :*/
fileprivate let data_fromTagFormat:String = "user"
fileprivate let k_sourceKey:[Character] = ["g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let main_toData:[Character] = ["n","e","w","P","k","g"]
fileprivate let mainErrorPath:String = "Gifttype view"

/*: "MF:ChatMentionMsg" :*/
fileprivate let dataEqualTitle:[Character] = ["M","F",":","C","h","a","t","M","e","n"]
fileprivate let showOfSucceedMsg:String = "tionMsgequal status observer"

/*: "uid" :*/
fileprivate let userNameMainValue:[UInt8] = [0xbb,0xa7,0xaa]

private func indexImage(title num: UInt8) -> UInt8 {
    return num ^ 206
}

/*: "onlineStatus" :*/
fileprivate let constContentBucketData:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let noti_blockText:[UInt8] = [0x72,0x65,0x73,0x55,0x77,0x65,0x4e,0x73,0x69]

/*: "c2c_ :*/
fileprivate let showFrameStatusMsg:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let k_bottomContent:[Character] = ["I","M","获","取","会","话","失","\u{8d25}","，"]
fileprivate let notiNameContent:String = "convId:add status"

/*: , code: :*/
fileprivate let user_firstUrl:String = "var var object, code:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let dataGiftKey:[Character] = ["I","M","插","入","消","息","失","败","，","t","a","r"]
fileprivate let app_bottomStr:String = "cell return homegetId:"

/*: "changeDirection" :*/
fileprivate let constBackgroundValue:String = "countange"
fileprivate let kNowData:String = "CTION"

/*: "rYMsgType" :*/
fileprivate let noti_maleId:[Character] = ["r","Y","M","s","g","T"]
fileprivate let appStopMsg:String = "YPE"

/*: "GJ:CallCustom" :*/
fileprivate let k_contentInnerKey:[Character] = ["G","J",":","C","a","l"]
fileprivate let userViewStr:[Character] = ["l","C","u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let user_textName:String = "IM\u{83b7}取"
fileprivate let notiEqualValue:[Character] = ["会","话","列","\u{8868}","失","败","，","c","o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let noti_inputFormat:String = "video"
fileprivate let const_haveViewTitle:String = "menu lab privatesgInfo"

/*: "needFold" :*/
fileprivate let const_succeedId:[Character] = ["n","e","e","d","F","o","l","d"]

/*: "group_ :*/
fileprivate let constKeyJumpButtonPath:String = "GROUP"
fileprivate let kFrameText:String = "_"

/*: "IM删除会话失败，convID: :*/
fileprivate let dataReloadPlayerPath:[Character] = ["I","M","删","除","\u{4f1a}","话","失","败","，","c","o","n"]
fileprivate let mainUserFormat:String = "if finish path image fieldvID:"

/*: "IM置顶会话失败，convId: :*/
fileprivate let noti_toolData:String = "IM置\u{9876}\u{4f1a}"
fileprivate let app_upDigitalValue:[Character] = ["\u{8bdd}","失","败"]
fileprivate let app_modelDataUrl:[Character] = ["，","c","o","n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let show_hideUrl:[Character] = ["S","D","K"," ","正","\u{5728}","连","接","到","\u{670d}"]
fileprivate let show_sendData:[Character] = ["务","器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let k_viewMessage:String = "SDK return make pop block"
fileprivate let dataListFormat:[Character] = ["连","接","到","\u{670d}","务","器"]

/*: "IM连接服务器失败，code: :*/
fileprivate let const_labName:String = "IM连\u{63a5}服务器失败"
fileprivate let showNameFormat:[Character] = ["\u{ff0c}"]
fileprivate let appColorName:String = "code:let arc track var"

/*: , error: :*/
fileprivate let constSucceedId:String = "table equal accept import, err"
fileprivate let userLiveTargetName:[Character] = ["o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let dataAngleRemoveTitle:[Character] = ["I","M","\u{8d26}","号","被","\u{6324}","下"]
fileprivate let user_attentionTitle:String = "\u{7ebf}。"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let const_pointModeEqualMessage:[UInt8] = []

private func nameDetail(item num: UInt8) -> UInt8 {
    return num ^ 231
}

/*: "Offline Notification" :*/
fileprivate let user_reasonUrl:String = "status mode calledOffline"
fileprivate let userManagerStr:String = "data target image let make Noti"

/*: "Cancel" :*/
fileprivate let userIndexFormat:String = "up unknown unknown selectCancel"

/*: "Re Login" :*/
fileprivate let appSucceedPath:[Character] = ["R","e"," ","L","o","g","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ModelDataV2Listener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol ActionObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func beyondPop(data: [TableReactiveCompatible])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func pointOfReference(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func methodFromMsg(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum ParentDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class ModelDataV2Listener: NSObject {
@objcMembers public class ModelDataV2Listener: NSObject {
    //: public static let shared = ModelDataV2Listener()
    public static let shared = ModelDataV2Listener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [TableReactiveCompatible] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [TableReactiveCompatible] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [TableReactiveCompatible] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [TableReactiveCompatible] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [TableReactiveCompatible] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: TableReactiveCompatible]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.hollerithCard()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func arrayInit() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(show_barStr, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func hollerithCard() {
        //: func__removeAllDelegate()
        premiumDetail()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.adddofficialWill()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func adddofficialWill() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = TableReactiveCompatible.declaration(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = TableReactiveCompatible.declaration(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func retention() -> [TableReactiveCompatible] {
        // 审核模式，在消息列表移除官方客服
        //: if PunctuateThen.share.appStatus == AppSkinStatus.special.rawValue {
        if PunctuateThen.share.appStatus == HiddenHashableRepresentation.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == PublisherReactiveCompatible.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == PublisherReactiveCompatible.appId() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension ModelDataV2Listener {
extension ModelDataV2Listener {
    //: func func__removeAllDelegate() {
    func premiumDetail() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func my(_ delegate: ActionObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func delegatePush(_ delegate: ActionObjectProtocol) {
        //: self.func__invokeDelegate {
        self.doShow {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func doShow(_ invocation: (ActionObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! ActionObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension ModelDataV2Listener {
extension ModelDataV2Listener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func liveAcrossEqual() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
        }
        //: self.initialization()
        self.hollerithCard()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func colorCompletion(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = PunctuateThen.share.appUserConfigMode.uSig
        let uSigStr = PunctuateThen.share.appUserConfigMode.uSig
        //: let uidStr = PunctuateThen.share.loginUserMode.userID
        let uidStr = PunctuateThen.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.worldView()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            GestureLogTool.playChangeImage(msg: (String(showDateTitle)) + "\(code)" + (const_viewText.capitalized + String(data_downUrl.prefix(5))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func preserve() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || PunctuateThen.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || PunctuateThen.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.drawObserver(showMsg: show_limitMessage)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension ModelDataV2Listener {
extension ModelDataV2Listener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func worldView() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func shadowgraph() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: SomebodyRequestTool.req_sysMessageList { _, result in
        SomebodyRequestTool.skinPath { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(appFrameUrl.replacingOccurrences(of: "touch", with: "cr") + String(kArrayUrl.suffix(6)))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: data_barId, object: self, userInfo: [String(bytes: main_removeUrl.map{voiceCellSize(result: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func circleOfCurvature(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: SomebodyRequestTool.req_getChatRoomInfo { succeed, result, _ in
        SomebodyRequestTool.infoPlay { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(showBeginFormat.replacingOccurrences(of: "frame", with: "p"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(data_tableTapLabelUrl) + appViewPath.replacingOccurrences(of: "count", with: "m"))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = TableReactiveCompatible.declaration(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(data_leadingStr))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(user_messagePath.suffix(5)) + "neRo" + String(noti_pointName))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension ModelDataV2Listener: V2TIMAdvancedMsgListener {
extension ModelDataV2Listener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(app_makeTitle.prefix(6)) + notiTextTagKey.replacingOccurrences(of: "object", with: "T"))) || msg.groupID.contains((String(main_barStr.prefix(6)) + "CHAT"))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            PhotoDanmuManager.messageUser().stopSub(msg: msg)
            //: return
            return
        }
        //: if msg.sender == PunctuateThen.share.loginUserMode.userID { return }
        if msg.sender == PunctuateThen.share.loginUserMode.userID { return }

        //: let dic = MemoryChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = MemoryChatManager.parse(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(main_toUrl.lowercased())] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? SharedModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: user_domainData, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(data_fromTagFormat.replacingOccurrences(of: "user", with: "ms") + String(k_sourceKey))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(main_toData) + String(mainErrorPath.prefix(4))) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: dataTurnContent, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if MemoryChatManager.checkVoiceMessage(message: msg) {
            if MemoryChatManager.draft(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = ModelThen.runCell(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                QueryQuantityoMsgTable.videoAdd(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.jumpEnd(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            PhotoThen.shared.manner(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            PhotoThen.shared.awardDoingInput(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(dataEqualTitle) + String(showOfSucceedMsg.prefix(7))) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            LayerChatManager.share.cohere(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: MoveReactiveCompatible.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: MoveReactiveCompatible.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != PublisherReactiveCompatible.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != PublisherReactiveCompatible.exceptXiao() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.doShow { $0.methodFromMsg?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            MemoryCustomReactiveCompatible.shared.failureTool(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: userNameMainValue.map{indexImage(title: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: constContentBucketData.reversed(), encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: noti_blockText.reversed(), encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: appNetPath,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            ReadoutReactiveCompatible.viewTime(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            MoveReactiveCompatible.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: notiBeautyInfoMsg, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: EqualCompartmentReactiveCompatible, isPrivete: Bool) {
    @objc public func underNamePrivete(targetId: String, msgCellData: EqualCompartmentReactiveCompatible, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.withPrivete(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(showFrameStatusMsg)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.faq(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.beyondFill(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func withPrivete(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = PunctuateThen.share.loginUserMode.userID
        let senderStr = PunctuateThen.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func jumpEnd(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(constBackgroundValue.replacingOccurrences(of: "count", with: "ch") + "Dire" + kNowData.lowercased())].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && RangePushManager.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(noti_maleId) + appStopMsg.lowercased())].stringValue == (String(k_contentInnerKey) + String(userViewStr)) && isSelf && RangePushManager.share.retrieve()!.isKind(of: GiftChatController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: show_buttonText) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: show_buttonText)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension ModelDataV2Listener: V2TIMConversationListener {
extension ModelDataV2Listener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.faq(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.beyondFill(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.faq(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.beyondFill(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: PunctuateThen.share.unreadMessageNum = Int(totalUnreadCount)
        PunctuateThen.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.doShow {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.pointOfReference?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func beyondFill(conVersationList: [TableReactiveCompatible]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if ReadoutReactiveCompatible.cellOpen().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                ReadoutReactiveCompatible.cellOpen().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.doShow {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.beyondPop?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func renderOf(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [TableReactiveCompatible] = []

        // V2TIMConversation->TableReactiveCompatible
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = TableReactiveCompatible(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.command(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [TableReactiveCompatible] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = GestureInfoTable.createBy(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.subTop(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension ModelDataV2Listener {
extension ModelDataV2Listener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func dismissClick(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.renderOf(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.beyondFill(conVersationList: arr as? [TableReactiveCompatible] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func petText(targetId: String) -> TableReactiveCompatible? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func faq(convList: [V2TIMConversation]) -> [TableReactiveCompatible] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [TableReactiveCompatible] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = TableReactiveCompatible(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.command(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.subTop(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func subTop(convModel: TableReactiveCompatible, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.adddofficialWill()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: ParentDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - PunctuateThen.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - PunctuateThen.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = ReadoutReactiveCompatible.comparable(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = MemoryChatManager.parseTXMessageData(data: elemData)
            let dict = MemoryChatManager.parse(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(main_toUrl.lowercased())][(noti_inputFormat.replacingOccurrences(of: "video", with: "m") + String(const_haveViewTitle.suffix(6)))][(String(const_succeedId))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.titleApp(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.titleApp(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func titleApp(convList: [TableReactiveCompatible], dataType: ParentDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func downrightType(targetId: String, dataType: ParentDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(showFrameStatusMsg)) + "\(targetId)" : (constKeyJumpButtonPath.lowercased() + kFrameText.capitalized) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            ReadoutReactiveCompatible.replacement(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(PunctuateThen.share.loginUserMode.userID, toUid: targetId)
            QueryQuantityoMsgTable.replyBlock(PunctuateThen.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.worldView()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func outView(convModel: TableReactiveCompatible, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.titleApp(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.titleApp(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension ModelDataV2Listener: V2TIMSDKListener {
extension ModelDataV2Listener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(show_hideUrl) + String(show_sendData)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(k_viewMessage.prefix(4)) + "已经成功" + String(dataListFormat)))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        GestureLogTool.playChangeImage(msg: (const_labName + String(showNameFormat) + String(appColorName.prefix(5))) + "\(code)" + (String(constSucceedId.suffix(5)) + String(userLiveTargetName)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        GestureLogTool.playChangeImage(msg: (String(dataAngleRemoveTitle) + user_attentionTitle))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        WrittenRecordSocketDelegate.shared.forwardingOut(userLoginOut: true)
        //: ModelDataV2Listener.shared.isConnection = false
        ModelDataV2Listener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (QueryEnableReactiveCompatible.shared as! QueryEnableReactiveCompatible).plugIn()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: const_pointModeEqualMessage.map{nameDetail(item: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = CurtilageAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if LayerAddrTool.share.interfaceLang == TrademarkVoiceMixedBagTextConvertible.ar.rawValue || LayerAddrTool.share.interfaceLang == TrademarkVoiceMixedBagTextConvertible.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            LayerAddrTool.share.interfaceLang == TrademarkVoiceMixedBagTextConvertible.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.atomic(fontSize: 13)
            config.rightFont = UIFont.atomic(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        SizeReactiveCompatible.exceptHeader()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        SizeReactiveCompatible.exceptHeader(title: (String(user_reasonUrl.suffix(7)) + String(userManagerStr.suffix(5)) + "fication").localized, message: msg, leftBtnTitle: (String(userIndexFormat.suffix(6))).localized, rightBtnTitle: (String(appSucceedPath)).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            LogLiveManager.anUp().whoIn()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            RangeManager.universalShared().proposal()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            ComponentRequestTool.model { t in
                //: ProgressHUD.dismiss()
                DirectThen.cutWith()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: k_deviceKey, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            ComponentRequestTool.make { t in
                //: if t {
                if t {
                    //: ModelDataV2Listener.shared.func__LogingIn { _ in
                    ModelDataV2Listener.shared.colorCompletion { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if LogLiveManager.anUp().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(LogLiveManager.anUp().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    WrittenRecordSocketDelegate.shared.rawTo()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
