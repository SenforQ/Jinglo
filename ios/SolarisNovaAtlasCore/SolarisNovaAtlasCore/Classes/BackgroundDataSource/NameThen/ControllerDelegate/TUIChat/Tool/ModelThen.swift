
//: Declare String Begin

/*: "msgType" :*/
fileprivate let dataGreetName:[Character] = ["m","s","g","T","y"]
fileprivate let mainLargePath:String = "of"

/*: "tips" :*/
fileprivate let userTableTitle:String = "tipview"

/*: "extra" :*/
fileprivate let dataTimeImageRawMessage:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let mainModelUrl:[Character] = ["m","s","g","I","n","f"]
fileprivate let main_toInfoPath:String = "library"

/*: "user" :*/
fileprivate let main_effectEqualMsg:String = "USER"

/*: "uid" :*/
fileprivate let noti_emptyId:String = "arrayid"

/*: "nickname" :*/
fileprivate let constAddErrorFormat:[UInt8] = [0x65,0x6d,0x61,0x6e,0x6b,0x63,0x69,0x6e]

/*: "headPic" :*/
fileprivate let data_contactMessage:[UInt8] = [0x93,0x90,0x8c,0x8f,0x7b,0x94,0x8e]

fileprivate func constituentModel(count num: UInt8) -> UInt8 {
    let value = Int(num) - 43
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "age" :*/
fileprivate let user_modeLevelUrl:[UInt8] = [0x89,0x8f,0x8d]

/*: "gift" :*/
fileprivate let data_colorValue:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let appTopStr:String = "ievent"
fileprivate let dataWillParticlePath:String = "let field key cellGroup"

/*: "WantGift" :*/
fileprivate let userMakePath:String = "observer targetWant"

/*: "txt" :*/
fileprivate let kImageTitle:String = "intimatet"

/*: "img" :*/
fileprivate let data_leadingContent:String = "errorg"

/*: "video" :*/
fileprivate let mainTableValue:[Character] = ["v","i","d","e","o"]

/*: "audio" :*/
fileprivate let constShareData:String = "audilabel"

/*: "messageType" :*/
fileprivate let userEqualStr:String = "mmovess"
fileprivate let show_userId:[Character] = ["p","e"]

/*: "contentType" :*/
fileprivate let kFileSizeStr:[Character] = ["c","o","n","t","e","n","t","T"]
fileprivate let appViewMsg:String = "ytempe"

/*: "ImgMsg" :*/
fileprivate let noti_thoughtMsg:[Character] = ["I","m","g","M","s","g"]

/*: "imageUri" :*/
fileprivate let appAtUrl:String = "size"
fileprivate let const_byContent:[Character] = ["a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let constValueFormat:[Character] = ["t","y","p","e"]

/*: "pic" :*/
fileprivate let app_managerContent:String = "pcolorc"

/*: "isBurn" :*/
fileprivate let data_userUrl:[Character] = ["i","s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let userContentNeedName:String = "text pathgiftI"
fileprivate let userActionUrl:String = "background"

/*: "params" :*/
fileprivate let user_managerStr:String = "paracancel"

/*: "audioLength" :*/
fileprivate let mainModelTitle:String = "edge"
fileprivate let notiSucceedJumpUrl:String = "label load videoudioL"

/*: "len" :*/
fileprivate let show_clickMsg:String = "letab"

/*: "audioSandbox" :*/
fileprivate let kLargeIndexName:[Character] = ["a","u","d","i","o","S","a","n"]
fileprivate let kShadowName:[Character] = ["d","b","o","x"]

/*: "senderId" :*/
fileprivate let appLeadingButtonTitle:String = "SENDE"
fileprivate let app_changeMsg:[Character] = ["r","I","d"]

/*: "AudioMsg" :*/
fileprivate let data_countPath:[Character] = ["A","u","d","i","o"]
fileprivate let showButtonKey:[Character] = ["M","s","g"]

/*: "content" :*/
fileprivate let mainTaskText:String = "conlargent"

/*: "TxtMsg" :*/
fileprivate let noti_viewPath:String = "view right height if frameTxtMsg"

/*: "message" :*/
fileprivate let showGiftFormat:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let showDateTitle:[Character] = ["q","u","o","t","e","M","s","g"]
fileprivate let kNeverKey:String = "Idstuff self"

/*: "quoteMsgInfo" :*/
fileprivate let user_havePath:String = "quoteMsgnor show manager top show"
fileprivate let const_colorTitle:String = "cell listInfo"

/*: "rYMsgType" :*/
fileprivate let show_containerMainData:String = "rYMsglet in view button"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let k_giftTitle:String = "now object to distance letGJ:S"
fileprivate let k_tableCenterId:String = "AutoGapp model with"
fileprivate let show_viewMsg:[Character] = ["x","t","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let mainBottomVideoTitle:[Character] = ["c","h","a","n","g","e","D","i","r","e"]
fileprivate let user_welcomeContent:String = "ctioresult"

/*: "system" :*/
fileprivate let constButtonId:String = "mainstem"

/*: "title" :*/
fileprivate let show_centerDetailNameTitle:[Character] = ["t","i","t","l","e"]

/*: "id" :*/
fileprivate let main_giftKey:[Character] = ["i","d"]

/*: "icon" :*/
fileprivate let kSucceedDataStr:String = "apartcon"

/*: "all" :*/
fileprivate let noti_currentData:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let user_toStr:String = "iccenter"
fileprivate let show_imageRawId:[Character] = ["t","s","_","p","c","r"]

/*: "status" :*/
fileprivate let appMediumStr:String = "key"
fileprivate let data_pointValue:[Character] = ["t","a","t","u","s"]

/*: "msgIncome" :*/
fileprivate let notiTimeCustomData:String = "M"
fileprivate let userDeviceTitle:[Character] = ["s","g","I","n","c","o","m","e"]

/*: "msgId" :*/
fileprivate let app_backgroundMessage:[Character] = ["m","s","g","I","d"]

/*: "StreetSmartInsetTarget" :*/
fileprivate let data_cornerModelMessage:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e","S","t","a","t","u","s"]

/*: "TMsgTranslatedContent" :*/
fileprivate let userEqualSavePath:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e"]
fileprivate let noti_showHourTitleMsg:String = "model imagedContent"

/*: "formatType" :*/
fileprivate let mainSourceKey:String = "forlab"

/*: "plain" :*/
fileprivate let user_fromPutMessage:String = "beanlain"

/*: "imageData" :*/
fileprivate let show_errorId:String = "imcolorg"
fileprivate let dataWhiteKey:String = "count language remove at extensioneData"

/*: "VideoMsg" :*/
fileprivate let main_userMsg:String = "VideoMsgto false color"

/*: "match" :*/
fileprivate let user_labelData:String = "rowtch"

/*: "mf" :*/
fileprivate let show_clickId:[Character] = ["m","f"]

/*: "userIntro" :*/
fileprivate let kLayerValue:String = "cell true varuserInt"
fileprivate let dataEqualKey:[Character] = ["r","o"]

/*: "targetId" :*/
fileprivate let user_giftMsg:[Character] = ["t","a","r","g","e","t","I","d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let dataCancelStr:String = "titletitletitletitle"
fileprivate let app_topName:String = "dd Hcell load extension content true"
fileprivate let userVoiceValue:[Character] = ["s"]

/*: "sendTime" :*/
fileprivate let user_modeData:String = "SEND"

/*: "isRead" :*/
fileprivate let user_orientationKey:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let appEqualMsg:[Character] = ["a","u","d","i","o","D","a","t"]
fileprivate let app_lengthFormat:[Character] = ["a"]

/*: "audioUri" :*/
fileprivate let appMakeData:String = "else size live headaudioUri"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ModelThen.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum MessageEquatable: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - ModelThen

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class ModelThen: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func aboveAdjust() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> EqualCompartmentReactiveCompatible? {
    public class func plusNeed(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> EqualCompartmentReactiveCompatible? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = objectList(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: ModelDataV2Listener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            ModelDataV2Listener.shared.underNamePrivete(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> EqualCompartmentReactiveCompatible? {
    public class func objectList(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> EqualCompartmentReactiveCompatible? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] = (userTableTitle.replacingOccurrences(of: "view", with: "s"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(dataTimeImageRawMessage)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = build(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SizeThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = SharedModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? TalkingMakeModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(mainModelUrl) + main_toInfoPath.replacingOccurrences(of: "library", with: "o"))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(main_effectEqualMsg.lowercased())] = [
            //: "uid": PunctuateThen.share.loginUserMode.userID,
            (noti_emptyId.replacingOccurrences(of: "array", with: "u")): PunctuateThen.share.loginUserMode.userID,
            //: "nickname": PunctuateThen.share.loginUserMode.nickname,
            String(bytes: constAddErrorFormat.reversed(), encoding: .utf8)!: PunctuateThen.share.loginUserMode.nickname,
            //: "headPic": PunctuateThen.share.loginUserMode.headPic,
            String(bytes: data_contactMessage.map{constituentModel(count: $0)}, encoding: .utf8)!: PunctuateThen.share.loginUserMode.headPic,
            //: "age": PunctuateThen.share.loginUserMode.age,
            String(bytes: user_modeLevelUrl.map{$0^232}, encoding: .utf8)!: PunctuateThen.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(dataTimeImageRawMessage)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = build(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (String(data_colorValue)) {
            //: var data: EqualCompartmentReactiveCompatible
            var data: EqualCompartmentReactiveCompatible
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(appTopStr.replacingOccurrences(of: "event", with: "s") + String(dataWillParticlePath.suffix(5)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ComponentThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ViewCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ViewCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SharedModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (String(userMakePath.suffix(4)) + "Gift") {
            //: let data = DataFileReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DataFileReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SharedModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (kImageTitle.replacingOccurrences(of: "intimate", with: "tx")) {
            //: var data: EqualCompartmentReactiveCompatible
            var data: EqualCompartmentReactiveCompatible
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(appTopStr.replacingOccurrences(of: "event", with: "s") + String(dataWillParticlePath.suffix(5)))] as? String == "1" { // 群聊
                //: data = ClickCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ClickCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = LoadReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = LoadReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SharedModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (data_leadingContent.replacingOccurrences(of: "error", with: "im")) {
            //: let data = ChatThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ChatThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SharedModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (String(mainTableValue)) {
            //: let data = DirectCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DirectCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SharedModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (constShareData.replacingOccurrences(of: "label", with: "o")) {
            //: let data = SizeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SizeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SharedModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(data_colorValue))] != nil && (msgInfo[(userEqualStr.replacingOccurrences(of: "move", with: "e") + "ageTy" + String(show_userId))] as? Int) == 4 {
            //: let data = LoadReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = LoadReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SharedModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: EqualCompartmentReactiveCompatible) -> EqualCompartmentReactiveCompatible? {
    public class func atTo(withData cellData: EqualCompartmentReactiveCompatible) -> EqualCompartmentReactiveCompatible? {
        // 图片
        //: if let reCellData = cellData as? ChatThen {
        if let reCellData = cellData as? ChatThen {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(PublisherReactiveCompatible.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(PublisherReactiveCompatible.visualisation())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(kFileSizeStr) + appViewMsg.replacingOccurrences(of: "temp", with: "p"))] = (String(noti_thoughtMsg))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] = (data_leadingContent.replacingOccurrences(of: "error", with: "im"))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(appAtUrl.replacingOccurrences(of: "size", with: "im") + String(const_byContent))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(mainModelUrl) + main_toInfoPath.replacingOccurrences(of: "library", with: "o"))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(String(constValueFormat))] = (String(noti_thoughtMsg))
            //: params["pic"] = filePath
            params[(app_managerContent.replacingOccurrences(of: "color", with: "i"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(data_userUrl))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(data_userUrl))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(userContentNeedName.suffix(5)) + userActionUrl.replacingOccurrences(of: "background", with: "d"))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(userContentNeedName.suffix(5)) + userActionUrl.replacingOccurrences(of: "background", with: "d"))] = "0"
            }

            //: dic["params"] = params
            dic[(user_managerStr.replacingOccurrences(of: "cancel", with: "ms"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = objectList(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? DirectCellData {
        if let reCellData = cellData as? DirectCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? SizeCellData {
        if let reCellData = cellData as? SizeCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = ReadoutReactiveCompatible.enableUp(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(mainModelTitle.replacingOccurrences(of: "edge", with: "a") + String(notiSucceedJumpUrl.suffix(5)) + "ength")] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(show_clickMsg.replacingOccurrences(of: "tab", with: "n"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(kLargeIndexName) + String(kShadowName))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(kLargeIndexName) + String(kShadowName))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] = (constShareData.replacingOccurrences(of: "label", with: "o"))
            //: msgInfo["senderId"] = PunctuateThen.share.loginUserMode.userID
            msgInfo[(appLeadingButtonTitle.lowercased() + String(app_changeMsg))] = PunctuateThen.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(mainModelUrl) + main_toInfoPath.replacingOccurrences(of: "library", with: "o"))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(String(constValueFormat))] = (String(data_countPath) + String(showButtonKey))
            //: dic["params"] = params
            dic[(user_managerStr.replacingOccurrences(of: "cancel", with: "ms"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = objectList(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? LoadReactiveCompatible {
        if let reCellData = cellData as? LoadReactiveCompatible {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(mainTaskText.replacingOccurrences(of: "large", with: "te"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] = (kImageTitle.replacingOccurrences(of: "intimate", with: "tx"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(mainModelUrl) + main_toInfoPath.replacingOccurrences(of: "library", with: "o"))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(String(constValueFormat))] = (String(noti_viewPath.suffix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(showGiftFormat))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.size() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(showDateTitle) + String(kNeverKey.prefix(2)))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(user_havePath.prefix(8)) + String(const_colorTitle.suffix(4)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(user_managerStr.replacingOccurrences(of: "cancel", with: "ms"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = objectList(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> EqualCompartmentReactiveCompatible? {
    public class func atGift(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> EqualCompartmentReactiveCompatible? {
        //: var cellData: EqualCompartmentReactiveCompatible?
        var cellData: EqualCompartmentReactiveCompatible?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(dataTimeImageRawMessage))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(mainModelUrl) + main_toInfoPath.replacingOccurrences(of: "library", with: "o"))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(main_effectEqualMsg.lowercased())] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? SharedModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == PunctuateThen.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == PunctuateThen.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == PunctuateThen.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == PunctuateThen.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(show_containerMainData.prefix(5)) + "Type")] as? String == (String(k_giftTitle.suffix(4)) + "ess:" + String(k_tableCenterId.prefix(5)) + "reet:T" + String(show_viewMsg)) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(mainBottomVideoTitle) + user_welcomeContent.replacingOccurrences(of: "result", with: "n"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = startBeauty(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (constButtonId.replacingOccurrences(of: "main", with: "sy")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = SharedModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? TalkingMakeModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SizeThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(mainTaskText.replacingOccurrences(of: "large", with: "te"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = ChatThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ChatThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = DirectCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DirectCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ComponentThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = ViewCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ViewCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = takeHome(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(dataTimeImageRawMessage))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(mainModelUrl) + main_toInfoPath.replacingOccurrences(of: "library", with: "o"))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? UpModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = DataFileReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DataFileReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = SizeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SizeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? SizeCellData {
            if let audioCellData = cellData as? SizeCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = self.voiceModel(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = QueryQuantityoMsgTable.applicationClick(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = QueryQuantityoMsgTable.videoAdd(runCell(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = NameCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(String(show_centerDetailNameTitle))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(mainTaskText.replacingOccurrences(of: "large", with: "te"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(data_leadingContent.replacingOccurrences(of: "error", with: "im"))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = RitualCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = ClickCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ClickCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = LoadReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = LoadReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? ContentModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(kSucceedDataStr.replacingOccurrences(of: "apart", with: "i"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: PunctuateThen.share.loginUserMode.headPic ?? ""
                        PunctuateThen.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = UpModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(mainTaskText.replacingOccurrences(of: "large", with: "te"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(dataTimeImageRawMessage))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = ViewModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(noti_currentData))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (user_toStr.replacingOccurrences(of: "center", with: "on") + "_cha" + String(show_imageRawId))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.requestContent(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(appMediumStr.replacingOccurrences(of: "key", with: "s") + String(data_pointValue))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(notiTimeCustomData.lowercased() + String(userDeviceTitle))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(userEqualStr.replacingOccurrences(of: "move", with: "e") + "ageTy" + String(show_userId))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(app_backgroundMessage))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(data_cornerModelMessage))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = StreetSmartInsetTarget(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(userEqualSavePath) + String(noti_showHourTitleMsg.suffix(8)))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = PhotoModel.atDict()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = PhotoModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = PublishNameModel.file()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = PublishNameModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = BridePriceModel.screenSearch()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func startBeauty(_ extraDic: [String: Any]) -> MessageEquatable {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(dataTimeImageRawMessage))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(mainModelUrl) + main_toInfoPath.replacingOccurrences(of: "library", with: "o"))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? SharedModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(mainSourceKey.replacingOccurrences(of: "lab", with: "m") + "atType")] as? String == (user_fromPutMessage.replacingOccurrences(of: "bean", with: "p")) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(appAtUrl.replacingOccurrences(of: "size", with: "im") + String(const_byContent))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(show_errorId.replacingOccurrences(of: "color", with: "a") + String(dataWhiteKey.suffix(5)))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(main_userMsg.prefix(8))) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = verticalOn(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(user_labelData.replacingOccurrences(of: "row", with: "ma"))] as? Int) == 1 || (msgInfo?[(user_labelData.replacingOccurrences(of: "row", with: "ma"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (userTableTitle.replacingOccurrences(of: "view", with: "s")) || (dic?[(userEqualStr.replacingOccurrences(of: "move", with: "e") + "ageTy" + String(show_userId))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(mainSourceKey.replacingOccurrences(of: "lab", with: "m") + "atType")] as? String == "mf") && ((dic?[(userEqualStr.replacingOccurrences(of: "move", with: "e") + "ageTy" + String(show_userId))] as? Int) == 15 || (dic?[(userEqualStr.replacingOccurrences(of: "move", with: "e") + "ageTy" + String(show_userId))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (String(kLayerValue.suffix(7)) + String(dataEqualKey)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String == (String(userMakePath.suffix(4)) + "Gift") || (msgInfo?[(userEqualStr.replacingOccurrences(of: "move", with: "e") + "ageTy" + String(show_userId))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func runCell(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.requestContent(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(dataTimeImageRawMessage))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(mainModelUrl) + main_toInfoPath.replacingOccurrences(of: "library", with: "o"))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(main_effectEqualMsg.lowercased())] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(appLeadingButtonTitle.lowercased() + String(app_changeMsg))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == PunctuateThen.share.loginUserMode.userID {
        if senderId == PunctuateThen.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(appLeadingButtonTitle.lowercased() + String(app_changeMsg))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(user_giftMsg))] = message.userID ?? (user?[(String(user_giftMsg))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(appLeadingButtonTitle.lowercased() + String(app_changeMsg))] = "\(msgInfo?[(noti_emptyId.replacingOccurrences(of: "array", with: "u"))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(user_giftMsg))] = "\(msgInfo?[(noti_emptyId.replacingOccurrences(of: "array", with: "u"))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(app_backgroundMessage))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(app_backgroundMessage))] = "\(msgInfo?[(String(app_backgroundMessage))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.requestContent(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(app_backgroundMessage))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(app_backgroundMessage))] = "\(customDic[(String(app_backgroundMessage))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(app_backgroundMessage))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(kLargeIndexName) + String(kShadowName))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(kLargeIndexName) + String(kShadowName))] = "\(msgInfo?[(String(kLargeIndexName) + String(kShadowName))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((mainModelTitle.replacingOccurrences(of: "edge", with: "a") + String(notiSucceedJumpUrl.suffix(5)) + "ength")) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(mainModelTitle.replacingOccurrences(of: "edge", with: "a") + String(notiSucceedJumpUrl.suffix(5)) + "ength")] = "\(msgInfo?[(mainModelTitle.replacingOccurrences(of: "edge", with: "a") + String(notiSucceedJumpUrl.suffix(5)) + "ength")] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (dataCancelStr.replacingOccurrences(of: "title", with: "y") + "-MM-" + String(app_topName.prefix(4)) + "H:mm:s" + String(userVoiceValue))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(user_modeData.lowercased() + "Time")] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == PunctuateThen.share.loginUserMode.userID ? "1" : "0"
        info?[(String(user_orientationKey))] = senderId == PunctuateThen.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func voiceModel(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = runCell(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(app_backgroundMessage))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func takeHome(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func build(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func verticalOn(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(dataGreetName) + mainLargePath.replacingOccurrences(of: "of", with: "pe"))] as? String, msgType == (constShareData.replacingOccurrences(of: "label", with: "o")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(kFileSizeStr) + appViewMsg.replacingOccurrences(of: "temp", with: "p"))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(data_countPath) + String(showButtonKey))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(appEqualMsg) + String(app_lengthFormat))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(appMakeData.suffix(8)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
