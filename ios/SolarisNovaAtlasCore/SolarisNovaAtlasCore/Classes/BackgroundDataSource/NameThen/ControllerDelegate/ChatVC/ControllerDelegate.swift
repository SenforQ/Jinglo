
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_colorEqualFormat:[UInt8] = [0xea,0xef,0xea,0xf5,0xa9,0xe4,0xf0,0xe5,0xe6,0xf3,0xbb,0xaa,0xa1,0xe9,0xe2,0xf4,0xa1,0xef,0xf0,0xf5,0xa1,0xe3,0xe6,0xe6,0xef,0xa1,0xea,0xee,0xf1,0xed,0xe6,0xee,0xe6,0xef,0xf5,0xe6,0xe5]

fileprivate func physicsLaboratory(image num: UInt8) -> UInt8 {
    let value = Int(num) - 129
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "roomId" :*/
fileprivate let app_modelFormat:String = "roomIdmake manager frame"

/*: "type" :*/
fileprivate let user_containerStr:[UInt8] = [0x84,0x89,0x80,0x75]

fileprivate func upView(value num: UInt8) -> UInt8 {
    let value = Int(num) - 16
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "extra" :*/
fileprivate let noti_actionMsg:String = "etexttra"

/*: "jumpKey" :*/
fileprivate let dataAdjustFormat:[UInt8] = [0x79,0x65,0x4b,0x70,0x6d,0x75,0x6a]

/*: "Send Gift" :*/
fileprivate let show_insideAppData:String = "let view plusSend"

/*: "View info" :*/
fileprivate let showTotaleractionKey:[Character] = ["V","i","e","w"," ","i","n","f"]
fileprivate let main_upImageTitle:String = "point"

/*: "Report" :*/
fileprivate let showPlayerValue:[Character] = ["R","e","p","o","r","t"]

/*: "uid" :*/
fileprivate let kSelectMakeValue:[UInt8] = [0xf5,0xe9,0xe4]

/*: "name" :*/
fileprivate let kShapeUrl:[UInt8] = [0x24,0x2b,0x27,0x2f]

private func listInterval(feature num: UInt8) -> UInt8 {
    return num ^ 74
}

/*: "Can't send blank message" :*/
fileprivate let user_toTitle:String = "Can\'t"
fileprivate let main_equalValue:[Character] = [" ","s","e","n","d"," ","b"]
fileprivate let const_rootMakeStr:[Character] = ["l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let k_fileUserTopData:[Character] = ["u","s","e","r"]

/*: "content" :*/
fileprivate let userPairSegmentUrl:[UInt8] = [0x74,0x78,0x79,0x63,0x72,0x79,0x63]

private func toRaw(text num: UInt8) -> UInt8 {
    return num ^ 23
}

/*: "msgType" :*/
fileprivate let const_applicationId:String = "msgTypein image"

/*: "txt" :*/
fileprivate let user_contentSourceMsg:String = "TXT"

/*: "isGroup" :*/
fileprivate let noti_viewPath:String = "isGroupcolor block string touch"

/*: "jumps" :*/
fileprivate let data_sizeEditPath:String = "jumodeps"

/*: "message" :*/
fileprivate let constToolStr:[UInt8] = [0xb4,0xbc,0xaa,0xaa,0xb8,0xbe,0xbc]

private func rudeWindow(hidden num: UInt8) -> UInt8 {
    return num ^ 217
}

/*: "TxtMsg" :*/
fileprivate let notiTextId:[Character] = ["T","x","t","M","s"]
fileprivate let constRequestHungUrl:String = "image"

/*: "toUid" :*/
fileprivate let notiObjectMagnitudeKey:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let kHeadKey:String = "msgInfoclean color frame fatal"

/*: "params" :*/
fileprivate let const_leadMsg:[UInt8] = [0x9f,0x8e,0x9d,0x8e,0x82,0x9c]

private func dataBarStyle(edge num: UInt8) -> UInt8 {
    return num ^ 239
}

/*: "contentType" :*/
fileprivate let data_modelQuoteUrl:[UInt8] = [0x44,0x48,0x49,0x53,0x42,0x49,0x53,0x73,0x5e,0x57,0x42]

/*: "ImgMsg" :*/
fileprivate let main_sharedFormat:String = "return label height to inImgMsg"

/*: "img" :*/
fileprivate let kAppText:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let main_playerPathTitle:[UInt8] = [0xd3,0xd7,0xdb,0xdd,0xdf,0xef,0xc8,0xd3]

/*: "pic" :*/
fileprivate let const_imageEqualMessage:String = "labelc"

/*: "msgId" :*/
fileprivate let dataFlushMessage:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let const_makeViewName:String = "path lab frame managertotalM"

/*: "giftId" :*/
fileprivate let show_resultCalledPath:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let main_playTitle:[Character] = ["g"]
fileprivate let app_managerData:String = "iftNumvalue bull request drop icon"

/*: "pkgItemsetId" :*/
fileprivate let appFemaleText:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "msg" :*/
fileprivate let data_viewMessage:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let constSaveContent:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let appMakeTitle:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let user_timeWithId:String = "empty"
fileprivate let appRowValue:[Character] = ["i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let showTimeTitle:[UInt8] = [0xde,0xd3,0xd7,0xd2,0xe6,0xdf,0xd5]

private func kitMake(leading num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "All Numbers" :*/
fileprivate let mainResultData:String = "to enable var selfAll N"
fileprivate let showImageName:String = "uadders"

/*: "sendNum" :*/
fileprivate let k_centerStr:[Character] = ["s"]
fileprivate let k_frameName:String = "endNumheight layer if for extension"

/*: "toUser" :*/
fileprivate let noti_addKey:String = "equal selftoUser"

/*: "msgIncome" :*/
fileprivate let main_sourceId:String = "narrow lab import return countmsgI"
fileprivate let constTimeMessage:String = "heart"

/*: "isConnection" :*/
fileprivate let dataModeUrl:String = "isConnstatus any"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ControllerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class ControllerDelegate: SizeViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = RicePaddyTransformable() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = TableReactiveCompatible() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = RicePaddyTransformable.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_colorEqualFormat.map{physicsLaboratory(image: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PunctuateThen.share.appConfigMode.disableShootScreen {
            if PunctuateThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = StartView(frame: CGRect(x: 0, y: 0, width: kQueryValue, height: notiKeyTitle))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        tiptoe()
        //: createUIConstraints()
        itemUp()
        //: bindInteraction()
        totalernationality()
        //: addTapGestureRecognizer()
        acceptAdd()
        //: func__chatRoom_insertTipMsg()
        pickApart()
        //: chatSettings()
        enableLoadWill()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            tillErasePrevious()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.someEqual(join: false)
        //: self.destroyTipsTimer()
        self.rank()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: MakeTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = MakeTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(RequestMsgCell.self, forCellReuseIdentifier: RequestMsgCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ActionReactiveCompatible.self, forCellReuseIdentifier: ActionReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(MemoryViewCell.self, forCellReuseIdentifier: MemoryViewCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(TelecastingViewCell.self, forCellReuseIdentifier: TelecastingViewCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: ToolView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(MessageViewCell.self, forCellReuseIdentifier: ToolView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: StateViewDelegate = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = StateViewDelegate(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: ChatReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ChatReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: GiftImageThen = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = GiftImageThen()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: HeatherView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = HeatherView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: TextView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = TextView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension ControllerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func enableLoadWill() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        LayerChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        someEqual(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func someEqual(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(app_modelFormat.prefix(6))): self.conversationData.groupID, String(bytes: user_containerStr.map{upView(value: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: SomebodyRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        SomebodyRequestTool.nameCompletion(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - ToDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension ControllerDelegate: ToDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> EqualCompartmentReactiveCompatible! {
    func price(_: MakeTableViewController!, everyIndicator data: V2TIMMessage!) -> EqualCompartmentReactiveCompatible! {
        //: let dic = MemoryChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = MemoryChatManager.parse(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = ModelThen.atGift(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: EqualCompartmentReactiveCompatible!) -> ToolView! {
    func draw(_: MakeTableViewController!, local data: EqualCompartmentReactiveCompatible!) -> ToolView! {
        //: if data.isKind(of: ClickCellData.self) {
        if data.isKind(of: ClickCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = RequestMsgCell(style: .default, reuseIdentifier: RequestMsgCell.className())
            //: let cellData = data as! ClickCellData
            let cellData = data as! ClickCellData
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.possibility(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.immersion(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: ChatThen.self) {
        if data.isKind(of: ChatThen.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ActionReactiveCompatible(style: .default, reuseIdentifier: ActionReactiveCompatible.className())
            //: let cellData = data as! ChatThen
            let cellData = data as! ChatThen
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: ComponentThen.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = MemoryViewCell(style: .default, reuseIdentifier: MemoryViewCell.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! ComponentThen
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: SizeThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = TelecastingViewCell(style: .default, reuseIdentifier: TelecastingViewCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! SizeThen
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ToolView!) {
    func cipherOrigin(_: MakeTableViewController!, managerPosition cell: ToolView!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(PunctuateThen.share.loginUid) {
            if userModel.uid == Int(PunctuateThen.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                possibility(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                germanism(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ToolView!) {
    func action(_: MakeTableViewController!, latchkey cell: ToolView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ActionReactiveCompatible.self) { // 图片
            //: let imageCellData = cell.messageData as! ChatThen
            let imageCellData = cell.messageData as! ChatThen
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(constButtonMessage)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = StylePhotoModel()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = ViewMeasurable()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [StylePhotoModel] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = LayerFlowLayout(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.occurrentWith()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: RequestMsgCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! RequestMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = MemoryChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = MemoryChatManager.parse(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(noti_actionMsg.replacingOccurrences(of: "text", with: "x"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: dataAdjustFormat.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<TelephoneMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: RangePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RangePushManager.share.actionModel(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: TelecastingViewCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! TelecastingViewCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = MemoryChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = MemoryChatManager.parse(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(noti_actionMsg.replacingOccurrences(of: "text", with: "x"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: dataAdjustFormat.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<TelephoneMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: RangePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RangePushManager.share.actionModel(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ToolView!) {
    func haywire(_: MakeTableViewController!, model cell: ToolView!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        noRange(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension ControllerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func rank() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func germanism(user: ContentModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = SelectReactiveCompatible(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.fullPhaseOfTheMoon(cellTitleList: [(String(show_insideAppData.suffix(4)) + " Gift").localized, "@", (String(showTotaleractionKey) + main_upImageTitle.replacingOccurrences(of: "point", with: "o")).localized, (String(showPlayerValue)).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.overdo(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.appAge(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.possibility(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.appSubUid(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func overdo(user: ContentModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.blockChange()
        //: let model = TalkingChatRoomMemberModel()
        let model = SodalistSendMemberModel()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.moveListModel(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func appAge(user: ContentModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: kSelectMakeValue.map{$0^128}, encoding: .utf8)!: String(user.uid), String(bytes: kShapeUrl.map{listInterval(feature: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.upDict(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func possibility(uid: Int) {
        //: RangePushManager.share.func__pushToUserDetailVC(uid: String(uid))
        RangePushManager.share.clickOfUid(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func appSubUid(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = TitleView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.toColorView(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension ControllerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func sinceFirst(msgStr: String, quoteModel _: PeregrineModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            drawObserver(showMsg: (user_toTitle + String(main_equalValue) + String(const_rootMakeStr)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: dataAdjustFormat.reversed(), encoding: .utf8)!: (String(k_fileUserTopData)), String(bytes: kSelectMakeValue.map{$0^128}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: userPairSegmentUrl.map{toRaw(text: $0)}, encoding: .utf8)!: temStr, (String(const_applicationId.prefix(7))): (user_contentSourceMsg.lowercased()), (String(noti_viewPath.prefix(7))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(data_sizeEditPath.replacingOccurrences(of: "mode", with: "m"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: constToolStr.map{rudeWindow(hidden: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: user_containerStr.map{upView(value: $0)}, encoding: .utf8)!: (String(notiTextId) + constRequestHungUrl.replacingOccurrences(of: "image", with: "g")),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(app_modelFormat.prefix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(notiObjectMagnitudeKey))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kHeadKey.prefix(7))): msgInfo, String(bytes: const_leadMsg.map{dataBarStyle(edge: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            noRange(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func tarotCard(img: UIImage) {
        //: let imagePath = ChatThen.getChatImagePath(image: img)
        let imagePath = ChatThen.qibla(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: data_modelQuoteUrl.map{$0^39}, encoding: .utf8)!: (String(main_sharedFormat.suffix(6))), (String(const_applicationId.prefix(7))): (String(kAppText)), String(bytes: main_playerPathTitle.map{$0^186}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(const_imageEqualMessage.replacingOccurrences(of: "label", with: "pi")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: user_containerStr.map{upView(value: $0)}, encoding: .utf8)!: (String(main_sharedFormat.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(app_modelFormat.prefix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(kHeadKey.prefix(7))): msgInfo, String(bytes: const_leadMsg.map{dataBarStyle(edge: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            noRange(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: EqualCompartmentReactiveCompatible, reSend: Bool) {
    private func noRange(msgData: EqualCompartmentReactiveCompatible, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            drawObserver(showMsg: show_limitMessage)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.withDevice(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((const_imageEqualMessage.replacingOccurrences(of: "label", with: "pi"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(const_imageEqualMessage.replacingOccurrences(of: "label", with: "pi"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(const_imageEqualMessage.replacingOccurrences(of: "label", with: "pi"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: SomebodyRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        SomebodyRequestTool.backOf(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.hemAndHaw(msgData, aboveValue: nil, priceDownActive: NSNumber(value: 0), demonstrate: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.doingBag(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(kHeadKey.prefix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.hemAndHaw(msgData, aboveValue: msgInfo[(String(dataFlushMessage))] as? String, priceDownActive: NSNumber(value: 0), demonstrate: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(const_makeViewName.suffix(6)) + "fCoin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(const_makeViewName.suffix(6)) + "fCoin")] as! NSNumber
                //: PunctuateThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                PunctuateThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func doingBag(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard PunctuateThen.share.loginUserMode.status != 1 else {
            guard PunctuateThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    drawObserver(showMsg: errorStr)
                }
                //: return
                return
            }

            //: RangePushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            RangePushManager.share.picParams(clickEvent: user_statusRecordContent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.harvestMoonSend()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            publicTransport(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = FileView(frame: CGRect(x: 0, y: 0, width: kQueryValue, height: notiKeyTitle))
            //: view.show()
            view.welt()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                drawObserver(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension ControllerDelegate: IndividualisticReactiveCompatible {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func afterwards(giftModel _: AvailableTransformable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func sectionTarget() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func ting(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = LoadListController()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.upDict(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func circle(actionType: TypePriorityTarget) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            favorite()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func allModel(giftModel _: AvailableTransformable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func doTo(giftModel: AvailableTransformable, giftNum: String, model: SodalistSendMemberModel) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            drawObserver(showMsg: show_limitMessage)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(notiObjectMagnitudeKey))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(show_resultCalledPath))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(main_playTitle) + String(app_managerData.prefix(6)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(app_modelFormat.prefix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(appFemaleText))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: SomebodyRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        SomebodyRequestTool.rangeBy(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.doingBag(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.requestTo(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.openContent(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func lowAction(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.keyNameure(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func alongTask(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.keyNameure(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func everyObjectMake(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.everyObjectMake(tap)
    }

    //: func func__starRecordBtnDown() {}
    func prevalence() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func example(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func favorite() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        CustomPermissionTool.videoOf(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = DirectReactiveCompatible.sizeUponTelecasting(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: RangePushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                RangePushManager.share.retrieve()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.tarotCard(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func imagePhotos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        CustomPermissionTool.behindCell(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.tarotCard(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension ControllerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func messageInfo(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(app_modelFormat.prefix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension ControllerDelegate {
    //: func func__TipsNotification() {
    func presentButton() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(tipsImage(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: userTableMessage,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func tipsImage(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: SizeThen = userinfo[(String(data_viewMessage))] as! SizeThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.keyNameure(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func publicTransport(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: userPairSegmentUrl.map{toRaw(text: $0)}, encoding: .utf8)!: tipStr, (String(const_applicationId.prefix(7))): (String(constSaveContent))]
        //: var message: EqualCompartmentReactiveCompatible?
        var message: EqualCompartmentReactiveCompatible?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ModelThen.objectList(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.keyNameure(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func pickApart() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard PunctuateThen.share.appUserConfigMode.rTips.count > 0 else { return }
            guard PunctuateThen.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: PunctuateThen.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.publicTransport(tipStr: PunctuateThen.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension ControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func openContent(extral: [String: Any?], model: SodalistSendMemberModel, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(kHeadKey.prefix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(noti_viewPath.prefix(7)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(const_applicationId.prefix(7)))] = (String(appMakeTitle))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(kHeadKey.prefix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: kSelectMakeValue.map{$0^128}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (user_timeWithId.replacingOccurrences(of: "empty", with: "n") + String(appRowValue)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: showTimeTitle.map{kitMake(leading: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(mainResultData.suffix(5)) + showImageName.replacingOccurrences(of: "add", with: "mb")).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(appMakeTitle))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(appMakeTitle))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(user_timeWithId.replacingOccurrences(of: "empty", with: "n") + String(appRowValue))] = "\(giftdic[(String(k_centerStr) + String(k_frameName.prefix(6)))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(noti_addKey.suffix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ModelThen.objectList(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.withDevice(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(main_sourceId.suffix(4)) + "ncom" + constTimeMessage.replacingOccurrences(of: "heart", with: "e"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(main_sourceId.suffix(4)) + "ncom" + constTimeMessage.replacingOccurrences(of: "heart", with: "e"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.hemAndHaw(cellData, aboveValue: msgInfoDic[(String(dataFlushMessage))] as? String, priceDownActive: msgIncome, demonstrate: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        addModel(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(const_makeViewName.suffix(6)) + "fCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(const_makeViewName.suffix(6)) + "fCoin")] as! NSNumber
            //: PunctuateThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            PunctuateThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: PunctuateThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.upWill(needReload: false, mf_coin: PunctuateThen.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.equalRescue()
    }

    //: func initCachEffectData() {
    func tillErasePrevious() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.channelId(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func addModel(giftDic: [String: Any], model: SodalistSendMemberModel) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.bindSumeraction(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in NumberryReactiveCompatible.share.ofItems() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SubMeasurable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.usModel(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.readDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func recordArr(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in NumberryReactiveCompatible.share.ofItems() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SubMeasurable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.usModel(model: giftModel as! ListHandyJSON)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.afterImage(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension ControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func tiptoe() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func itemUp() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(mainKeyValue)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(mainKeyValue)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - k_netId)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func totalernationality() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = LoadListController()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! EqualCompartmentReactiveCompatible).isKind(of: ClickCellData.self) {
                    if (cellData as! EqualCompartmentReactiveCompatible).isKind(of: ClickCellData.self) {
                        //: let groupCellData = cellData as! ClickCellData
                        let groupCellData = cellData as! ClickCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(dataFlushMessage))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: ModelDataV2Listener.shared.rx
        ModelDataV2Listener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(dataModeUrl.prefix(6)) + "ection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
