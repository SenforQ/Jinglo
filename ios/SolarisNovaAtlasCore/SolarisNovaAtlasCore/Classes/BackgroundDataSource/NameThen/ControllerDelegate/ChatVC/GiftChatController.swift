
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let const_modeRenderKey:[UInt8] = [0xe7,0xe0,0xe7,0xfa,0xa6,0xed,0xe1,0xea,0xeb,0xfc,0xb4,0xa7,0xae,0xe6,0xef,0xfd,0xae,0xe0,0xe1,0xfa,0xae,0xec,0xeb,0xeb,0xe0,0xae,0xe7,0xe3,0xfe,0xe2,0xeb,0xe3,0xeb,0xe0,0xfa,0xeb,0xea]

/*: "F5F5F5" :*/
fileprivate let mainLabelHandleData:String = "FcenterFcenterFcenter"

/*: "#128CFF" :*/
fileprivate let showItemPicId:[Character] = ["#","1","2","8","C"]
fileprivate let notiNameInmateText:String = "viewview"

/*: "Feedback" :*/
fileprivate let app_myKey:String = "view true back model groupFeedback"

/*: "#8370FF" :*/
fileprivate let data_backKey:String = "#8370FFselected if"

/*: "Quick Greeting" :*/
fileprivate let showModelMsg:[Character] = ["Q","u","i","c","k"," ","G","r","e","e","t","i"]
fileprivate let appBagUrl:[Character] = ["n","g"]

/*: "Beauty Photos" :*/
fileprivate let data_imageFormat:[Character] = ["B","e","a","u","t","y"," ","P"]
fileprivate let user_playerTitle:[Character] = ["h","o","t","o","s"]

/*: "url" :*/
fileprivate let dataKitPackageId:[Character] = ["u","r","l"]

/*: "unlockGift" :*/
fileprivate let userValueMessage:String = "umakelo"

/*: "%d" :*/
fileprivate let k_itemName:String = "model color go string%d"

/*: "isBurn" :*/
fileprivate let show_sessionToUrl:[UInt8] = [0xce,0xd4,0xe5,0xd2,0xd5,0xc9]

private func modelCorner(selected num: UInt8) -> UInt8 {
    return num ^ 167
}

/*: "giftId" :*/
fileprivate let constErrorTitle:[Character] = ["g","i","f","t","I"]
fileprivate let noti_blockValue:[Character] = ["d"]

/*: "rYMsgType" :*/
fileprivate let userAcceptName:String = "request from imagerYMsg"
fileprivate let constToImageMsg:String = "Typeself path position register type"

/*: "GJ:CallCustom" :*/
fileprivate let show_pathFormat:String = "leading valueGJ:C"
fileprivate let app_limitKey:String = "userm"

/*: "changeDirection" :*/
fileprivate let data_reportPath:String = "CHANG"
fileprivate let appCookieName:String = "ctiinstance"

/*: "extra" :*/
fileprivate let main_windowMsg:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let data_textWhiteMessage:String = "view with infomsgInfo"

/*: "toUid" :*/
fileprivate let mainSectionData:String = "toUidlet type size"

/*: "wantGift" :*/
fileprivate let showMmAppFormat:String = "conversation collection data content modelwantGift"

/*: "jumpKey" :*/
fileprivate let k_equalMessage:String = "jumpKeif red label content"
fileprivate let mainKeyMessage:[Character] = ["y"]

/*: "truePersonAuth" :*/
fileprivate let dataMinimumTipKey:String = "truePindex view"

/*: "logType" :*/
fileprivate let main_sharedData:[Character] = ["l","o","g","T","y"]
fileprivate let data_viewFormat:[Character] = ["p","e"]

/*: "msgType" :*/
fileprivate let mainEqualData:[UInt8] = [0x48,0x56,0x42,0x71,0x5c,0x55,0x40]

/*: "txt" :*/
fileprivate let noti_mediaMessage:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let notiBottomFormat:String = "typfirst"

/*: "QuickMsg" :*/
fileprivate let noti_modelValue:String = "Quickpath image"
fileprivate let main_observerImageStr:[Character] = ["M","s","g"]

/*: "params" :*/
fileprivate let userDragNetKey:[UInt8] = [0x39,0x28,0x3b,0x28,0x24,0x3a]

/*: "content" :*/
fileprivate let user_levelData:[UInt8] = [0x7e,0x72,0x73,0x69,0x78,0x73,0x69]

private func makeSystem(wrap num: UInt8) -> UInt8 {
    return num ^ 29
}

/*: "id" :*/
fileprivate let data_viewUrl:String = "path"

/*: "txtId" :*/
fileprivate let noti_whiteKey:String = "value lefttxtId"

/*: "message" :*/
fileprivate let data_requestId:[UInt8] = [0xe4,0xec,0xfa,0xfa,0xe8,0xee,0xec]

/*: "TxtMsg" :*/
fileprivate let constLabPlayMessage:[Character] = ["T","x","t","M","s","g"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let showVideoMinReturnName:[UInt8] = [0xeb,0x4,0xfe,0xf8,0xfa,0xb5,0xfd,0xfa,0x1,0x1,0x4,0xb5,0xf9,0x4,0xc,0x3,0x1,0x4,0xf6,0xf9,0xb5,0xfb,0xf6,0xfe,0x1,0xfa,0xf9,0xc3,0xb5,0xe5,0x7,0x4,0xf8,0xfa,0xf9,0xa,0x7,0xfa]

fileprivate func shareTable(add num: UInt8) -> UInt8 {
    let value = Int(num) - 149
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let appStartStr:String = "lstylength"

/*: "audio" :*/
fileprivate let main_makeFormat:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let constPartyStr:String = "want"
fileprivate let dataPathPairMsg:String = "dioIdshadow table"

/*: "audioLength" :*/
fileprivate let mainTableStr:[UInt8] = [0x68,0x74,0x67,0x6e,0x65,0x4c,0x6f,0x69,0x64,0x75,0x61]

/*: "audioSandbox" :*/
fileprivate let noti_cellData:[Character] = ["a","u","d","i","o"]
fileprivate let mainNextData:String = "bottom eh modelSa"
fileprivate let const_pathFormat:String = "ndbattentionx"

/*: "senderId" :*/
fileprivate let mainOfCurrentKey:[UInt8] = [0x22,0x34,0x3f,0x35,0x34,0x23,0x18,0x35]

private func largeImage(video num: UInt8) -> UInt8 {
    return num ^ 81
}

/*: "AudioMsg" :*/
fileprivate let data_rangeMsg:[Character] = ["A","u","d","i","o","M"]
fileprivate let data_messageStr:String = "SG"

/*: "Can't send blank message" :*/
fileprivate let mainEveryOmitKey:String = "Can\'t s"
fileprivate let constValueData:String = "end kit intimate lab lab"
fileprivate let appToFormat:[Character] = ["k"," ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let kTextPath:[Character] = ["q","u","o","t","e","M"]
fileprivate let mainCustomTitle:String = "image match text titlesgId"

/*: "quoteMsgInfo" :*/
fileprivate let noti_cityEqualName:[Character] = ["q","u","o","t","e","M","s","g","I","n","f"]
fileprivate let main_colorItemPositionFormat:[Character] = ["o"]

/*: "contentType" :*/
fileprivate let userModelFrameValue:[UInt8] = [0xbd,0xc9,0xc8,0xce,0xbf,0xc8,0xce,0xae,0xd3,0xca,0xbf]

fileprivate func equalAttachKey(suite num: UInt8) -> UInt8 {
    let value = Int(num) - 90
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let mainCancelPath:String = "ImgMsgin large color request"

/*: "img" :*/
fileprivate let notiFromTitle:String = "iinteraction"

/*: "imageUri" :*/
fileprivate let k_normalTopPath:[UInt8] = [0x1c,0x18,0x14,0x12,0x10,0x20,0x7,0x1c]

/*: "customSend" :*/
fileprivate let dataResumeName:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "pic" :*/
fileprivate let notiValueName:String = "pequal"

/*: "VideoMsg" :*/
fileprivate let dataPriceFormat:String = "VideoMsgsucceed view"

/*: "video" :*/
fileprivate let noti_titleMessage:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let mainMultipleMsg:[UInt8] = [0xb9,0xb5,0xac,0xbf,0xa8,0x93,0xb7,0xbd]

private func colorPath(equal num: UInt8) -> UInt8 {
    return num ^ 218
}

/*: "videoUrl" :*/
fileprivate let noti_imageKey:[UInt8] = [0x6c,0x72,0x55,0x6f,0x65,0x64,0x69,0x76]

/*: "duration" :*/
fileprivate let user_toKey:[UInt8] = [0x8f,0x9e,0x99,0x8a,0x9f,0x82,0x84,0x85]

private func sizeTableTrue(name num: UInt8) -> UInt8 {
    return num ^ 235
}

/*: "len" :*/
fileprivate let k_withValue:[UInt8] = [0x8,0x1,0xa]

/*: "Documents/User/voice/" :*/
fileprivate let mainQuickSucceedFormat:String = "center keyDocum"
fileprivate let data_colorMsg:String = "let in valueser/vo"
fileprivate let showToName:String = "data var self var typeice/"

/*: "Documents/User/record/" :*/
fileprivate let showManagerValue:String = "Documetext target app"
fileprivate let mainTitleFormat:String = "nts/Uscase line view"
fileprivate let dataTouchFileContent:String = "bar image labeler/rec"

/*: "msgIncome" :*/
fileprivate let showTitlePath:String = "MSG"
fileprivate let notiItemSocialValue:String = "self gift enable makeIncome"

/*: "mfBean" :*/
fileprivate let appResultText:[UInt8] = [0x6e,0x61,0x65,0x42,0x66,0x6d]

/*: "messageType" :*/
fileprivate let user_selectedKey:String = "MESSAG"

/*: "WantGift" :*/
fileprivate let const_sizeKey:String = "model make layer import cornerWantGift"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let kContentValue:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t","T"]
fileprivate let showNameUrl:[Character] = ["i"]
fileprivate let noti_playerId:String = "psTitle_price cell gesture render"

/*: "msgId" :*/
fileprivate let k_firstFormat:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let appViewStr:String = "TOTA"

/*: "attachSysMsg" :*/
fileprivate let notiAcceptUrl:[Character] = ["a","t","t","a","c","h","S","y","s","M","s"]
fileprivate let constVideoFormat:String = "G"

/*: "intimatePhoto" :*/
fileprivate let const_arrayContent:String = "frametimate"
fileprivate let noti_modePath:String = "value edit floatPhoto"

/*: _ :*/
fileprivate let user_labValue:[Character] = ["_"]

/*: "totalIntimate" :*/
fileprivate let notiModelValue:String = "TO"
fileprivate let noti_filterStr:String = "talInguard size normal self"

/*: "uid" :*/
fileprivate let notiEqualKey:[UInt8] = [0xd4,0xc8,0xc5]

private func strategicWarning(title num: UInt8) -> UInt8 {
    return num ^ 161
}

/*: "intimate" :*/
fileprivate let noti_minId:[UInt8] = [0xb8,0xbd,0xc3,0xb8,0xbc,0xb0,0xc3,0xb4]

fileprivate func skipTitle(index num: UInt8) -> UInt8 {
    let value = Int(num) - 79
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let showCenterModelStr:[UInt8] = [0x4,0x22,0x2f,0xe8,0x35,0xe1,0x36,0x34,0x26,0xe1,0x35,0x29,0x26,0xe1,0x27,0x36,0x2f,0x24,0x35,0x2a,0x30,0x2f,0xe1,0x25,0x36,0x26,0xe1,0x35,0x30,0xe1,0x3a,0x30,0x36,0x33,0xe1,0x2d,0x22,0x24,0x2c,0xe1,0x30,0x27,0xe1,0x2a,0x2f,0x35,0x2a,0x2e,0x22,0x24,0x3a,0xe1,0x2d,0x26,0x37,0x26,0x2d,0xef]

fileprivate func nameText(mode num: UInt8) -> UInt8 {
    let value = Int(num) - 193
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftNum" :*/
fileprivate let app_indexMsg:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let noti_videoId:[Character] = ["p","k","g"]
fileprivate let userRangeAddFormat:[Character] = ["I","t","e","m","s","e","t","I","d"]

/*: "from" :*/
fileprivate let mainPermissionValue:String = "ftom"

/*: "wantGiftId" :*/
fileprivate let user_modelSizeViewMsg:[Character] = ["w","a","n","t","G","i","f","t","I"]
fileprivate let const_siblingMagnitudeKey:String = "data"

/*: "WantGiftMsg" :*/
fileprivate let userBackTitle:String = "WantGiftview player equal strip out"
fileprivate let constImagePath:[Character] = ["M","s","g"]

/*: "gift" :*/
fileprivate let appLevelStr:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let constCurrentValue:String = "color errorView info"

/*: "Notes" :*/
fileprivate let show_buttonStr:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let mainAttentionName:String = "model toReport"

/*: "View read receipts?" :*/
fileprivate let show_giftTitle:[Character] = ["V","i","e","w"," ","r","e"]
fileprivate let dataToFormat:String = "to model colorad rec"
fileprivate let mainPartyId:String = "universal include self if groupeipts?"

/*: "He" :*/
fileprivate let kMessageViewHeadName:[Character] = ["H","e"]

/*: "She" :*/
fileprivate let data_lastMakeMsg:String = "unknown view size push areaShe"

/*: "%@ is using too low a version" :*/
fileprivate let notiMaxFormat:String = "icon mobile else%@ is"
fileprivate let userUpStr:String = "request center regular label too "
fileprivate let notiTouchTitle:String = "a veactual equal self"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let kTitleText:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a"]
fileprivate let notiImageMsg:String = "t leasmobile self"
fileprivate let appBurnKey:String = "tableecondtable"

/*: "Upload failed" :*/
fileprivate let mainViewButtonName:String = "size var byUplo"
fileprivate let const_toUserUrl:String = "lobserver"

/*: "播放状态: :*/
fileprivate let noti_sourceData:[Character] = ["播","\u{653e}","状","\u{6001}",":"]

/*: "c2c_" :*/
fileprivate let show_maxFormat:String = "c2c_normal bottom equal layer observer"

/*: "msg" :*/
fileprivate let dataScreenFormat:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let mainIconValue:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let kStrengthTitle:[UInt8] = [0x7d,0x4b,0x51,0x4,0x47,0x45,0x4a,0x4,0x51,0x57,0x41,0x4,0x50,0x4c,0x41,0x4,0x52,0x4d,0x40,0x41,0x4b,0x4,0x47,0x4c,0x45,0x50,0x4,0x42,0x41,0x45,0x50,0x51,0x56,0x41,0x4,0x50,0x4b,0x4,0x43,0x4d,0x52,0x41,0x4,0x1,0x64,0x4,0x45,0x4,0x47,0x45,0x48,0x48]

private func ifKit(background num: UInt8) -> UInt8 {
    return num ^ 36
}

/*: "cellData" :*/
fileprivate let noti_viewText:String = "make errorcellData"

/*: "System notification" :*/
fileprivate let noti_colorMessage:[Character] = ["S","y","s","t","e","m"," "]
fileprivate let user_appId:[Character] = ["n","o","t","i","f","i","c","a","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GiftChatController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ListFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class GiftChatController: SizeViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = RequestHandyJSON()
    private var userWrap = RequestHandyJSON() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ListFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: RequestHandyJSON = RequestHandyJSON()) {
    init(chatID: String, isFrom: ListFromEnum = .Normal, userWrap: RequestHandyJSON = RequestHandyJSON()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        GiftChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        MoveReactiveCompatible.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == PublisherReactiveCompatible.getXiaoMiID() {
        if targetId == PublisherReactiveCompatible.exceptXiao() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == PublisherReactiveCompatible.getCustomerServiceID() {
        } else if targetId == PublisherReactiveCompatible.appId() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: const_modeRenderKey.map{$0^142}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PunctuateThen.share.appConfigMode.disableShootScreen {
            if PunctuateThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = StartView(frame: CGRect(x: 0, y: 0, width: kQueryValue, height: notiKeyTitle))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        playerDetail()
        //: createUI()
        prideOfPlace()
        //: createUIConstraints()
        playConstraints()
        //: bindInteraction()
        shove()
        //: addTapGestureRecognizer()
        acceptAdd()
        //: func__requestChatInfo()
        nameData()
        //: func__getFillDraftText()
        fillRequest()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        SectorThen.shared.viewSize()
        //: func__addVoiceNotification()
        director()
        //: addReadPeceiptNotification()
        skimOverImage()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            task()
            //: func__TipsNotification()
            scaleOfMeasurement()
            //: if PunctuateThen.share.loginUserMode.sex == Gender.male.rawValue {
            if PunctuateThen.share.loginUserMode.sex == TopComparable.male.rawValue {
                //: func__setReplyTipsView()
                thanView()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        prixFixe(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        prixFixe(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            toTitle()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: MakeTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = MakeTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (mainLabelHandleData.replacingOccurrences(of: "center", with: "5")))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(TagMsgCell.self, forCellReuseIdentifier: TagMsgCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ActionReactiveCompatible.self, forCellReuseIdentifier: ActionReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(GestureThen.self, forCellReuseIdentifier: GestureThen.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(ParentThen.self, forCellReuseIdentifier: ParentThen.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(PublisherView.self, forCellReuseIdentifier: PublisherView.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(TelecastingViewCell.self, forCellReuseIdentifier: TelecastingViewCell.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(CompartmentEqualMsgCell.self, forCellReuseIdentifier: CompartmentEqualMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(WithMessageCell.self, forCellReuseIdentifier: WithMessageCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(JumpViewCell.self, forCellReuseIdentifier: JumpViewCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: ToolView.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(MessageViewCell.self, forCellReuseIdentifier: ToolView.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: RefMatchDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = RefMatchDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: StateViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: TotalernationalityViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = StateViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(showItemPicId) + notiNameInmateText.replacingOccurrences(of: "view", with: "F")))!, .font: UIFont.clickLarge(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(app_myKey.suffix(8))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(pessimisticNegativeFeedbackLikeSnap), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: ChatReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = ChatReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: HeatherView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = HeatherView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.clickLarge(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(data_backKey.prefix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(showModelMsg) + String(appBagUrl)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.readRemove().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(theAction), for: .touchUpInside)
        //: btn.isHidden = !PunctuateThen.share.appUserConfigMode.showDirectGreet || PunctuateThen.share.loginUserMode.sex == Gender.male.rawValue || PunctuateThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !PunctuateThen.share.appUserConfigMode.showDirectGreet || PunctuateThen.share.loginUserMode.sex == TopComparable.male.rawValue || PunctuateThen.share.appStatus == HiddenHashableRepresentation.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: TopTipsView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = TopTipsView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: ListView = {
        //: let v = TalkingSelectTranslationView()
        let v = ListView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.format(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: ImageReactiveCompatible = {
        //: let v = TalkingTranslationResultView()
        let v = ImageReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.sinceFirst(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.clickLarge(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(data_backKey.prefix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(data_imageFormat) + String(user_playerTitle)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.readRemove().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(processClick), for: .touchUpInside)
        //: btn.isHidden = PunctuateThen.share.loginUserMode.sex == Gender.male.rawValue || PunctuateThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = PunctuateThen.share.loginUserMode.sex == TopComparable.male.rawValue || PunctuateThen.share.appStatus == HiddenHashableRepresentation.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: ShankGamesView = {
        //: let v = TalkingLiveRoomGamesView()
        let v = ShankGamesView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: PublisherDataSource = {
        //: let view = TalkingPrivateBannerView.init()
        let view = PublisherDataSource()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: TotaloScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = TotaloScreenView()
        //: v.isHidden = (PunctuateThen.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (PunctuateThen.share.appStatus == HiddenHashableRepresentation.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: SingularThen = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = SingularThen()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.labAdd()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension GiftChatController: EqualReactiveCompatible {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func heritableUponView(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if PunctuateThen.share.loginUserMode.sex == Gender.male.rawValue {
                if PunctuateThen.share.loginUserMode.sex == TopComparable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if PunctuateThen.share.loginUserMode.sex == Gender.male.rawValue {
                if PunctuateThen.share.loginUserMode.sex == TopComparable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.gestureTo(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension GiftChatController: ToDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func theAction() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        MoveReactiveCompatible.counterest(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.drawObserver(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.takeDict(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func processClick() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        MoveReactiveCompatible.propel(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = DigitiserViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().beforeRender(urlStr: dict?[(String(dataKitPackageId))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(userValueMessage.replacingOccurrences(of: "make", with: "n") + "ckGift")] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = SizeModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: show_sessionToUrl.map{modelCorner(selected: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(constErrorTitle) + String(noti_blockValue))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.titleTo(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> EqualCompartmentReactiveCompatible! {
    func price(_: MakeTableViewController!, everyIndicator data: V2TIMMessage!) -> EqualCompartmentReactiveCompatible! {
        //: let dic = MemoryChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = MemoryChatManager.parse(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = ModelThen.atGift(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: NameCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(userAcceptName.suffix(5)) + String(constToImageMsg.prefix(4)))].stringValue == (String(show_pathFormat.suffix(4)) + "allCus" + app_limitKey.replacingOccurrences(of: "user", with: "to")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(data_reportPath.lowercased() + "eDire" + appCookieName.replacingOccurrences(of: "instance", with: "on"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(main_windowMsg))][(String(data_textWhiteMessage.suffix(7)))][(String(mainSectionData.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = LoadReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = LoadReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: SizeCellData.self) == true ||
        if cellData?.isKind(of: SizeCellData.self) == true ||
            //: cellData?.isKind(of: ChatThen.self) == true ||
            cellData?.isKind(of: ChatThen.self) == true ||
            //: cellData?.isKind(of: LoadReactiveCompatible.self) == true ||
            cellData?.isKind(of: LoadReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: DirectCellData.self) == true {
            cellData?.isKind(of: DirectCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            MoveReactiveCompatible.asState(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            MoveReactiveCompatible.behindInstance(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: EqualCompartmentReactiveCompatible!) -> ToolView! {
    func draw(_: MakeTableViewController!, local data: EqualCompartmentReactiveCompatible!) -> ToolView! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = compartment(data: data)

        //: if data.isKind(of: LoadReactiveCompatible.self) {
        if data.isKind(of: LoadReactiveCompatible.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = TagMsgCell(style: .default, reuseIdentifier: TagMsgCell.className())
            //: let cellData = data as! LoadReactiveCompatible
            let cellData = data as! LoadReactiveCompatible

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.size() {
                //: if cellData.msgModel.quoteMsgInfo.uid == PunctuateThen.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == PunctuateThen.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = PunctuateThen.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = PunctuateThen.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: ChatThen.self) {
        if data.isKind(of: ChatThen.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ActionReactiveCompatible(style: .default, reuseIdentifier: ActionReactiveCompatible.className())
            //: let cellData = data as! ChatThen
            let cellData = data as! ChatThen
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: DirectCellData.self) {
        if data.isKind(of: DirectCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = GestureThen(style: .default, reuseIdentifier: GestureThen.className())
            //: let cellData = data as! DirectCellData
            let cellData = data as! DirectCellData
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ViewCellData.self) {
        if data.isKind(of: ViewCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = ParentThen(style: .default, reuseIdentifier: ParentThen.className())
            //: let cellData = data as! ViewCellData
            let cellData = data as! ViewCellData
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: DataFileReactiveCompatible.self) {
        if data.isKind(of: DataFileReactiveCompatible.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = PublisherView(style: .default, reuseIdentifier: PublisherView.className())
            //: let cellData = data as! DataFileReactiveCompatible
            let cellData = data as! DataFileReactiveCompatible
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = AvailableTransformable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.startTableStr(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(showMmAppFormat.suffix(8))))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: SizeCellData.self) {
        if data.isKind(of: SizeCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = WithMessageCell(style: .default, reuseIdentifier: WithMessageCell.className())
            //: let cellData = data as! SizeCellData
            let cellData = data as! SizeCellData
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: NameCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = CompartmentEqualMsgCell(style: .default, reuseIdentifier: CompartmentEqualMsgCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! NameCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: SizeThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = TelecastingViewCell(style: .default, reuseIdentifier: TelecastingViewCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! SizeThen
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: RitualCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = JumpViewCell(style: .default, reuseIdentifier: JumpViewCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! RitualCellData
            //: cell.fill(with: cellData)
            cell.ladenInfo(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ToolView!) {
    func cipherOrigin(_: MakeTableViewController!, managerPosition cell: ToolView!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != PublisherReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != PublisherReactiveCompatible.appId() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: RangePushManager.share.func__pushToUserDetailVC(uid: String(uid))
            RangePushManager.share.clickOfUid(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ToolView!) {
    func action(_: MakeTableViewController!, latchkey cell: ToolView!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ActionReactiveCompatible.self) { // 图片
            //: let imageCellData = cell.messageData as! ChatThen
            let imageCellData = cell.messageData as! ChatThen
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = RangeMakeThen(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.bull(imageCellData, domain: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.modusVivendiEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: showChangeName)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.giftView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.subedit(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                fileController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(constButtonMessage)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = StylePhotoModel()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = ViewMeasurable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [StylePhotoModel] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = LayerFlowLayout(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                fileController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: GestureThen.self) { // 视频
            //: let videoCellData = cell.messageData as! DirectCellData
            let videoCellData = cell.messageData as! DirectCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = FailViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                fileController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = CheckThen(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.becomeOff(videoCellData, pic: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.modusVivendiEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: showChangeName)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.giftView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.subedit(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                fileController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: WithMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! WithMessageCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! SizeCellData, cellView: audioCell)
            welcome(cellData: audioCell.cellData as! SizeCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: TagMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! TagMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = MemoryChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = MemoryChatManager.parse(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(main_windowMsg))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(k_equalMessage.prefix(6)) + String(mainKeyMessage))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(k_equalMessage.prefix(6)) + String(mainKeyMessage))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(dataMinimumTipKey.prefix(5)) + "ersonAuth") {
                    //: if PunctuateThen.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if PunctuateThen.share.loginUserMode.isTPAuth == AuthColumnConvertible.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if PunctuateThen.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if PunctuateThen.share.loginUserMode.isTPAuth == AuthColumnConvertible.unknown.rawValue {
                        //: RangePushManager.share.func__pushUserVerifyController(toast: nil)
                        RangePushManager.share.controller(toast: nil)
                        //: } else if PunctuateThen.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if PunctuateThen.share.loginUserMode.isTPAuth == AuthColumnConvertible.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = TopViewController()
                        //: RangePushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        RangePushManager.share.retrieve()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: RangePushManager.share.func__pushUserVerifyController(toast: nil)
                        RangePushManager.share.controller(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<TelephoneMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: RangePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RangePushManager.share.actionModel(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(userAcceptName.suffix(5)) + String(constToImageMsg.prefix(4)))] as? String == (String(show_pathFormat.suffix(4)) + "allCus" + app_limitKey.replacingOccurrences(of: "user", with: "to")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(data_textWhiteMessage.suffix(7)))][(String(main_sharedData) + String(data_viewFormat))].intValue == 1 {
                    //: checkAndCallVoice()
                    colorAcrossVoice()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    videoWith()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: TelecastingViewCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! TelecastingViewCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = MemoryChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = MemoryChatManager.parse(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(main_windowMsg))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(k_equalMessage.prefix(6)) + String(mainKeyMessage))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<TelephoneMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: RangePushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    RangePushManager.share.actionModel(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: CompartmentEqualMsgCell.self) {
            //: RangePushManager.share.func__pushToUserDetailVC(uid: targetId)
            RangePushManager.share.clickOfUid(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ToolView!) {
    func haywire(_: MakeTableViewController!, model cell: ToolView!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: CompartmentCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ActionReactiveCompatible.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! ActionReactiveCompatible
            //: cellData = imageCell.cellData as! ChatThen
            cellData = imageCell.cellData as! ChatThen
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: GestureThen.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! GestureThen
            //: cellData = videoCell.cellData as! DirectCellData
            cellData = videoCell.cellData as! DirectCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: WithMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! WithMessageCell
            //: cellData = audioCell.cellData as! SizeCellData
            cellData = audioCell.cellData as! SizeCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: TagMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! TagMsgCell
            //: cellData = textCell.cellData as! LoadReactiveCompatible
            cellData = textCell.cellData as! LoadReactiveCompatible
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: EqualCompartmentReactiveCompatible? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: EqualCompartmentReactiveCompatible? = ModelThen.atTo(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: DirectCellData.self) {
                if reCellData!.isKind(of: DirectCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    removeChange(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    the(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: EqualCompartmentReactiveCompatible!) {
    func nonnegative(_: MakeTableViewController!, auditoryImage cellData: EqualCompartmentReactiveCompatible!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = MoveReactiveCompatible.dataArrayHead(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: EqualCompartmentReactiveCompatible!) {
    func with(_: MakeTableViewController!, start cellData: EqualCompartmentReactiveCompatible!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        MoveReactiveCompatible.requestObject(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: EqualCompartmentReactiveCompatible) -> EqualCompartmentReactiveCompatible {
    private func compartment(data: EqualCompartmentReactiveCompatible) -> EqualCompartmentReactiveCompatible {
        //: let isSelf = data.msgModel.user.uid == Int(PunctuateThen.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(PunctuateThen.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if PunctuateThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if PunctuateThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = PunctuateThen.share.loginUserMode.headPic!
                data.msgModel.user.headPic = PunctuateThen.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = PunctuateThen.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = PunctuateThen.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension GiftChatController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func colorSection() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!: (String(noti_mediaMessage))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(notiBottomFormat.replacingOccurrences(of: "first", with: "e")): (String(noti_modelValue.prefix(5)) + String(main_observerImageStr))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_textWhiteMessage.suffix(7))): msgInfo, String(bytes: userDragNetKey.map{$0^73}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.the(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func takeDict(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: user_levelData.map{makeSystem(wrap: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: user_levelData.map{makeSystem(wrap: $0)}, encoding: .utf8)!: content, (String(noti_whiteKey.suffix(5))): greetId, String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!: (String(noti_mediaMessage))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: data_requestId.map{$0^137}, encoding: .utf8)!: content, (String(noti_whiteKey.suffix(5))): greetId, (notiBottomFormat.replacingOccurrences(of: "first", with: "e")): (String(constLabPlayMessage))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(data_textWhiteMessage.suffix(7))): msgInfo, String(bytes: userDragNetKey.map{$0^73}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.the(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            StatusThen.shared.nameWish(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.drawObserver(showMsg: String(bytes: showVideoMinReturnName.map{shareTable(add: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        MoveReactiveCompatible.repulse(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(appStartStr.replacingOccurrences(of: "style", with: "e"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!: (String(main_makeFormat)),
                               //: "audioId": greetId,
                               (constPartyStr.replacingOccurrences(of: "want", with: "au") + String(dataPathPairMsg.prefix(5))): greetId,
                               //: "audioLength": length,
                               String(bytes: mainTableStr.reversed(), encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(noti_cellData) + String(mainNextData.suffix(2)) + const_pathFormat.replacingOccurrences(of: "attention", with: "o")): model.unZipFilePath,
                               //: "senderId": PunctuateThen.share.loginUserMode.userID]
                               String(bytes: mainOfCurrentKey.map{largeImage(video: $0)}, encoding: .utf8)!: PunctuateThen.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(constPartyStr.replacingOccurrences(of: "want", with: "au") + String(dataPathPairMsg.prefix(5))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: mainTableStr.reversed(), encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(noti_cellData) + String(mainNextData.suffix(2)) + const_pathFormat.replacingOccurrences(of: "attention", with: "o")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (notiBottomFormat.replacingOccurrences(of: "first", with: "e")): (String(data_rangeMsg) + data_messageStr.lowercased())]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(data_textWhiteMessage.suffix(7))): msgInfo, String(bytes: userDragNetKey.map{$0^73}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.the(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func sinceFirst(msgStr: String, quoteModel: PeregrineModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            drawObserver(showMsg: (mainEveryOmitKey + String(constValueData.prefix(4)) + "blan" + String(appToFormat)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: user_levelData.map{makeSystem(wrap: $0)}, encoding: .utf8)!: temStr, String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!: (String(noti_mediaMessage))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: data_requestId.map{$0^137}, encoding: .utf8)!: temStr, (notiBottomFormat.replacingOccurrences(of: "first", with: "e")): (String(constLabPlayMessage))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(data_textWhiteMessage.suffix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(kTextPath) + String(mainCustomTitle.suffix(4)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(noti_cityEqualName) + String(main_colorItemPositionFormat))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: userDragNetKey.map{$0^73}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? PeregrineModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            the(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func titleTo(img: UIImage, sendModel: SizeModel) {
        //: let imagePath = ChatThen.getChatImagePath(image: img)
        let imagePath = ChatThen.qibla(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: userModelFrameValue.map{equalAttachKey(suite: $0)}, encoding: .utf8)!: (String(mainCancelPath.prefix(6))), String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!: (notiFromTitle.replacingOccurrences(of: "interaction", with: "mg")), String(bytes: k_normalTopPath.map{$0^117}, encoding: .utf8)!: imagePath, String(bytes: dataResumeName.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(notiValueName.replacingOccurrences(of: "equal", with: "ic")): imagePath,
                                        //: "type": "ImgMsg",
                                        (notiBottomFormat.replacingOccurrences(of: "first", with: "e")): (String(mainCancelPath.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: show_sessionToUrl.map{modelCorner(selected: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(constErrorTitle) + String(noti_blockValue)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_textWhiteMessage.suffix(7))): msgInfo, String(bytes: userDragNetKey.map{$0^73}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            the(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: EqualCompartmentReactiveCompatible?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func removeChange(msgData: EqualCompartmentReactiveCompatible?, sendModel: SizeModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: userModelFrameValue.map{equalAttachKey(suite: $0)}, encoding: .utf8)!: (String(dataPriceFormat.prefix(8))), String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!: (String(noti_titleMessage)), String(bytes: dataResumeName.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(notiBottomFormat.replacingOccurrences(of: "first", with: "e")): (String(dataPriceFormat.prefix(8))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: mainMultipleMsg.map{colorPath(equal: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: noti_imageKey.reversed(), encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: user_toKey.map{sizeTableTrue(name: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: show_sessionToUrl.map{modelCorner(selected: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(constErrorTitle) + String(noti_blockValue)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(data_textWhiteMessage.suffix(7))): msgInfo, String(bytes: userDragNetKey.map{$0^73}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = ModelThen.objectList(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.withDevice(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.sussOut(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.custom(in: cellData, oversupply: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.sussOut(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.current(dict, local: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        the(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func example(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!: (String(main_makeFormat)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: mainTableStr.reversed(), encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(noti_cellData) + String(mainNextData.suffix(2)) + const_pathFormat.replacingOccurrences(of: "attention", with: "o")): voicePath,
                       //: "senderId": PunctuateThen.share.loginUserMode.userID]
                       String(bytes: mainOfCurrentKey.map{largeImage(video: $0)}, encoding: .utf8)!: PunctuateThen.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(noti_cellData) + String(mainNextData.suffix(2)) + const_pathFormat.replacingOccurrences(of: "attention", with: "o")): voicePath,
                         //: "type": "AudioMsg",
                         (notiBottomFormat.replacingOccurrences(of: "first", with: "e")): (String(data_rangeMsg) + data_messageStr.lowercased()),
                         //: "len": "\(voiceTime)"]
                         String(bytes: k_withValue.map{$0^100}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_textWhiteMessage.suffix(7))): msgInfo, String(bytes: userDragNetKey.map{$0^73}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            the(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: EqualCompartmentReactiveCompatible, reSend: Bool) {
    private func the(msgData: EqualCompartmentReactiveCompatible, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            drawObserver(showMsg: show_limitMessage)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(mainSectionData.prefix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: DirectCellData.self) == false && msgData.isKind(of: DataFileReactiveCompatible.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: DirectCellData.self) == false, msgData.isKind(of: DataFileReactiveCompatible.self) == false, params[(notiBottomFormat.replacingOccurrences(of: "first", with: "e"))] as? String != (String(noti_modelValue.prefix(5)) + String(main_observerImageStr)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.withDevice(msgData)
        }
        //: let recordTime = MemoryChatManager.getMessageInsertTime()
        let recordTime = MemoryChatManager.cancelPenetrate()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((notiValueName.replacingOccurrences(of: "equal", with: "ic"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(notiValueName.replacingOccurrences(of: "equal", with: "ic"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(notiValueName.replacingOccurrences(of: "equal", with: "ic"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(notiBottomFormat.replacingOccurrences(of: "first", with: "e"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(data_rangeMsg) + data_messageStr.lowercased()) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(noti_cellData) + String(mainNextData.suffix(2)) + const_pathFormat.replacingOccurrences(of: "attention", with: "o"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(noti_cellData) + String(mainNextData.suffix(2)) + const_pathFormat.replacingOccurrences(of: "attention", with: "o"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(noti_cellData) + String(mainNextData.suffix(2)) + const_pathFormat.replacingOccurrences(of: "attention", with: "o")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(mainQuickSucceedFormat.suffix(5)) + "ents/U" + String(data_colorMsg.suffix(6)) + String(showToName.suffix(4)))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = notiBarRecordFormat + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(showManagerValue.prefix(6)) + String(mainTitleFormat.prefix(6)) + String(dataTouchFileContent.suffix(6)) + "ord/")) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = k_failureTitle + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(constPartyStr.replacingOccurrences(of: "want", with: "au") + String(dataPathPairMsg.prefix(5)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(main_makeFormat))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: SomebodyRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        SomebodyRequestTool.anyModel(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(noti_modelValue.prefix(5)) + String(main_observerImageStr)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.hemAndHaw(msgData, aboveValue: nil, priceDownActive: NSNumber(value: 0), demonstrate: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.modusVivendiEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: userPointValue)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(noti_whiteKey.suffix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    MoveReactiveCompatible.repulse(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.model(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(data_textWhiteMessage.suffix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((showTitlePath.lowercased() + String(notiItemSocialValue.suffix(6)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(showTitlePath.lowercased() + String(notiItemSocialValue.suffix(6)))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if LogLiveManager.anUp().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: user_screenUrl, object: nil, userInfo: [String(bytes: appResultText.reversed(), encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.imageTo(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(user_selectedKey.lowercased() + "eType")] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(noti_modelValue.prefix(5)) + String(main_observerImageStr)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!] = (String(const_sizeKey.suffix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    const_screenFormat.occurrenceIn(eventID: userTabValue)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(noti_modelValue.prefix(5)) + String(main_observerImageStr)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!] = (String(noti_mediaMessage))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(data_textWhiteMessage.suffix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.withDevice(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(PunctuateThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(PunctuateThen.share.loginUserMode.userID)" + (String(kContentValue) + String(showNameUrl) + String(noti_playerId.prefix(8))) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if PunctuateThen.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if PunctuateThen.share.loginUserMode.sex == TopComparable.male.rawValue, type != (String(noti_modelValue.prefix(5)) + String(main_observerImageStr)) {
                //: self.removeReplyTipsView()
                self.toFile()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.hemAndHaw(updateMsgData, aboveValue: msgInfo[(String(k_firstFormat))] as? String, priceDownActive: msgIncome, demonstrate: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            MoveReactiveCompatible.behindInstance(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((appViewStr.lowercased() + "lMfCoin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(appViewStr.lowercased() + "lMfCoin")] as! NSNumber
                //: PunctuateThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                PunctuateThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(notiAcceptUrl) + constVideoFormat.lowercased())) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(notiAcceptUrl) + constVideoFormat.lowercased())] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.digitizerStr(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((const_arrayContent.replacingOccurrences(of: "frame", with: "in") + String(noti_modePath.suffix(5)))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(const_arrayContent.replacingOccurrences(of: "frame", with: "in") + String(noti_modePath.suffix(5)))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? PhotoModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.bull(updateMsgData, domain: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(noti_titleMessage))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(noti_titleMessage))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? PublishNameModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.becomeOff(updateMsgData, pic: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        funcNowTip()

        //: updateSession()
        toSession()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func toSession() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        ReadoutReactiveCompatible.equalGarrison(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func modusVivendiEvent(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard PunctuateThen.share.loginUserMode.status != 1 else {
            guard PunctuateThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    drawObserver(showMsg: errorStr)
                }
                //: return
                return
            }

            //: RangePushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            RangePushManager.share.picParams(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.harvestMoonSend()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = FileView(frame: CGRect(x: 0, y: 0, width: kQueryValue, height: notiKeyTitle))
            //: view.show()
            view.welt()
            //: } else if errorCode == 60003, !PunctuateThen.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !PunctuateThen.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            MessageThen.shared.eponym()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                drawObserver(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension GiftChatController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func subedit(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(data_textWhiteMessage.suffix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.imageTo(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func equalView() {
        //: if self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.messageData(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(PunctuateThen.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(PunctuateThen.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (constDevicePath.array(forKey: const_videoBackMsg)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.dismissTo()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                constDevicePath.set(intimateCacheData, forKey: const_videoBackMsg)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func compoundNotification(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = MemoryChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = MemoryChatManager.parse(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(main_windowMsg))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(data_textWhiteMessage.suffix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(notiModelValue.lowercased() + String(noti_filterStr.prefix(5)) + "timate")]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.appTime(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func imageTo(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((notiModelValue.lowercased() + String(noti_filterStr.prefix(5)) + "timate")), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(notiModelValue.lowercased() + String(noti_filterStr.prefix(5)) + "timate")] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.appTime(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func appTime(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = ReadoutReactiveCompatible.comparable(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.messageData(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.playForOn(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.downContent(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.downContent(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        ReadoutReactiveCompatible.checkTime(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: notiEqualKey.map{strategicWarning(title: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: noti_minId.map{skipTitle(index: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: user_clickViewFileUrl,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension GiftChatController: IndividualisticReactiveCompatible {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func sectionTarget() {
        //: guard PunctuateThen.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard PunctuateThen.share.loginUserMode.sex == TopComparable.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.sizeDown(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func ting(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func circle(actionType: TypePriorityTarget) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    placeHandle()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    presentMakeBlunder()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                placeHandle()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if PunctuateThen.share.loginUserMode.sex == Gender.female.rawValue {
            if PunctuateThen.share.loginUserMode.sex == TopComparable.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if PunctuateThen.share.loginUserMode.loungePlus {
                if PunctuateThen.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.migration(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                presentMakeBlunder()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    redirect()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    presentMakeBlunder()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                redirect()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.imageBy(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func presentMakeBlunder() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.drawObserver(showMsg: String(bytes: showCenterModelStr.map{nameText(mode: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func doTo(giftModel _: AvailableTransformable, giftNum _: String, model _: SodalistSendMemberModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func allModel(giftModel: AvailableTransformable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        startTableStr(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: DataFileReactiveCompatible?, fromStr: String?) {
    func startTableStr(giftModel: AvailableTransformable, giftNum: String, cellData: DataFileReactiveCompatible?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            drawObserver(showMsg: show_limitMessage)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(mainSectionData.prefix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(constErrorTitle) + String(noti_blockValue))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(app_indexMsg))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(noti_videoId) + String(userRangeAddFormat))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(mainPermissionValue.replacingOccurrences(of: "to", with: "ro"))] = fromStr
        }
        // 送礼接口
        //: SomebodyRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        SomebodyRequestTool.slight(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.modusVivendiEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: user_statusRecordContent)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.requestTo(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.giftView(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.toSession()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(data_textWhiteMessage.suffix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((notiModelValue.lowercased() + String(noti_filterStr.prefix(5)) + "timate")), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(notiModelValue.lowercased() + String(noti_filterStr.prefix(5)) + "timate")] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.appTime(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.discourse(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                const_screenFormat.occurrenceIn(eventID: userAppUrl)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func afterwards(giftModel: AvailableTransformable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        const_screenFormat.occurrenceIn(eventID: app_giftKey)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            drawObserver(showMsg: show_limitMessage)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= PunctuateThen.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            presentMakeBlunder()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(user_modelSizeViewMsg) + const_siblingMagnitudeKey.replacingOccurrences(of: "data", with: "d"))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(notiBottomFormat.replacingOccurrences(of: "first", with: "e"))] = (String(userBackTitle.prefix(8)) + String(constImagePath))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!: (String(const_sizeKey.suffix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(data_textWhiteMessage.suffix(7))): msgInfo, String(bytes: userDragNetKey.map{$0^73}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ModelThen.objectList(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            the(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func lowAction(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(noti_bottomId - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.keyNameure(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func alongTask(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.keyNameure(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func everyObjectMake(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.everyObjectMake(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                totalimate(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func prevalence() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if SectorThen.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            SectorThen.shared.toExecute()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension GiftChatController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func giftView(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(data_textWhiteMessage.suffix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!] = (String(appLevelStr))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(data_textWhiteMessage.suffix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ModelThen.objectList(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.withDevice(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((showTitlePath.lowercased() + String(notiItemSocialValue.suffix(6)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(showTitlePath.lowercased() + String(notiItemSocialValue.suffix(6)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.hemAndHaw(cellData, aboveValue: msgInfoDic[(String(k_firstFormat))] as? String, priceDownActive: msgIncome, demonstrate: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        refer(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((appViewStr.lowercased() + "lMfCoin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(appViewStr.lowercased() + "lMfCoin")] as! NSNumber
            //: PunctuateThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            PunctuateThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: PunctuateThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.upWill(needReload: false, mf_coin: PunctuateThen.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func task() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.channelId(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func refer(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.readDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func brisanceScreen(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.afterImage(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func outsideWith() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension GiftChatController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func colorAcrossVoice() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.bagStart { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.toSession()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func videoWith() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.tv { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.toSession()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension GiftChatController {
    //: @objc private func feedbackBtnClick() {
    @objc private func pessimisticNegativeFeedbackLikeSnap() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = InvisibleViewController()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        fileController()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func firstClick() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = SelectReactiveCompatible(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.fullPhaseOfTheMoon(cellTitleList: [(String(constCurrentValue.suffix(9))).localized, (String(show_buttonStr)).localized, (String(mainAttentionName.suffix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .toEvent()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: RangePushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                RangePushManager.share.clickOfUid(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                labAdd()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = TitleView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.toColorView(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func labAdd() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = ToolComponentReactiveCompatible()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.valueExecute(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.nearToUpgrade(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func pin() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            totalimate(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            fileController()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func firstPresent(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ImageTransformable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.selectReceive(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension GiftChatController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func skimOverImage() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(corrigenda(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: notiBeautyInfoMsg,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(colorTo),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: constSkipPath,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func corrigenda(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard PunctuateThen.share.loginUserMode.loungePlus == true else { return }
        guard PunctuateThen.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func colorTo() {
        //: PunctuateThen.share.loginUserMode.loungePlus = true
        PunctuateThen.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func priceFrom() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? EqualCompartmentReactiveCompatible else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? EqualCompartmentReactiveCompatible else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if MoveReactiveCompatible.starve(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.contentInfo(tipStr: (String(show_giftTitle) + String(dataToFormat.suffix(6)) + String(mainPartyId.suffix(6))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension GiftChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func placeHandle() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        CustomPermissionTool.videoOf(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = DirectReactiveCompatible.sizeUponTelecasting(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.successSize(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func pushMake() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        CustomPermissionTool.behindCell(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.successSize(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func successSize(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: PunctuateThen.share.appStatus == AppSkinStatus.normal.rawValue else {
            PunctuateThen.share.appStatus == HiddenHashableRepresentation.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SizeModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.titleTo(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = LayerFlowLayout(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SizeModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.titleTo(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        fileController()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension GiftChatController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func redirect() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard MoveReactiveCompatible.smallness(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == TopComparable.male.rawValue ? "He".localized : (String(data_lastMakeMsg.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.drawObserver(showMsg: (String(notiMaxFormat.suffix(5)) + " using" + String(userUpStr.suffix(5)) + "low " + String(notiTouchTitle.prefix(4)) + "rsion").viewfinder(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.closePicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func closePicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = DirectReactiveCompatible.sizeUponTelecasting(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            DirectReactiveCompatible.take(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.briquetUser(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.drawObserver(showMsg: (String(kTitleText) + String(notiImageMsg.prefix(6)) + "t 5 " + appBurnKey.replacingOccurrences(of: "table", with: "s")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func briquetUser(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: RecordEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = TransgressionThen(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SizeModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.removeChange(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        DirectThen.cutWith()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        fileController()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: EqualCompartmentReactiveCompatible, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func sussOut(msgData: EqualCompartmentReactiveCompatible, sendModel: SizeModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ClickBuildListener.shared.numberry(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.drawObserver(showMsg: (String(mainViewButtonName.suffix(4)) + "ad fai" + const_toUserUrl.replacingOccurrences(of: "observer", with: "ed")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.hemAndHaw(msgData, aboveValue: nil, priceDownActive: NSNumber(value: 0), demonstrate: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.removeChange(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension GiftChatController {
    //: func func__addVoiceNotification() {
    func director() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(activeAgent),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(fileInStopBodyPart),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(forwarding(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(saveFrame(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: const_ageStr,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(compoundNotification(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: user_domainData,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func prixFixe(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            SectorThen.shared.viewSize()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            SectorThen.shared.words()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func activeAgent() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        prixFixe(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func fileInStopBodyPart() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        prixFixe(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func forwarding(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            prixFixe(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            prixFixe(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: SizeCellData, cellView: TalkingChatAudioMsgCell) {
    func welcome(cellData: SizeCellData, cellView: WithMessageCell) {
        //: guard TalkingSocketManager.shared.workSpace == false else {
        guard WrittenRecordSocketDelegate.shared.workSpace == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.drawObserver(showMsg: mainKeyTransformMsg)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? EqualCompartmentReactiveCompatible)!.isKind(of: SizeCellData.self) == false {
            if (indexModel as? EqualCompartmentReactiveCompatible)!.isKind(of: SizeCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! SizeCellData
            let audioModel = indexModel as! SizeCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = ReadoutReactiveCompatible.enableUp(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        SectorThen.shared.toExecute()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if SectorThen.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(noti_sourceData)) + "\(SectorThen.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if SectorThen.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                SectorThen.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        SectorThen.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        SectorThen.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: EqualCompartmentReactiveCompatible = self.msgView.uiMsgs.object(at: index) as! EqualCompartmentReactiveCompatible
                let subModel: EqualCompartmentReactiveCompatible = self.msgView.uiMsgs.object(at: index) as! EqualCompartmentReactiveCompatible
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: SizeCellData.self) {
                if subModel.isKind(of: SizeCellData.self) {
                    //: let audioModel = subModel as! SizeCellData
                    let audioModel = subModel as! SizeCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = ReadoutReactiveCompatible.enableUp(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    QueryQuantityoMsgTable.nearButton(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        SectorThen.shared.dismissLoad(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension GiftChatController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func nameData() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.skin()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.takeDict(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? EqualCompartmentReactiveCompatible
                let indexModel = model as? EqualCompartmentReactiveCompatible
                //: if indexModel?.isKind(of: SizeCellData.self) == true || indexModel?.isKind(of: ChatThen.self) == true || indexModel?.isKind(of: LoadReactiveCompatible.self) == true || indexModel?.isKind(of: DirectCellData.self) == true {
                if indexModel?.isKind(of: SizeCellData.self) == true || indexModel?.isKind(of: ChatThen.self) == true || indexModel?.isKind(of: LoadReactiveCompatible.self) == true || indexModel?.isKind(of: DirectCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.funcNowTip()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.titleWith()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.priceFrom()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func skin() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.putBackThroughFinish(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.toffeeNosed(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.equalView()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == PunctuateThen.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == PunctuateThen.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == PunctuateThen.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == PunctuateThen.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.nearToUpgrade(userWrap)
    }

    //: func func__getFillDraftText() {
    func fillRequest() {
        //: let userID = "c2c_"+targetId
        let userID = (String(show_maxFormat.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.sectionTarget()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func toTitle() {
        //: let userID = "c2c_"+targetId
        let userID = (String(show_maxFormat.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension GiftChatController {
    //: func func__TipsNotification() {
    func scaleOfMeasurement() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(saveBring(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: userTableMessage,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func saveBring(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: SizeThen = userinfo[(String(dataScreenFormat))] as! SizeThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.keyNameure(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func contentInfo(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: user_levelData.map{makeSystem(wrap: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: mainEqualData.map{$0^37}, encoding: .utf8)!: (String(mainIconValue))]
        //: var message: EqualCompartmentReactiveCompatible?
        var message: EqualCompartmentReactiveCompatible?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = ModelThen.plusNeed(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ModelThen.objectList(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.keyNameure(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func titleWith() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        MoveReactiveCompatible.phoneService { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.contentInfo(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func digitizerStr(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.contentInfo(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func funcNowTip() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        MoveReactiveCompatible.pathHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.contentInfo(tipStr: String(bytes: kStrengthTitle.map{ifKit(background: $0)}, encoding: .utf8)!.viewfinder(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func saveFrame(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: EqualCompartmentReactiveCompatible = dict?["cellData"] as! EqualCompartmentReactiveCompatible
        let cellData: EqualCompartmentReactiveCompatible = dict?[(String(noti_viewText.suffix(8)))] as! EqualCompartmentReactiveCompatible
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.keyNameure(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension GiftChatController: RefReactiveCompatible {
    //: func func__setReplyTipsView() {
    func thanView() {
        //: let key =  "\(PunctuateThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(PunctuateThen.share.loginUserMode.userID)" + (String(kContentValue) + String(showNameUrl) + String(noti_playerId.prefix(8))) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && PunctuateThen.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && PunctuateThen.share.appStatus == HiddenHashableRepresentation.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.alongNor()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            heritableUponView(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            toFile()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func addOn(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        sinceFirst(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        toFile()
    }

    //: func removeReplyTipsView() {
    func toFile() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            heritableUponView(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension GiftChatController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func playerDetail() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: const_buttonText, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: notiKeyTitle - noti_bottomId, width: kQueryValue, height: notiKeyTitle)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: kQueryValue,
                                     //: height: ScreenHeight)
                                     height: notiKeyTitle)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func totalimate(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.occurrentWith() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if RangeManager.universalShared().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: show_replaceKey, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: BackgroundDataSource.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: notiKeyTitle - noti_bottomId,
                                     //: width: ScreenWidth,
                                     width: kQueryValue,
                                     //: height: ScreenHeight)
                                     height: notiKeyTitle)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if RangeManager.universalShared().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: show_replaceKey, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension GiftChatController {
    /// 创建UI
    //: private func createUI() {
    private func prideOfPlace() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(noti_colorMessage) + String(user_appId)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func playConstraints() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? noti_bottomId : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.areaRemoveRadiiAt(width: contentView.width,
                                          //: height: contentView.height,
                                          height: contentView.height,
                                          //: corners: [.topLeft, .topRight],
                                          corners: [.topLeft, .topRight],
                                          //: cornerRadii: .init(width: 8, height: 8))
                                          cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? constGameData : mainKeyValue)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + k_netId)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? constGameData : mainKeyValue)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + k_netId)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + k_netId)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func shove() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.pin()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.firstClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.circle(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                const_screenFormat.occurrenceIn(eventID: noti_videoData, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
